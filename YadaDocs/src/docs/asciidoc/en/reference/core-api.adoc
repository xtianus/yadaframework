:docinfo: shared
= Core API Reference (auto-generated)

Generated from source (auto-extracted). Lists public methods per class (signatures collapsed to one line); includes Javadoc summary when present.

== net.yadaframework.components
=== YadaAttachedFileCloneSet (YadaWeb/src/main/java/net/yadaframework/components/YadaAttachedFileCloneSet.java)
- `YadaAttachedFileCloneSet() throws IOException`
- `void moveAll(Map<String, String> oldRelativeNewRelative) throws IOException` - Move all collected files to some relative folder (will be created when missing)

=== YadaDataTableFactory (YadaWeb/src/main/java/net/yadaframework/components/YadaDataTableFactory.java)
- `YadaDataTableProxy getSingleton(String id, @Nullable Locale locale)` - Get an existing YadaDataTable instance identified by id and locale.
- `YadaDataTableProxy getSingleton(String id, YadaDataTableConfigurer configurer)` - Get or create a YadaDataTable instance identified by id, when no i18n is needed.
- `YadaDataTableProxy getSingleton(String id, @Nullable Locale locale, YadaDataTableConfigurer configurer)` - Get or create a YadaDataTable instance identified by id.

=== YadaDateFormatter (YadaWeb/src/main/java/net/yadaframework/components/YadaDateFormatter.java)
- `YadaDateFormatter()`
- `Date parse(final String text, final Locale locale) throws ParseException` - Chiamato automaticamente da Spring per convertire una stringa in un oggetto Date con il formato html5
- `String print(final Date object, final Locale locale)` - Chiamato automaticamente da Spring per convertire un oggetto Date in una stringa con il formato html5
- `Date parseLocal(final String text, final Locale locale) throws ParseException` - Metodo di utilità da usare per convertire una stringa in una data formattata come specificato in messages.properties (quindi locale-dependent)
- `String printLocal(final Date object, final Locale locale)` - Metodo di utilità da usare per convertire una data in una stringa formattata come specificato in messages.properties (quindi locale-dependent). Si può usare in pagina così: th:text="${@yadaDateFormatter.printLocal(story.publishDate, #locale)}" Però non è molto meglio dello standard thymeleaf th:with="df=#{date.format}" th:text="${#calendars.format(story.publishDate, df)}"

=== YadaEmailBuilder (YadaWeb/src/main/java/net/yadaframework/components/YadaEmailBuilder.java)
- `static YadaEmailBuilder instance(String emailName, Locale locale, YadaEmailService yadaEmailService)` - Get a new instance of email builder to send an email.
- `YadaEmailBuilder from(String fromEmail, Optional<String> fromName)`
- `YadaEmailBuilder from(String ...fromNameAndEmail)`
- `YadaEmailBuilder to(String...toEmail)` - batch mode is used, in which case a copy of the same email is sent to each recipient.
- `YadaEmailBuilder replyTo(String replyTo)` - Specify a "reply-to" field in the email. This is really useful only if the "from" email is different from this value. Actually if they don't differ, better leave this out so that the from name is also used when replying in the email client.
- `YadaEmailBuilder subjectParams(Object...subjectParams)` - Assigns values to subject parameters as {0}, {1} etc. - can be omitted when the subject has no parameters.
- `YadaEmailBuilder addModelAttribute(String name, Object value)` - Thymeleaf model attribute to use in the template.
- `YadaEmailBuilder modelAttributes(Map<String, Object> templateParams)` - Thymeleaf model attributes to use in the template - can be null.
- `YadaEmailBuilder addInlineResources(String cidname, String path)` - Add an inline image to be used with src="cid:somekey"
- `YadaEmailBuilder inlineResources(Map<String, String> inlineResources)` - Key-value pairs of inline images to be used with "cid:somekey" as src. The value is a context-relative path, e.g. "/res/img/mylogo.jpg"
- `YadaEmailBuilder addAttachment(String filename, File attachment)` - Add a file attachment.
- `YadaEmailBuilder attachments(Map<String, File> attachments)` - Key-value pairs of filename-File to send as attachment. The filename key should have the correct extension to produce the expected mime type.
- `YadaEmailBuilder addTimestamp(boolean addTimestamp)` - Add a timestamp to the subject - defaults to false. Useful when sending many similar emails to the same recipient so they don't get collapsed by email clients under the same subject. For example, when notification emails are critical.
- `YadaEmailBuilder batch(boolean batch)` - Choose whether to send this email to all recipients as a batch of distinct emails
- `YadaEmailBuilder batch()` - Send this email to all recipients as a batch of distinct emails
- `boolean send()` - Send the email.

=== YadaEmailService (YadaWeb/src/main/java/net/yadaframework/components/YadaEmailService.java)
- `String buildLink(String relativeLink)` - Convert a site-relative link to absolute, because in emails we can't use @{}. Example: th:href="${@yadaEmailService.buildLink('/read/234')}"
- `boolean sendSupportRequest(String username, String supportRequest, HttpServletRequest request, Locale locale)`
- `boolean sendHtmlEmail(String[] toEmail, String emailName, Object[] subjectParams, Map<String, Object> templateParams, Map<String, String> inlineResources, Locale locale, boolean addTimestamp)`
- `boolean sendHtmlEmail(String[] toEmail, String replyTo, String emailName, Object[] subjectParams, Map<String, Object> templateParams, Map<String, String> inlineResources, Locale locale, boolean addTimestamp)` - Invia una email usando un template thymeleaf. Il template è localizzato, per cui si può chiamare ad esempio <emailName>.html oppure <emailName>_de.html. Il subject è localizzato e parametrizzato, e la sua chiave è email.subject.<emailName>
- `boolean sendHtmlEmail(String[] fromEmail, String[] toEmail, String replyTo, String emailName, Object[] subjectParams, Map<String, Object> templateParams, Map<String, String> inlineResources, Map<String, File> attachments, Locale locale, boolean addTimestamp)`
- `boolean sendHtmlEmail(String[] fromEmail, String[] toEmail, String replyTo, String emailName, Object[] subjectParams, Map<String, Object> templateParams, Map<String, String> inlineResources, Map<String, File> attachments, Locale locale, boolean addTimestamp, boolean batch)` - Invia una email usando un template thymeleaf. Il template è localizzato, per cui si può chiamare ad esempio <emailName>.html oppure <emailName>_de.html. Il subject è localizzato e parametrizzato, e la sua chiave è email.subject.<emailName>
- `boolean sendHtmlEmail(YadaEmailParam yadaEmailParam)` - Invia una email usando un template thymeleaf. Il template è localizzato, per cui si può chiamare ad esempio <emailName>.html oppure <emailName>_de.html. Il subject è localizzato e parametrizzato, e la sua chiave è email.subject.<emailName>
- `boolean sendHtmlEmail(YadaEmailParam yadaEmailParam, boolean batch)` - Send a HTML email. Better use {@link YadaEmailBuilder} for a cleaner api
- `String loadTemplate(Locale locale, Map<String, Object> templateParams, String emailName)`
- `String getTemplatePath(String templateName)`
- `String getMailTemplateFile(String templateNameNoHtml, Locale locale)` - Dato un nome di template senza estensione, ne ritorna il nome completo di localizzazione. Per esempio "saluti" diventa "saluti_it" se esiste, altrimenti resta "saluti". Se il template non esiste, lancia InternalException
- `boolean sendEmail(String[] from, String to, String replyTo, String cc, String bcc, String subject, String body, boolean html, File inlineFile, String inlineFilename)` - Send an email by specifying the content directly, without a template
- `boolean sendEmail(YadaEmailContent yadaEmailContent)`
- `boolean sendEmailBatch(List<YadaEmailContent> yadaEmailContents)`
- `String timestamp(Locale locale)`
- `String timestamp()`

=== YadaFileManager (YadaWeb/src/main/java/net/yadaframework/components/YadaFileManager.java)
- `YadaManagedFile moveToTemp(YadaManagedFile yadaManagedFile) throws IOException` - Move the file to the public temp folder for later processing.
- `boolean delete(YadaManagedFile managedFile)` - Remove a managed file from disk and database
- `File getAbsoluteFile(YadaManagedFile managedFile)` - Returns the absolute path of a managed file
- `// public void delete(YadaManagedFile managedFile)` - //	/** //	 * Deletes a file from disk and database //	 * @param managedFile the file to delete //	 */
- `YadaAttachedFile duplicateFiles(YadaAttachedFile yadaAttachedFileCopy, YadaAttachedFileCloneSet yadaAttachedFileCloneSet) throws IOException` - You don't usually need to call this method but {@link YadaUtil#copyEntity(net.yadaframework.core.CloneableFiltered)} instead.<br> Makes a copy of just the filesystem files. New names are generated from the old ones by appending an incremental number. The input YadaAttachedFile is updated with the new names. The old files are not deleted. This method is used by YadaUtil.copyEntity() when a field is a YadaAttachedFile so that files are copied too. 	      This is useful when the final path depends on the id 		  of a cloned object so it can't be determined during cloning. 		  The method yadaAttachedFileCloneSet.moveAll() will have to be called after the clone has been persisted.
- `File getAbsoluteMobileFile(YadaAttachedFile yadaAttachedFile)` - Returns the absolute path of the mobile file
- `File getAbsoluteDesktopFile(YadaAttachedFile yadaAttachedFile)` - Returns the absolute path of the desktop file
- `File getAbsolutePdfFile(YadaAttachedFile yadaAttachedFile)` - Returns the absolute path of the pdf file
- `File getAbsoluteFile(YadaAttachedFile yadaAttachedFile)` - Returns the absolute path of the default file (no mobile/desktop variant)
- `File getAbsoluteFile(YadaAttachedFile yadaAttachedFile, String filename)` - Returns the absolute path of a file
- `// public YadaAttachedFile findOne(Long yadaAttachedFileId)` - //	/** //	 * Find one from repository //	 * @param yadaAttachedFileId the attachment id //	 */
- `void deleteFileAttachment(Long yadaAttachedFileId)` - Deletes from the filesystem all files related to the attachment
- `void deleteFileAttachment(YadaAttachedFile yadaAttachedFile)` - Deletes from the filesystem all files related to the attachment
- `void deleteFileAttachment(List<YadaAttachedFile> yadaAttachedFiles)` - Deletes from the filesystem all files related to the attachments
- `String getMobileImageUrl(YadaAttachedFile yadaAttachedFile)` - Returns the (relative) url of the mobile image if any, or null
- `String getDesktopImageUrl(YadaAttachedFile yadaAttachedFile)` - Returns the (relative) url of the desktop image. If not defined, falls back to the plain file.
- `String getPdfImageUrl(YadaAttachedFile yadaAttachedFile)` - Returns the (relative) url of the pdf image if any, or null.
- `String getFileUrl(YadaAttachedFile yadaAttachedFile)` - Returns the (relative) url of the file, or null.
- `String getFileUrl(String relativeFolderPath, String filename)` - Returns the url of a YadaAttachedFile that has the given attributes
- `File uploadFile(MultipartFile multipartFile) throws IOException` - Copies a received file to the upload folder. The returned File is the only pointer to the uploaded file.
- `YadaManagedFile manageFile(MultipartFile multipartFile) throws IOException` - Copies a received file to the upload folder. A pointer to the file is stored in the database as YadaManagedFile
- `YadaManagedFile manageFile(MultipartFile multipartFile, String description) throws IOException` - Copies a received file to the upload folder. A pointer to the file is stored in the database as YadaManagedFile
- `YadaAttachedFile attachReplace(YadaAttachedFile currentAttachedFile, MultipartFile multipartFile, String namePrefix) throws IOException` - Replace the file associated with the current attachment The multipartFile is moved to the destination when config.isFileManagerDeletingUploads() is true, otherwise the original is copied and left unchanged.
- `YadaAttachedFile attachReplace(YadaAttachedFile currentAttachedFile, File managedFile, MultipartFile multipartFile, String namePrefix) throws IOException` - Replace the file associated with the current attachment The managedFile is moved to the destination when config.isFileManagerDeletingUploads() is true, otherwise the original is copied and left unchanged.
- `YadaAttachedFile attachReplace(YadaAttachedFile currentAttachedFile, File managedFile, MultipartFile multipartFile, String namePrefix, String targetExtension, Integer desktopWidth, Integer mobileWidth) throws IOException` - Replace the file associated with the current attachment, only if a file was actually attached The managedFile is moved to the destination when config.isFileManagerDeletingUploads() is true, otherwise the original is copied and left unchanged.
- `YadaAttachedFile attachReplace(boolean move, YadaAttachedFile currentAttachedFile, File managedFile, MultipartFile multipartFile, String namePrefix, String targetExtension, Integer desktopWidth, Integer mobileWidth) throws IOException` - Replace the file associated with the current attachment, only if a file was actually attached and left unchanged.
- `YadaAttachedFile attachNew(MultipartFile multipartFile, String relativeFolderPath, String namePrefix) throws IOException` - Copies an uploaded file to the destination folder, creating a database association to assign to an Entity. The name of the file is in the format [basename]managedFileName_id.ext. Images are not resized.
- `YadaAttachedFile attachNew(File managedFile, MultipartFile multipartFile, String relativeFolderPath, String namePrefix) throws IOException` - Copies a managed file to the destination folder, creating a database association to assign to an Entity. The name of the file is in the format [basename]managedFileName_id.ext. Images are not resized.
- `YadaAttachedFile attachNew(File managedFile, MultipartFile multipartFile, String relativeFolderPath, String namePrefix, String targetExtension, Integer desktopWidth, Integer mobileWidth) throws IOException` - Copies (and resizes) a managed file to the destination folder, creating a database association to assign to an Entity. The name of the file is in the format [basename]managedFileName_id.ext
- `YadaAttachedFile attachNew(File managedFile, String clientFilename, String relativeFolderPath, String namePrefix, String targetExtension, Integer desktopWidth, Integer mobileWidth) throws IOException` - Copies (and resizes) a managed file to the destination folder, creating a database association to assign to an Entity. The name of the file is in the format [basename]managedFileName_id.ext
- `YadaAttachedFile attachNew(boolean move, File managedFile, String clientFilename, String relativeFolderPath, String namePrefix, String targetExtension, Integer desktopWidth, Integer mobileWidth) throws IOException` - Copies (and resizes) a managed file to the destination folder, creating a database association to assign to an Entity. The name of the file is in the format [basename]managedFileName_id.ext
- `YadaAttachedFile attach(boolean move, YadaAttachedFile yadaAttachedFile, File managedFile, String clientFilename, String namePrefix, String targetExtension, Integer desktopWidth, Integer mobileWidth) throws IOException` - Performs file copy and (for images) resize to different versions. The managedFile is moved to the destination when config.isFileManagerDeletingUploads() is true, otherwise the original is copied and left unchanged.

=== YadaFormHelper (YadaWeb/src/main/java/net/yadaframework/components/YadaFormHelper.java)
- `boolean isYadaLocalEnum(Object someClass)` - Check if a class is a YadaLocalEnum. For example YadaJobState is a YadaLocalEnum. Used by list.html to check for enum options.

=== YadaJobManager (YadaWeb/src/main/java/net/yadaframework/components/YadaJobManager.java)
- `void init(ContextRefreshedEvent event)` - Start the scheduler. Method called after Spring has finished initializing the Application Context, so that YadaPersistentEnums have been initialized.
- `boolean startJob(YadaJob yadaJob)` - Activate the job so that it becomes available for the scheduler to start it. The scheduled time is left unchanged unless null, when it is set to NOW (start ASAP). This method does nothing to a job that is already scheduled and in the ACTIVE state. Any errors are reset.
- `boolean isJobGroupPaused(String jobGroup)` - Check if the job group has been paused
- `void pauseJobGroup(String jobGroup, boolean interrupt)` - Pause all jobs in the given group
- `void resumeJobGroup(String jobGroup)` - Resume jobs of the given group
- `void deleteJob(YadaJob yadaJob)` - Removes the job from the database.
- `long countActiveOrRunningJobs(String jobGroup)` - Returns the number of jobs for the given group that are in the ACTIVE/RUNNING state
- `List<YadaJob> getAllActiveOrRunningJobs(String jobGroup)` - Returns all jobs for the given group that are in the ACTIVE/RUNNING state, ordered by state and scheduled time. The running jobs are the actual cached instances present in the YadaJobScheduler.
- `void disableAndInterruptJobs(List<? extends YadaJob> yadaJobs)` - Set a list of jobs to DISABLED then interrupt the threads. All jobs are first set to disabled, then they are all interrupted.
- `void completeJob(Long yadaJobId)` - Set a job to COMPLETED
- `void toggleDisabledAndPaused(Long yadaJobId)` - Toggle between paused and disabled.
- `void disableAndInterruptJob(Long yadaJobId)` - Set a job to DISABLED then interrupt its thread
- `void pauseAndInterruptJob(Long yadaJobId)` - Set a job to PAUSED then interrupt its thread
- `YadaJob changeJobPriority(YadaJob yadaJob, int priority)`
- `YadaJob reschedule(YadaJob yadaJob, Date newScheduling)`
- `YadaJob getJobInstance(Long id)` - Returns an instance of a YadaJob. The instance is freshly loaded from the database if no other thread already holds a reference to it, otherwise the instance is shared among threads. It is thus possible for concurrent threads to modify the same entity (= table row) without incurring in a concurrent modification exception. The instance is removed from the cache when no one holds a reference to it anymore. The typical scenario is when a job is already running (an instance has been cached by the scheduler) and a user from the web interface changes its name.
- `YadaJob replaceWithCached(YadaJob yadaJob)` - Call this method after fetching a job instance from the database to replace it with a cached running instance if any.
- `// public YadaJob getJobInstanceIfRunning(Long id)` - //	/** //	 * Returns an instance of YadaJob only if the job is currently running. //	 * @param id //	 * @return the instance or null //	 */

=== YadaJobScheduler (YadaWeb/src/main/java/net/yadaframework/components/YadaJobScheduler.java)
- `void onRemoval(RemovalNotification<Long, YadaJob> removal)`
- `YadaJob load(Long id)`
- `void run()`
- `void onSuccess(Void result)`
- `void onFailure(Throwable thrown)`
- `YadaJob getJobInstance(Long id)` - Returns an instance of a YadaJob. The instance is freshly loaded from the database if no other thread already holds a reference to it, otherwise the instance is shared among threads. It is thus possible for concurrent threads to modify the same entity (= table row) without incurring in a concurrent modification exception. The instance is removed from the cache when no one holds a reference to it anymore. The typical scenario is when a job is already running (an instance has been cached by the scheduler) and a user from the web interface changes its name.
- `YadaJob getJobInstanceIfRunning(Long id)` - Returns an instance of YadaJob only if the job is currently running.

=== YadaJsonMapper (YadaWeb/src/main/java/net/yadaframework/components/YadaJsonMapper.java)
- `YadaJsonMapper getObject(String name)`

=== YadaKeyRateLimiter (YadaWeb/src/main/java/net/yadaframework/components/YadaKeyRateLimiter.java)
- `YadaKeyRateLimiter(long maxEvents, long periodMillis, int maxCacheSize)` - Create a new RateLimiter using milliseconds and will always be valid. Each element in the cache takes just 4 bytes (Integer key) + 4 bytes (IncrementableInteger). A value of 100000 should be fine (the wider the period, the higher the possible different sized probably)
- `YadaKeyRateLimiter(long maxEvents, long period, TimeUnit periodUnit, int maxCacheSize)` - Create a new RateLimiter using the preferred unit of time and will always be valid. Each element in the cache takes just 4 bytes + the hostname length. A value of 100000 should be fine (the wider the period, the higher the possible different sized probably)
- `synchronized boolean validateRate(String key)` - To be called at every event
- `synchronized long getCurrentRate(String key)`
- `static void main(String[] args) throws InterruptedException` - Test and example method

=== YadaLocalePathVariableFilter (YadaWeb/src/main/java/net/yadaframework/components/YadaLocalePathVariableFilter.java)
- `static final String CALLED_FLAG = YadaLocalePathVariableFilter.class.getName() + ".CALLED";`
- `static final String ORIGINAL_REQUEST = YadaLocalePathVariableFilter.class.getName() + ".ORIGINAL_REQUEST";`
- `// public final static void resetCalledFlag(HttpServletRequest request)`
- `void doFilter(ServletRequest servletRequest, ServletResponse response, FilterChain filterChain) throws IOException, ServletException`
- `void init(FilterConfig filterConfig) throws ServletException`
- `void destroy()`

=== YadaLongRunningExclusive (YadaWeb/src/main/java/net/yadaframework/components/YadaLongRunningExclusive.java)
- `T execute(Object...arguments) throws Exception` - Execute a long running operation that should not be invoked again before it's finished
- `T execute(String runningUsername, Object...arguments) throws Exception` - Execute a long running operation that should not be invoked again before it's finished
- `boolean isExecuting()`
- `Date getLastSuccessfulRun()` - Returns the timestamp of the last run that didn't generate an exception.
- `Exception getLastError()` - Returns the exception generated in the last run, if any.
- `T getLastResult()` - Returns the last result. Can be stale if the long running operation has yet to terminate
- `String getRunningUsername()` - Get the name of the user who is running (or has last run) the task

=== YadaMariaDB (YadaWeb/src/main/java/net/yadaframework/components/YadaMariaDB.java)
- `static DB openEmbeddedDB(DBConfiguration config) throws ManagedProcessException` - This factory method is the mechanism for opening an existing embedded database for use. This method assumes that the database has already been prepared for use.
- `static DB openEmbeddedDB(int port) throws ManagedProcessException` - This factory method is the mechanism for opening an existing embedded database for use. This method assumes that the database has already been prepared for use with default configuration, allowing only for specifying port.

=== YadaMariaDBServer (YadaWeb/src/main/java/net/yadaframework/components/YadaMariaDBServer.java)
- `YadaMariaDBServer(YadaConfiguration config)`
- `Integer getPort()`

=== YadaNotify (YadaWeb/src/main/java/net/yadaframework/components/YadaNotify.java)
- `YadaNotifyData empty(Model model)` - Initialise an empty instance
- `YadaNotifyData empty(RedirectAttributes redirectAttributes)` - Initialise an empty instance
- `YadaNotifyData title(String title, Model model)` - Initialise the instance with a title
- `YadaNotifyData title(String title, Model model, Locale locale)` - Initialise the instance with a title and a specific locale
- `YadaNotifyData title(String title, RedirectAttributes redirectAttributes)` - Initialise the instance with a title, to be displayed after a redirect
- `YadaNotifyData title(String title, RedirectAttributes redirectAttributes, Locale locale)` - Initialise the instance with a title and a specific locale, to be displayed after a redirect
- `YadaNotifyData titleKey(Model model, String ... titleKeyAndArgs)` - Initialise the instance with a title formed with a key and its optional parameters for the message.properties in the Request locale
- `YadaNotifyData titleKey(Model model, Locale locale, String ... titleKeyAndArgs)` - Initialise the instance
- `YadaNotifyData titleKey(RedirectAttributes redirectAttributes, String ... titleKeyAndArgs)` - Initialise the instance
- `YadaNotifyData titleKey(RedirectAttributes redirectAttributes, Locale locale, String ... titleKeyAndArgs)` - Initialise the instance
- `boolean clearAll(Model model, RedirectAttributes redirectAttributes, HttpServletRequest request)` - Clear all previously added messages.
- `boolean isNotificationPending(HttpServletRequest request)` - Test if a modal is going to be opened when back to the view (usually after a redirect)
- `boolean isNotificationPending(Model model, RedirectAttributes redirectAttributes)` - Test if a modal is going to be opened when back to the view
- `boolean isErrorSet(Model model, RedirectAttributes redirectAttributes)` - Return true if error() has been called before
- `static YadaNotify instance(Model model)` - Create a new YadaNotify instance.
- `static YadaNotify instance(RedirectAttributes redirectAttributes)` - Create a new YadaNotify instance to be shown after a redirect
- `String yadaSave()` - Makes the notification active. Can be called many times to add different notifications, even on the same instance.
- `YadaNotify yadaTitle(String title)` - Set the notification title
- `YadaNotify yadaMessage(String message)` - Set the notification message. Can be HTML.
- `YadaNotify yadaAutoclose(long milliseconds)` - Set the autoclose time in milliseconds - no close button is rendered
- `YadaNotify yadaReloadOnClose()` - Set the page to reload when the modal is closed
- `YadaNotify yadaMessageSource(MessageSource messageSource, Locale locale)` - Set localization parameters
- `YadaNotify yadaTitleKey(String ... titleKeyAndArgs)` - Set the notification title using localization
- `YadaNotify yadaMessageKey(String ... messageKeyAndArgs)` - Set the notification message using localization. Can be HTML.
- `YadaNotify redirectOnClose(String path)` - The page will redirect on modal close
- `YadaNotify script(String scriptId)` - Add a script id to call when opening the notification modal. The script must be inserted into "/script.html"
- `YadaNotify yadaOk()` - Set the notification severity
- `YadaNotify yadaInfo()` - Set the notification severity
- `YadaNotify yadaError()` - Set the notification severity
- `static boolean isNotifyModalPending(HttpServletRequest request)` - Test if a modal is going to be opened when back to the view (usually after a redirect)
- `static boolean isYadaError(Model model, RedirectAttributes redirectAttributes)` - Return true if yadaError() has been called before
- `static boolean isYadaNotifySaved(Model model, RedirectAttributes redirectAttributes)` - Check if the YadaNotify.yadaSave() has been called in this request
- `String getViewName()` - Return the view name for the yada notification modal. Can be used when adding many messages and the last message is not at the method end. Also used in html to get the correct modal. Note: this always returns the yada notification modal. Use {@code yadaConfiguration.getNotifyModalView()} if your application has a custom notification modal configured in conf.webapp.prod.xml

=== YadaNotifyData (YadaWeb/src/main/java/net/yadaframework/components/YadaNotifyData.java)
- `String add()` - Makes the notification active. Can be called many times to add different notifications, even on the same instance, after setting a new title/message/severity.
- `YadaNotifyData keepLoader()` - The notification modal does not turn off any existing loader
- `YadaNotifyData keepLoader(Boolean keepLoader)` - The notification modal does not turn off any existing loader when keepLoader is true
- `YadaNotifyData setTitle(String title)` - Set the notification title
- `YadaNotifyData setTitleKey(String ... titleKeyAndArgs)` - Set the notification title using localization
- `YadaNotifyData message(String message)` - Set the notification message. Can be HTML.
- `YadaNotifyData message(String messageFormat, Object... params)` - Set the notification message with slf4j-style parameters. Can be HTML.
- `YadaNotifyData autoclose(long milliseconds)` - Set the autoclose time in milliseconds - no close button is rendered
- `YadaNotifyData center()` - Vertically center the modal (with Bootstrap 4)
- `YadaNotifyData addClasses(String classes)` - Add a space-separated list of classes to add to the notification .modal-dialog element
- `YadaNotifyData reloadOnClose()` - Set the page to reload when the modal is closed
- `YadaNotifyData messageKey(String ... messageKeyAndArgs)` - Set the notification message using localization. Can be HTML.
- `YadaNotifyData redirectOnClose(String path)` - The page will redirect on modal close
- `YadaNotifyData callScript(String scriptId)` - Add a script id to call when opening the notification modal. The script must be inserted into "/script.html"
- `YadaNotifyData ok(boolean active)` - Set the notification severity if active is true
- `YadaNotifyData info(boolean active)` - Set the notification severity if active is true
- `YadaNotifyData error(boolean active)` - Set the notification severity if active is true
- `YadaNotifyData ok()` - Set the notification severity. This is the default.
- `YadaNotifyData info()` - Set the notification severity
- `YadaNotifyData error()` - Set the notification severity

=== YadaSecurityUtilStub (YadaWeb/src/main/java/net/yadaframework/components/YadaSecurityUtilStub.java)
- `void onApplicationEvent(ContextRefreshedEvent event)`
- `boolean checkUrlAccess(HttpServletRequest request, String path)` - Checks if the current user has access to the specified path

=== YadaSetup (YadaWeb/src/main/java/net/yadaframework/components/YadaSetup.java)
- `void init() throws Exception`

=== YadaSimpleRateLimiter (YadaWeb/src/main/java/net/yadaframework/components/YadaSimpleRateLimiter.java)
- `YadaSimpleRateLimiter(long maxEvents, long periodMillis)` - Create a new RateLimiter using milliseconds
- `YadaSimpleRateLimiter(long maxEvents, long period, TimeUnit periodUnit)` - Create a new RateLimiter using the preferred unit of time
- `synchronized boolean validateRate()` - To be called at every event
- `synchronized long getCurrentInvalidCounter()` - Return the number of events that have been flagged as invalid in this period so far
- `synchronized long getAndResetHighestInvalidCounter()`
- `static void main(String[] args) throws InterruptedException` - Test and example method

=== YadaSleepingRateLimiter (YadaWeb/src/main/java/net/yadaframework/components/YadaSleepingRateLimiter.java)
- `YadaSleepingRateLimiter(long totElements, long periodMillis)` - Create a new RateLimiter using milliseconds
- `YadaSleepingRateLimiter(long totElements, long period, TimeUnit periodUnit)` - Create a new RateLimiter using the preferred unit of time
- `YadaSleepingRateLimiter(long totElements, String configRateLimiterPath, YadaConfiguration config)` - Create a new RateLimiter reading values from the given configuration path, which must have attributes @hours and @maxmillis, e.g. &lt;rateLimiter hours="6" maxmillis="30000" />
- `void setMaxSleepMilliseconds(long maxSleepMillis)` - The maximum amount of time that the limiter sleeps on a single invocation
- `void sleepWhenNeeded()`
- `static void main(String[] args)` - Test and example method

=== YadaUtil (YadaWeb/src/main/java/net/yadaframework/components/YadaUtil.java)
- `final static YadaUtil INSTANCE = new YadaUtil();` - Instance to be used when autowiring is not available
- `void init()`
- `void ensurePoiTempFolder()` - Ensure the temporary folder for POI files exists and is writable. It must be called every time before using POI because the temp folder is periodically deleted by tomcat and POI does not recreate it automatically (on linux).
- `static <T> T lazyUnsafeInit(T instance, Supplier<T> initializer)`
- `static <T> List<T> lazyUnsafeInit(List<T> list)`
- `List<Path> getFilesInFolder(Path folderPath, String contains) throws IOException` - Returns a list of files from a folder where the name contains the given string, sorted alphabetically
- `String getRandomString(int length)` - Returns a random string of the given length with characters in the range "A".."Z", "a".."z", "0".."9"
- `String getRandomText(int minlen)` - Returns a random string (currently an hex random number)
- `String joinIfNotEmpty(@NotNull String separator, String...toJoin)` - Joins a number of strings, adding a separator only when the strings are not empty. In other words, null or empty strings are skipped without adding a separator.
- `String getTimestampAsRelative(ZonedDateTime timestamp, Locale locale, Integer maxHours)` - Given a date in the past, returns a string like "12 minutes ago", "2 hours ago", "today at 12:51", "yesterday at 5:32"... For dates before yesterday, the full RFC_1123 format is used, as 'Tue, 3 Jun 2008 11:05:30 GMT'. No "x days ago" format is currently provided. 			The default is 3 when null. There is no maximum value, in order to have a "76 hours ago" result if needed.
- `double stringToDouble(String value, Locale locale) throws ParseException` - Parse a string as a double, using the correct decimal separator (if any).
- `<T> void addIfNotNull(List<T> list, T element)` - Add an element to the list only if the element is not null
- `<T> void addIfMissing(List<T> list, T element)` - Add an element to the list only if not there already
- `Set<String> getEmptySortedSet(List<String> sortOrder)` - Create a new TreeSet that sorts values according to the order specified in the parameter. Values that are missing from sortOrder are sorted alphabetically
- `int compare(String left, String right)`
- `List<LocalDate> getDays(Date fromInclusive, Date toInclusive)` - Returns a list of days between two dates included. Days are at midnight in the system timezone.
- `List<LocalDate> getDays(LocalDate fromInclusive, LocalDate toInclusive)` - Returns a list of days between two dates included.
- `Date getDateFromDateTimeIsoString(String isoDateString, String isoTimeString, TimeZone timezone)` - Given a ISO date, a ISO time and a timezone, return the Date.
- `String getRfcDateTimeStringForTimezone(Date date, TimeZone timezone, Locale locale)` - Returns a string for date and time in the specified timezone and locale
- `String getIsoDateStringForTimezone(Date date, TimeZone timezone)` - Convert a date in the timezone to a ISO string, like '2011-12-03'
- `String getIsoTimeStringForTimezone(Date time, TimeZone timezone)` - Convert a time in the timezone to a ISO string, like '10:15' or '10:15:30'
- `String getIsoDateTimeStringForTimezone(Date dateTime, TimeZone timezone)` - Convert a datetime in the timezone to a ISO string, like '2011-12-03T10:15:30'
- `Map<String, Object> makeJsonObject()` - Create a single empty "json" object for use in other methods. Json objects are actually maps that get converted by Spring on return.
- `Map<String, Object> getJsonObject(Map<String, Object> jsonSource, String objectPath)` - Given a json stored as a map, returns the json at the specified key. Non need for this method if the objectPath is a simple key: just use the Map get(key) method.
- `Map<String, Object> getJsonObject(Map<String, Object> jsonSource, String listPath, int listIndex)` - Given a json stored as a map, returns the json at the specified list index
- `List<Object> getJsonArray(Map<String, Object> jsonSource, String objectPath)` - Given a json stored as a map, returns the json array at the specified key Non need for this method if the objectPath is a simple key: just use the Map get(key) method.
- `Object getJsonAttribute(Map<String, Object> jsonSource, String objectPath)` - Given a json stored as a map, returns the value at the specified key, with optional nesting. No need for this method if the objectPath is a simple key: just use the Map get(key) method.
- `Map<String, Object> makeJsonObject(Map<String, Object> parentObject, String path)` - Creates an empty json object at the given path, if missing. Any missing array cells are also created with an empty object as needed.
- `void setJsonAttribute(Map<String, Object> jsonObject, String path, Object value)` - Sets a json property at the given path. Non need for this method if the path is a simple key: just use the Map put(key, value) method. Any missing array cells are also created with an empty object as needed.
- `List<String> getTimezones()` - Returns a list of user-friendly timezones like "Europe/Rome"
- `List<String> getTimezoneOffsets(String prefix)` - Get a list of GMT/UTC time offsets from UTC-12:00 to UTC+14:00
- `boolean isEmailValid(String email)` - Simple email address syntax check: the format should be X@Y.Y where X does not contain @ and Y does not contain @, nor . at the edges. Also no spaces anywhere.
- `String mapToString(Map<String, String> stringMap)` - Convert a map of strings to a commaspace-separated string of name=value pairs
- `int getRandom(int minIncluded, int maxIncluded)` - Returns a random integer number
- `Class<?> findGenericClass(Object specificClassInstance)` - Given the instance of a "specific" class created specifying a single type T while extending a generic class, retrieve the class of the type T. It also works when looking for the generic super-super class at any hierarchy level. Example: the generic class is public abstract class Shape<T extends Color> {...} the specific class is public class Circle extends Shape<Red> the instance is new Circle() the returned value is Red.class
- `void joinFiles(Path sourceFolder, String sourceFilePattern, File outputFile, Integer depth, Boolean deleteSource) throws IOException` - Merges all files matched by a pattern, in no particular order.
- `File makeTempFolder() throws IOException`
- `String relativize(File ancestorFolder, File descendantFolder)` - Finds the path between two files or folders using forward (unix) slashes as a separator
- `String relativize(Path ancestorPath, Path descendantPath)` - Finds the path between two files or folders using forward (unix) slashes as a separator. It works even if the two arguments point to places in different trees.
- `String[] splitHtml(String htmlToSplit, int splitPos)` - Split an HTML string in two parts, not breaking words, handling closing and reopening of html tags. Useful when showing some part of a text and the whole of it after a user clicks. For example, the string "&lt;p>Some text here&lt;/p> becomes ["&lt;p>Some text&lt;/p>","&lt;p>here&lt;/p>"]. The HTML is not splitted exactly at splitPos if there's a word there, a tag, or if the paragraph ends in the next 20 characters: in such cases the split position is increased accordingly. Note: does not work in any possible scenario. For example &lt;ul>&lt;li> is not split properly because it creates two list entries if the split point is inside the li. Tag attributes are not currently handled properly.
- `String findAvailableFilename(String baseName, String extensionNoDot, String counterSeparator, Set<String> usedNames) throws IOException` - Ensure that the given filename has not been already used, by adding a counter. For example, if baseName is "dog" and usedNames is {"dog.jpg", "dog_1.jpg", "dog_2.jpg"}, the result will be "dog_3.jpg" The usedNames array doesn't have to contain identical or sequential baseNames: {"dog.jpg", "cat.jpg", "dog_2.jpg"} This version does not check if a file exists on disk. For that, see {@link #findAvailableName(File, String, String, String)} This method can be used with any strings, not necessarily filenames: just use null for the extension.
- `boolean dateValid(Date someDate, Integer maxYears)` - Check if a date is not more than maxYears years from now, not in an accurate way. Useful to check validity of a date coming from the browser.
- `YadaIntDimension getImageDimensionDumb(File imageFile)`
- `YadaIntDimension getImageDimension(File imageFile)` - Gets the image dimensions considering the EXIF orientation flag. Remember to use the "-auto-orient" flag of the ImageMagick convert command. If the EXIF width and height information is missing, the getImageDimensionDumb() method is called instead. See https://www.impulseadventure.com/photo/exif-orientation.html
- `String getCurrentStackTraceFormatted()` - Returns the current stack trace as a string, formatted on separate lines
- `<T> T getRandomElement(List<T> list)` - Returns a random element from the list
- `static String formatTimeInterval(long amount, TimeUnit timeUnit)` - Convert from an amount of time to a string in the format xxd:hh:mm:ss
- `void autowire(Object instance)` - Perform autowiring of an instance that doesn't come from the Spring context, e.g. a JPA @Entity. Post processing (@PostConstruct etc) is also performed but initialization is not.
- `Object autowireAndInitialize(Object instance)` - Perform autowiring of an instance that doesn't come from the Spring context, e.g. a JPA @Entity or normal java instance made with new. Post processing (@PostConstruct etc) and initialization are also performed. Beans from the WebApplicationContext like @Controller are not injected because they don't belong to the root Application Context used here. If you need to do that, use {@link YadaWebUtil#autowireAndInitialize(Object)}
- `static String stripCounterFromFilename(String filename, String counterSeparator)` - Remove a counter that has been added by {@link #findAvailableName}
- `File findAvailableNameHighest(File targetFolder, String baseName, String extensionNoDot, String counterSeparator)` - Returns a file that doesn't already exist in the specified folder with the specified leading characters (baseName) and optional extension. The file will always have a number at the end that is higher than any other numbers on similar files in the folder. Can be used to find both files and folders.
- `static File findAvailableName(File targetFolder, String baseName, String extensionNoDot, String counterSeparator) throws IOException` - Creates an empty file that doesn't already exist in the specified folder with the specified leading characters (baseName). A counter may be appended to make the file unique. This operation is thread safe.
- `static File findAvailableName(File targetFile, String counterSeparator) throws IOException` - Creates a file with a unique filename by appending a number after the specified separator if needed. If the targetFile exists already, a new file is created with a proper counter at the end. The counter may be stripped altogether (if the original file had a counter and no file without counter exists) or added or incremented. The new counter might not be higher than the original one, nor sequential. It depends on what's already on the filesystem. This operation is thread safe.
- `static <targetClass> void prefetchLocalizedStrings(targetClass fetchedEntity, Class<?> targetClass, String...attributes)` - Force initialization of localized strings implemented with Map&lt;Locale, String>. It must be called in a transaction.
- `static <targetClass> void prefetchLocalizedStringsRecursive(targetClass fetchedEntity, Class<?> targetClass, String...attributes)` - Force initialization of localized strings implemented with Map&lt;Locale, String>. It must be called in a transaction.
- `static <entityClass> void prefetchLocalizedStringListRecursive(List<entityClass> entities, Class<?> entityClass, String...attributes)` - Force initialization of localized strings implemented with Map&lt;Locale, String>. It must be called in a transaction.
- `void doWith(Field field) throws IllegalArgumentException, IllegalAccessException`
- `boolean matches(Field field)`
- `static <entityClass> void prefetchLocalizedStringList(Collection<entityClass> entities, Class<?> entityClass, String...attributes)` - Force initialization of localized strings implemented with Map&lt;Locale, String>. It must be called in a transaction.
- `void doWith(Field field) throws IllegalArgumentException, IllegalAccessException`
- `boolean matches(Field field)`
- `static String getObjectToString(Object object)`
- `static String getLocalValue(Map<Locale, String> LocalizedValueMap)` - Returns the localized value from a map of Locale -> String. Used in entities with localized string attributes. If a default locale has been configured with <code>&lt;locale default='true'></code>, then that locale is attempted when there is no value (null or "") for the needed locale (and they differ)
- `static String getLocalValue(Map<Locale, String> localizedValueMap, Locale locale)` - Returns the localized value from a map of Locale -> String. Used in entities with localized string attributes. If a default locale has been configured with <code>&lt;locale default='true'></code>, then that locale is attempted when there is no value for the needed locale (and they differ) If the map is null, returns the empty string.
- `boolean deleteFileSilently(Path file)` - Deletes a file without reporting any errors.
- `boolean closeSilently(Closeable closeable)` - Close a closeable ignoring exceptions and null.
- `void sleepRandom(long minMilliseconds, long maxMilliseconds)`
- `void sleep(long milliseconds)`
- `String md5Hash(String clear) throws NoSuchAlgorithmException` - Create a MD5 hash of a string (from http://snippets.dzone.com/posts/show/3686)
- `long copyStream(InputStream inputStream, OutputStream outputStream, Integer bufferSize, Long sizeLimit) throws IOException` - Copies an inputStream to an outputStream. This method blocks until input data is available, end of file is detected, or an exception is thrown. Streams are not closed. Use a small buffer (256) when data is over the internet to prevent timeouts somewhere. Use a big buffer for in-memory or disk operations.
- `Field getFieldNoTraversing(Class rootClass, String attributeName)`
- `Class getType(Class rootClass, String attributePath) throws NoSuchFieldException, SecurityException` - Reflection to get the type of a given field, even nested or in a superclass.
- `static String getMessage(String key, @Nullable Object ... params)` - Ritorna un messaggio localizzato da un contesto fuori da Spring (ad esempio in un enum o un Entity)
- `Object getNewInstanceSamePackage(Class anyClassInPackage, String simpleClassName)` - Create an instance of a class that belongs to the same package of some given class
- `static List<Class> getClassesInPackage(Package thePackage)` - Return all the classes of a given package.
- `static <T> T getBean(Class<T> beanClass, Object ... args)` - Get any bean defined in the Spring ApplicationContext
- `static Object getBean(String nameInApplicationContext, Object ... args)` - Get any bean defined in the Spring ApplicationContext
- `static Object getBean(String nameInApplicationContext)` - Get any bean defined in the Spring ApplicationContext
- `Date minutesAgo(int minuti)` - Ritorna la data nel passato per il numero di minuti indicati
- `Date daysAgo(int days)` - Ritorna la data nel passato per il numero di giorni indicati
- `boolean deleteAll(File folder, String prefix)` - Delete all files in a folder that have the specified prefix
- `boolean deleteSilently(File file)` - Delete a file ignoring errors.
- `boolean deleteIfEmpty(Path folder)` - Deleted a folder only when empty
- `int cleanupFolder(Path folder, String prefix)` - Removes files from a folder starting with the prefix (can be an empty string) The folder itself is not removed. return the number of deleted files
- `int cleanupFolder(Path folder, String prefix, Date olderThan)` - Removes files from a folder starting with the prefix (can be an empty string) and older than the given date. The folder itself is not removed.
- `String getFileNoPath(String fileWithPath)` - Returns the file name given the file path
- `static String[] splitFileNameAndExtension(String filename)` - Splits a filename in the prefix and the extension parts. If there is no extension, the second array cell is the empty string
- `String getFileExtension(String filename)` - Da un nome tipo abcd.JPG ritorna "jpg"
- `String getFileExtension(File file)`
- `int shellExec(String command, List<String> args, Map substitutionMap, ByteArrayOutputStream outputStream) throws IOException`
- `int shellExec(String command, List<String> args, Map substitutionMap, ByteArrayOutputStream outputStream, int timeoutSeconds) throws IOException` - Run an external shell command. pair is like "key"-->"value". If the value is a collection, arguments are unrolled so key-->collection will result in key0=val0 key1=val1...
- `int shellExec(String command, List<String> args, ByteArrayOutputStream outputStream) throws IOException` - Run an external shell command without keyword substitution in the parameters.
- `int shellExec(String shellCommandKey, Map substitutionMap) throws IOException` - Run an external shell command that has been defined in the configuration file. The command must be as in the following example: <pre> &lt;imageConvert timeoutseconds="20"> &lt;executable windows="true">magick&lt;/executable> &lt;executable mac="true" linux="true">/usr/local/bin/magick&lt;/executable> &lt;arg>convert&lt;/arg> &lt;arg>${FILENAMEIN}&lt;/arg> &lt;arg>${FILENAMEOUT}&lt;/arg> &lt;/imageConvert> </pre> Be aware that args can not contain "Commons Configuration variables" because they clash with placeholders as defined below. See the yadaframework documentation for full syntax. pair is like "key"-->"value". If the value is a collection, arguments are unrolled so key-->collection will result in key0=val0 key1=val1...
- `int shellExec(String shellCommandKey, Map substitutionMap, ByteArrayOutputStream outputStream) throws IOException` - Run an external shell command that has been defined in the configuration file. The command must be as in the following example: <pre> &lt;imageConvert timeoutseconds="20"> &lt;executable windows="true">magick&lt;/executable> &lt;executable mac="true" linux="true">/usr/local/bin/magick&lt;/executable> &lt;arg>convert&lt;/arg> &lt;arg>${FILENAMEIN}&lt;/arg> &lt;arg>${FILENAMEOUT}&lt;/arg> &lt;/imageConvert> </pre> See the yadaframework documentation for full syntax. pair is like "key"-->"value". If the value is a collection, arguments are unrolled so key-->collection will result in key0=val0 key1=val1...
- `String exec(String command, List<String> args, Map substitutionMap, ByteArrayOutputStream outputStream)` - Esegue un comando di shell pair is like "key"-->"value" . If the value is a collection, arguments are unrolled.
- `String exec(String command, List<String> args, ByteArrayOutputStream outputStream)` - Esegue un comando di shell
- `boolean exec(String shellCommandKey, Map substitutionMap)` - Esegue il comando configurato pair is like "key"-->"value"
- `// public boolean exec(String shellCommandKey, Map<String, String> params)` - //	/** //	 * Esegue il comando configurato //	 * @param shellCommandKey chiave completa xpath del comando shell da eseguire e.g. "config/shell/processTunableWhiteImage" //	 * @param params mappa nome-valore delle variabili da sostituire nel comando, per esempio "{NAME}"="pippo" //	 */
- `boolean isCodiceFiscaleValid(String codiceFiscale)`
- `Throwable getRootException(Throwable exception)` - Trova l'eccezione che ha causato questa, nella catena delle eccezioni
- `String[] splitAtWord(String value, int splitPoint)` - Spezza una stringa in due, circa al carattere splitPoint, ma a fine parola.
- `String abbreviate(String string, int length, boolean breakAtWord)` - Cuts the input string at the given length, optionally keeping the whole last word and adding some dots at the end
- `String abbreviate(String string, int length, boolean breakAtWord, String ellipsis)`
- `static Object copyEntity(CloneableFiltered source)`
- `static Object copyEntity(CloneableFiltered source, boolean setFieldDirectly)`
- `static Object copyEntity(CloneableFiltered source, Class classObject)` - Questo metodo crea la copia di un oggetto TRAMITE I SUOI GETTER (anche privati), facendo in modo che alcune collection/mappe vengano copiate pur restando indipendenti. In pratica le collection/mappe sono ricreate come istanze nuove con i medesimi oggetti di quelle originali. Questo permette di condividere gli oggetti tra le copie, ma di mantenere le associazioni slegate. Così se copio un Prodotto mi trovo gli stessi componenti dell'originale (gli Articolo sono gli stessi) ma posso in seguito toglierli/aggiungerli senza influire sull'altra istanza da cui son partito a copiare. E' possibile specificare quali attributi non copiare grazie all'interfaccia CloneableFiltered. La id non è mai copiata. Per esempio se l'oggetto che si vuole copiare ha l'attributo pippo e l'attributo pluto, si può fare in modo che sia copiato solo pippo e non pluto (che rimane quindi null). Se una collection/mappa deve essere clonata anche nel contenuto, i suoi elementi devono implementare CloneableDeep (vedi LocalString). Per esempio se l'attributo pippo è una collection di oggetti Cane che non implementa ClonableDeep, nella copia verrà creata una nuova collection di oggetti Cane che saranno gli stessi della collection di partenza. Se invcece Cane implementa ClonableDeep, allora gli oggetti Cane contenuti nella copia di pippo sono essi stessi delle copie che seguono le stesse regole qui indicate. ATTENZIONE: - da verificare se gli attributi dei parent sono duplicati pure loro
- `static Object copyEntity(CloneableFiltered source, Class classObject, boolean setFieldDirectly)`
- `static Object copyEntity(CloneableFiltered source, Class classObject, boolean setFieldDirectly, YadaAttachedFileCloneSet yadaAttachedFileCloneSet)` - This is useful when the final path depends on the id 		  of a cloned object so it can't be determined during cloning. 		  The method yadaAttachedFileCloneSet.moveAll() will have to be called after the clone has been persisted.
- `static boolean isType(Class<?> fieldType, Class requiredType)`
- `// public void copyFields(Calendar fromCal, Calendar toCal)` - //	/** //	 * Copy all fields from the first argument to the second, without modifying the timezone. //	 * The purpose is to have the same time in a different timezone. //	 * @param fromCal //	 * @param toCal //	 */
- `static boolean dateWithin(Calendar dateToCheck, int fromMonth, int fromDayInclusive, int toMonth, int toDayExcluded)` - Check if a date is within two dates expressed as month/day, regardless of the year and of the validity of such dates.
- `Date roundBackToHour(Date date)`
- `Calendar roundForwardToHour(Calendar calendar)` - Returns the same calendar object aligned to the next hour
- `Calendar roundBackToHour(Calendar calendar)` - Returns the same calendar object aligned to the previous hour
- `Date roundBackToHour(Date date, TimeZone timezone)` - Resets minutes to zero
- `long daysBetween(ZonedDateTime olderDate, ZonedDateTime earlierDate)` - Returns the days between two dates. It doesn't take into consideration the time component, so the difference between some time yesterday and any other time today will always be 1
- `int daysDifference(Date date1, Date date2)` - Counts the days interval between two dates. Time component is ignored.
- `static long minutesDifference(Date recentDate, Date oldDate)` - Returns the minutes between two dates. It is negative when the first argument is earlier than the second.
- `static long minutesDifferenceAbs(Date firstDate, Date secondDate)` - Returns the absolute value of the minutes between two dates. It will always be positive.
- `static long millisSinceMidnight(Calendar calendar)` - Returns the number of milliseconds since midnight
- `static Date roundBackToMidnight(Date date, TimeZone timezone)`
- `static Calendar roundBackToMidnightClone(Calendar source)` - Create a new calendar rounded back to the start of the day.
- `static Calendar roundBackToMidnight(Calendar calendar)` - Rounds back the calendar to the start of the day.
- `static Calendar roundForwardToAlmostMidnight(Calendar calendar)` - Rounds forward the calendar to the end of the day at 23:59:59.999
- `static Calendar getLastMidnight()` - Returns the last midnight
- `static Calendar addDaysClone(Calendar source, int days)` - Adds or removes the days. The original object is cloned.
- `static Calendar addDays(Calendar calendar, int days)` - Adds or removes the days. The original object is modified.
- `static Calendar addMinutes(Calendar calendar, int minutes)` - Adds or removes the minutes. The original object is modified.
- `static Date addMinutes(Date date, int minutes)` - Aggiunge (o rimuove) i minuti indicati dalla data
- `static Date addHours(Date date, int hours)` - Aggiunge (o rimuove) le ore indicate dalla data
- `static Date addDays(Date date, int days)` - Aggiunge (o rimuove) i giorni indicati dalla data
- `static Date addYears(Date date, int years)` - Aggiunge (o rimuove) gli anni indicati dalla data (approssimato)
- `boolean sameDay(Date a, Date b)` - Returns true if the two dates are on the same day
- `boolean createZipProcess(File zipFile, File[] sourceFiles, String[] filenames, boolean fixNames) throws IOException` - Create a zip of a set of files using an external process. The process must be configured as "config/shell/zipWithRename" and should use zip and zipnote (for renaming). See the /YadaWeb/scripts folder for an example. 		  and if filenames is provided, the renamed file will be forced to have the same 		  extension of the source file (existing extensions will be removed).
- `void createZipFileFromFolders(File zipFile, File[] foldersToZip) throws IOException` - Create a zip file of a folder
- `void createZipFile(File zipFile, File[] sourceFiles, String[] filenamesNoExtension)` - Create a zip of a list of files. An exception is thrown when a source file is not readable. Adapted from http://www.exampledepot.com/egs/java.util.zip/CreateZip.html
- `void createZipFile(File zipFile, File[] sourceFiles, String[] filenamesNoExtension, boolean ignoreErrors)` - Create a zip of a list of files. Adapted from http://www.exampledepot.com/egs/java.util.zip/CreateZip.html
- `static Calendar roundBackToLastMonthStart(Calendar calendar)`
- `static Calendar roundBackToMonth(Calendar calendar)`
- `static Date roundBackToMonth(Date date, TimeZone timezone) //timezone = TimeZone.getTimeZone("GMT");`
- `static Date roundFowardToMonth(Date date, TimeZone timezone) //timezone = TimeZone.getTimeZone("GMT");`
- `static String normalizzaCellulareItaliano(String cellulare)`
- `static boolean validaCellulare(String cellulare)`
- `static SortedSet<Entry<String,String>> sortByValue(Map data)`
- `int compare(Entry<String, String> element1, Entry<String, String> element2)`
- `static SortedSet<Entry<String,String>> sortByKey(Map data)`
- `int compare(Entry<String, String> element1, Entry<String, String> element2)`
- `static ApplicationContext getApplicationContext()` - Ritorna un riferimento all'ApplicationContext per l'utilizzo fuori dal Container (ad esempio negli Entity)
- `void setApplicationContext(ApplicationContext applicationContext)`
- `static String reduceToSafeFilename(String originalFilename)` - Converte un filename in modo che sia valido sia per il filesystem (unix/dos) sia per il browser. E' molto distruttiva in quanto i caratteri non previsti vengono eliminati. Per questo si chiama "reduce" :-) Converte anche a lowercase.
- `static String reduceToSafeFilename(String originalFilename, boolean toLowercase)` - Converte un filename in modo che sia valido sia per il filesystem (unix/dos) sia per il browser. E' molto distruttiva in quanto i caratteri non previsti vengono eliminati. Per questo si chiama "reduce" :-) Converte anche a lowercase.
- `String ensureSafeFilename(String originalFilename)` - Converts a candidate filename so that it is valid on all operating systems and browsers, if needed, and also to lowercase.
- `String ensureSafeFilename(String originalFilename, boolean toLowercase)` - Converts a candidate filename so that it is valid on all operating systems and browsers, if needed.

=== YadaWebUtil (YadaWeb/src/main/java/net/yadaframework/components/YadaWebUtil.java)
- `final static YadaWebUtil INSTANCE = new YadaWebUtil();` - Instance to be used when autowiring is not available
- `final YadaPageRequest FIND_ONE = YadaPageRequest.of(0, 1);`
- `String getFormFragment(String formFragmentViewName)` - Returns the view name of a form fragment for the configured bootstrap version. Example usage: &lt;div th:replace="~{${@yadaWebUtil.getFormFragment('fileUpload')}::body(fieldName='topInd...
- `String getRandomId6()` - Convenience method for generating a random id of 6 characters to be used in pages with "@yadaWebUtil.randomId6"
- `String getRandomId12()` - Convenience method for generating a random id of 12 characters to be used in pages with "@yadaWebUtil.randomId12"
- `boolean isFragment(Object someObject)` - Check if an object holds a thymeleaf fragment instance. To be used in Thymeleaf templates.
- `boolean hasGlobalError(BindingResult binding, String errorCode)` - Checks if a global error with the given code already exists in the binding result. Useful to avoid adding the same error multiple times.
- `<T> BindingResult relinkBindingResult(BindingResult originalBindingResult, T newTargetObject, Model model)` - Creates a new BindingResult associated with a new instance of the same type, preserving all error information from the original BindingResult.
- `boolean isMultipartMissing(MultipartFile multipartFile)` - Returns true if the MultipartFile has not been uploaded at all, not even an empty file
- `void registerDynamicMapping(String newPath, Object controllerInstance, String methodName, RequestMappingHandlerMapping requestMappingHandlerMapping)` - Creates a dynamic @RequestMapping i.e. there's no need for it to be annotated and located in a controller instance. Useful if the url can be user-defined via some cms for example.
- `String getContentUrlRelative(File someContentFile)` - Returns the application-relative url of a file from the contents folder
- `void setCookie(String cookieName, String cookieValue, int expirationDays, HttpServletResponse response)` - Set a cookie
- `String getCookieValue(HttpServletRequest request, String cookieName)` - Returns the value of a cookie, null if not defined
- `File saveBase64Image(String base64Image, File targetFileNoExtension)` - Save to disk a base64 image received from javascript.
- `Object autowireAndInitialize(Object instance)` - Perform autowiring of an instance that doesn't come from the Spring context, e.g. a JPA @Entity or normal java instance made with new. Post processing (@PostConstruct etc) and initialization are also performed. This version can inject beans from the WebApplicationContext like @Controllers. You may consider a class hierarchy redesign before using this method.
- `boolean isEmpty(YadaPageRows<?> yadaPageRows)`
- `String getRequestMapping(HttpServletRequest request)` - Returns the last part of the current request, for example from "/some/product" returns "product"
- `String getRequestMapping()` - Returns the last part of the current request, for example from "/some/product" returns "product" This version does not require the request to be passed as parameter.
- `String removeLanguageFromOurUrl(String fullUrlWithHttp, HttpServletRequest request)` - If the url is a full url that points to our server, make it relative to the server and strip any language in the path. The result can be used in thymeleaf @{url} statements and will get the proper browser language when needed.
- `void passThrough(Model model, HttpServletRequest request, String ... nameFilter)` - Adds all request parameters to the Model, optionally filtering by name. Existing model attributes are not overwritten.
- `String addOrUpdateUrlParameter(String sourceUrl, String paramName, String paramValue)`
- `String getFullUrl(String relativeUrl, Locale locale, String...params)` - Returns a full url, including the server address and any optional request parameters. 			Existing parameters are not replaced. Null values become empty strings. Null names are skipped with their values.
- `boolean isErrorPage(HttpServletRequest request)` - Returns true if we are in a forward that should display an error handled by YadaController.yadaError() or YadaGlobalExceptionHandler
- `boolean downloadTempFile(String tempFilename, String contentType, String clientFilename, HttpServletResponse response)` - Copies the content of a file to the Response then deletes the file. To be used when the client needs to download a previously-created temporary file that you don't want to keep on server. Remember to set the "produces" attribute on the @RequestMapping with the appropriate content-type. This version is different from {@link #downloadFile(Path, boolean, String, String, HttpServletResponse)} because it can be used when you don't have the source temp File but only its name, for example because the temp file has been created in a previous request that redirected to the download url sending just the name of the temp file.
- `boolean downloadFile(Path fileToDownload, boolean thenDeleteFile, String contentType, String clientFilename, HttpServletResponse response)` - Copies the content of a file to the Response then optionally deletes the file. To be used when the client needs to download a previously-created file that you don't want to keep on server. No need to set the "produces" attribute on the @RequestMapping with the appropriate content-type.
- `String redirectString(String url, Locale locale, String...params)` - Create a redirect string to be returned by a @Controller, taking into account the locale in the path. If you can do a redirect with a relative url ("some/url") you don't need to use this method because the language path won't be overwritten. Otherwise if you need to use an absolute url ("/some/url") then this method inserts the appropriate language path in the url (and any parameters at the end too). 			Existing parameters are not replaced. Null values become empty strings. Null names are skipped with their values.
- `String enhanceUrl(String url, Locale locale, String...params)` - Creates a new URL string from a starting one, taking into account the locale in the path and optional url parameters. 			Existing parameters are not replaced. Null values become empty strings. Null names are skipped with their values.
- `void downloadZip(String returnedFilename, File[] sourceFiles, String[] filenamesNoExtension, boolean ignoreErrors, HttpServletResponse response) throws Exception` - Make a zip file and send it to the client. The temp file is automatically deleted.
- `<T extends YadaLocalEnum<?>> List<T> sortLocalEnum(Class<T> localEnum, Locale locale)` - Sorts a localized enum according to the locale specified
- `int compare(T o1, T o2)`
- `String getBrowserLanguage()` - Returns the first language in the request language header as a string.
- `String getBrowserCountry()` - Returns the first country in the request language header as a string.
- `File saveAttachment(MultipartFile attachment) throws IOException` - Save an uploaded file to a temporary file
- `void saveAttachment(MultipartFile attachment, Path targetPath) throws IOException` - Save an uploaded file to the given target file
- `void saveAttachment(MultipartFile attachment, File targetFile) throws IOException` - Save an uploaded file to the given target file
- `String sanitizeUrl(String url)` - Fix a url so that it valid and doesn't allow XSS attacks
- `String makeUrl(String...segments)` - Assembles a url given its parts as string. URLEncoding is not performed.
- `String makeUrl(String[] segments, Map<String, String> requestParams, Boolean urlEncode)` - Assembles a url given its parts as string. Use null for no parameters, use a null value for no value (just the name will be added)
- `String makeSlug(String source)` - From a given string, creates a "slug" that can be inserted in a url and still be readable.
- `static String makeSlugStatic(String source)` - From a given string, creates a "slug" that can be inserted in a url and still be readable. It is static so that it can be used in Entity objects (no context)
- `boolean checkInvalidSlugCharacters(String text)` - Find invalid characters from a slug: white space, :,;=&!+~\()@*$' The char "-" is excluded.
- `String urlDecode(String source)` - Decodes a string with URLDecoder, handling the useless try-catch that is needed
- `String urlEncode(String source)` - Encodes a string with URLEncoder, handling the useless try-catch that is needed
- `HttpServletRequest getCurrentRequest()` - Returns the current request or null
- `boolean isAjaxRequest()` - Returns true if the current request is an ajax request
- `boolean isAjaxRequest(HttpServletRequest request)` - Returns true if the request is an ajax request
- `String versionifyResourceUrl(String urlNotVersioned)` - Trasforma /res/img/favicon.ico in /res-0002/img/favicon.ico
- `static String removeHtmlStatic(String source)` - Removes all HTML tags. Static to be used from Entity beans without forcing autowiring on each instance.
- `String cleanContent(String content, String ... extraTags)` - Cleans the html content leaving only the following tags: b, em, i, strong, u, br, cite, em, i, p, strong, img, li, ul, ol, sup, sub, s
- `String getWebappAddress(HttpServletRequest request)` - Ritorna l'indirizzo completo della webapp, tipo http://www.yadaframework.net:8080/site, senza slash finale Da thymeleaf si usa con ${@yadaWebUtil.getWebappAddress(#httpServletRequest)}
- `boolean isWebImage(String extension)` - Indica se l'estensione appartiene a un'immagine accettabile
- `String getFileExtension(MultipartFile multipartFile)` - ritorna ad esempio "jpg" lowercase
- `String modalError(String title, String message, Model model)` - Visualizza un errore se non viene fatto redirect
- `String modalInfo(String title, String message, Model model)`
- `String modalOk(String title, String message, Model model)`
- `void modalError(String title, String message, RedirectAttributes redirectAttributes)` - Visualizza un errore se viene fatto redirect
- `void modalInfo(String title, String message, RedirectAttributes redirectAttributes)`
- `void modalOk(String title, String message, RedirectAttributes redirectAttributes)`
- `void modalAutoclose(long milliseconds, Model model)` - Set the automatic closing time for the notification - no close button is shown
- `void modalAutoclose(long milliseconds, RedirectAttributes redirectAttributes)` - Set the automatic closing time for the notification - no close button is shown
- `void modalReloadOnClose(Model model)` - Set the page to reload when the modal is closed
- `void modalReloadOnClose(RedirectAttributes redirectAttributes)` - Set the page to reload when the modal is closed
- `boolean isNotifyModalPending(HttpServletRequest request)` - Test if a modal is going to be opened when back to the view (usually after a redirect)
- `void notifyModal(String title, String message, String severity, String redirectSemiurl, Model model)` - Da usare direttamente solo quando si vuole fare un redirect dopo aver mostrato un messaggio. Se chiamato tante volte, i messaggi si sommano e vengono mostrati tutti all'utente.
- `boolean isModalError(Model model)` - Return true if modalError has been called before
- `boolean isNotifyModalRequested(Model model)` - Return true if for this thread the notifyModal (or a variant) has been called
- `String getClientAddress(HttpServletRequest request)` - Returns the browser's remote ip address. If the connection uses a proxy that sets the "X-Forwarded-For" header, the result is taken from that header.
- `String getClientIp(HttpServletRequest request)`
- `String modalConfirm(String message, String confirmButton, String cancelButton, Model model)`
- `String modalConfirmAndReload(String message, String confirmButton, String cancelButton, Model model)` - Show the confirm modal and reloads when the confirm button is pressed, adding the confirmation parameter to the url. The modal will be opened on load. Usually used by non-ajax methods.
- `String modalConfirm(String message, String confirmButton, String cancelButton, Model model, Boolean reloadOnConfirm, Boolean openModal)` - Show the confirm modal and optionally reloads when the confirm button is pressed
- `void callScriptOnModal(String scriptId, Model model)` - Add a script id to call when opening the notification modal
- `String getModalConfirmViewName()` - Return the view name for the confirmation modal. Used in html to get the correct modal.
- `String ensureThymeleafUrl(String url)` - Encloses the string in a thymeleaf url operator when missing


== net.yadaframework.core
=== YadaAiConfigImportSelector (YadaWeb/src/main/java/net/yadaframework/core/YadaAiConfigImportSelector.java)
- `String[] selectImports(AnnotationMetadata importingClassMetadata)`

=== YadaAjaxInterceptor (YadaWeb/src/main/java/net/yadaframework/core/YadaAjaxInterceptor.java)
- `void postHandle(HttpServletRequest request, HttpServletResponse response, Object handler, @Nullable ModelAndView modelAndView) throws Exception`

=== YadaAppConfig (YadaWeb/src/main/java/net/yadaframework/core/YadaAppConfig.java)
- `void init()`
- `static synchronized YadaConfiguration getStaticConfig()` - Initialize and return a static instance of YadaConfiguration for internal use.
- `ClassLoaderTemplateResolver emailTemplateResolver()`
- `SpringTemplateEngine emailTemplateEngine()`
- `TaskScheduler taskScheduler()`
- `Executor taskExecutor()` - Creates a ThreadPoolTaskExecutor with default values. If you need to configure it, override this method in your appConfig bean.
- `MessageSource messageSource()`
- `JavaMailSender javaMailSender()`
- `// public void onEvent(Event event)`

=== YadaConfiguration (YadaWeb/src/main/java/net/yadaframework/core/YadaConfiguration.java)
- `void copyTo(YadaConfiguration yadaConfiguration)` - Copy the already initialised configuration to a different instance
- `int getAsyncTimeoutMinutes()` - Returns the configured timeout for asynchronous requests in seconds. Equivalent to the Tomcat parameter asyncTimeout, but more effective (the application-defined parameter takes precedence).
- `boolean isDatabaseEnabled()`
- `boolean isUseEmbeddedDatabase()`
- `int getEmbeddedDatabasePort()`
- `String getEmbeddedDatabaseBaseDir()`
- `String getEmbeddedDatabaseDataDir()`
- `String getEmbeddedDatabaseTmpDir()`
- `File getEmbeddedDatabaseSourceSql()` - Returns a pointer to the sql file configured for loading the embedded database at startup
- `String getForB3B4B5(String classForB3, String classForB4, String classForB5)` - Given three strings (e.g. classes) returns the one that corresponds to the configured Bootstrap version, from 3 to 5
- `boolean isB5()`
- `boolean isB4()`
- `boolean isB3()`
- `int getBootstrapVersion()` - The configured bootstrap version may be used to return the correct html for modals etc.
- `Formatter<Date> getDateFormatter()` - Returns the configured Date Formatter. Use like &lt;dateFormatter>net.yadaframework.components.YadaDateFormatter&lt;/dateFormatter> in config. Defaults to DefaultFormattingConversionService when not configured.
- `int getTomcatPortOffset()` - Quick way to specify a new value for all embedded tomcat ports: the offset will be added to the default value. Some values may generate errors for overlapping ports e.g. an offset of 71 would set the AJP port to 8080 that may be already in use by the HTTP port of another instance with the offset at 0.
- `int getTomcatHttpPort()`
- `int getTomcatHttpsPort()`
- `int getTomcatAjpPort()`
- `int getTomcatAjpRedirectPort()`
- `int getTomcatShutdownPort()`
- `File getTomcatKeystoreFile()`
- `String getTomcatKeystorePassword()`
- `int getTomcatMaxPostSize()` - The maximum size in bytes of the POST which will be handled by the container FORM URL parameter parsing. The limit can be disabled by setting this attribute to a value less than zero. If not specified, this attribute is set to 2097152 (2 MiB).
- `String getGoogleApiKey()` - Google api key (for Maps etc) read from "security.properties"
- `boolean isLocalFlag(String name)` - Gets the value of any boolean config key defined in the /config/local configuration file (it should reside on the developers computer in a personal folder, not shared). When not defined or not a boolean, the value is false. Only available in dev env.
- `Object getLocalConfig(String name)` - Gets the value of any config key defined in the /config/local configuration file (it should reside on the developers computer in a personal folder, not shared). When not defined, the value is an empty string. Only available in dev env.
- `String getNotifyModalView()` - Returns the configured path for the notification modal. The configuration path is config/paths/notificationModalView
- `String getTargetImageExtension()` - Returns the image extension (without dot) to use when uploading user images. Defaults to "jpg".
- `boolean isPreserveImageExtension(String extensionNoDot)` - Check if the image extension has to be preserved when converting. The value is taken from &lt;dimension targetImageExtension="jpg" preserveImageExtension="gif,webp">
- `boolean isFileManagerDeletingUploads()` - Tells if the YadaFileManager has to delete uploaded files when attaching them, or to keep them in the uploads folder for later use. true by default.
- `SortedSet<Entry<Integer,String>> getLocalSet(String configPath, String messageBaseKey, Locale locale, MessageSource messageSource)` - Looks in the configuration for a list of ids, then fetches from message.properties the localized text corresponding to the ids. The id is appended to the messageBaseKey parameter after a dot. The result is a sorted set of id-text, sorted on the text. <br> Example:<br> In the config we have <pre> &lt;countries> 	&lt;countryId>1&lt;/countryId> 	&lt;countryId>2&lt;/countryId> 	&lt;countryId>3&lt;/countryId> 	&lt;countryId>4&lt;/countryId> &lt;/countries> </pre> In message.properties we have <pre> customer.country.1 = England customer.country.2 = France customer.country.3 = USA customer.country.4 = Albania </pre> The call to this method will be: <pre> config.getLocalSet("config/countries/countryId", "customer.country", locale, messageSource); </pre> The resulting set will have the countries in the following order: Albania, England, France, USA. Results are cached forever. Example: config/countries/countryId Example: customer.country
- `int compare(Entry<Integer, String> element1, Entry<Integer, String> element2)`
- `String getUploadsDirname()`
- `// public String getUploadsUrl()` - //	/** //	 * Returns the url for the uploads folder //	 * @return //	 */
- `File getUploadsFolder()` - Folder where files are uploaded before processing. Should not be a public folder.
- `boolean isEmailThrowExceptions()` - Returns true if YadaEmaiService should throw an exception instead of returning false when it receives an exception on send
- `String getPasswordResetSent(Locale locale)`
- `String getRegistrationConfirmationLink(Locale locale)`
- `boolean emailBlacklisted(String email)` - Checks if an email address has been blacklisted in the configuration
- `boolean useDatabaseMigrationAtStartup()` - True if during startup YadaAppConfig should run the FlyWay migrate operation
- `String flywayTableName()` - Name of the flyway schema history table. Uses the default name of "flyway_schema_history" when not configured.
- `boolean useDatabaseMigrationOutOfOrder()` - "Out of order" flag in FlyWay https://flywaydb.org/documentation/configuration/parameters/outOfOrder
- `Locale getDefaultLocale()` - Returns the configured default locale.
- `boolean isLocalePathVariableEnabled()` - True if the filter enables the use of locales in the url, like /en/mypage
- `// public String getLocaleForRequest(String locale)` - //	/** //	 * Returns the locale to be injected in the request. //	 * Used when the locale string only has the language and you want to store the full language_COUNTRY locale in your request. //	 * The configuration must be like &lt;locale>es&lt;request>es_ES&lt;/request>&lt;/locale> //	 * @param locale //	 * @return either the input parameter unaltered or the "request" locale if configured //	 */
- `String getCountryForLanguage(String language)`
- `boolean isLocaleAddCountry()` - True if locale paths only have the language component ("en") but you also need the country component ("US") in the request Locale
- `Set<Locale> getLocaleSet()` - Get the set of configured locales in no particular order.
- `List<String> getLocaleStrings()` - Get a list of iso2 locales that the webapp can handle
- `List<Locale> getLocales()` - Returns the configured locales as objects, using countries if configured
- `String getErrorPageForward()` - Returns the page to forward to after an unhandled exception or HTTP error. The value is the @RequestMapping value, without language in the path
- `boolean isBeta()`
- `boolean isAlpha()`
- `int getMaxPasswordLength()`
- `int getMinPasswordLength()`
- `File getTempImageDir()` - Ritorna il path del folder in cui sono memorizzate le immagini temporanee accessibili via web, ad esempio per il preview della newsletter
- `String getTempImageRelativePath()` - Ritorna il path del folder in cui sono memorizzate le immagini temporanee accessibili via web, relativamente al folder "contents"
- `String getWebappAddress(HttpServletRequest request)` - Return the webapp address without a trailing slash. E.g. http://www.mysite.com/app or http://www.mysite.com The address is computed from the request when not null, else it is read from the configuration.
- `String getWebappAddress()` - Return the webapp address without a trailing slash. E.g. http://www.mysite.com/app or http://www.mysite.com
- `String getServerAddress(HttpServletRequest request)` - Return the server address without a trailing slash. E.g. http://col.letturedametropolitana.it Warning: this version does not work properly behind an ajp connector
- `String getServerAddress()` - Return the server address without a trailing slash. E.g. http://col.letturedametropolitana.it
- `String getEmailLogoImage()`
- `String getMin()` - Returns ".min" if this is not a development environment. Use like <script yada:src="@{|/res/dataTables/jquery.dataTables${@config.min}.js|}"
- `int getAutologinExpirationHours()`
- `int getTagFilterMax()` - Ritorna il numero massimo di suggerimenti di tag per una storia
- `int getTagMaxSuggested()` - Ritorna il numero massimo di suggerimenti di tag per una storia
- `int getTagMaxNum()` - Ritorna il numero massimo di tag normali che è possibile assegnare a una storia
- `String getTagReservedPrefix()` - Ritorna il prefisso di un tag redazionale (special)
- `String[] getSupportRequestRecipients()`
- `int getFacebookType()`
- `int getGoogleType()`
- `String getFacebookBaseStoryUrl()` - Ritorna la url da usare per postare una story su facebook (a cui appendere l'id)
- `String getFacebookTestPageAccessToken()`
- `String getFacebookPageAccessToken()`
- `String getFacebookTestSecret()`
- `String getFacebookSecret()`
- `String getFacebookTestPageId()`
- `String getFacebookPageId()`
- `String getFacebookTestAppId()`
- `String getFacebookAppId()`
- `String getGoogleSecret()`
- `String getGoogleClientId()`
- `String getRoleSpringName(Integer roleId)` - Given a role id, returns the configured role name prefixed by "ROLE_", e.g. "ROLE_USER"
- `String getRoleName(Integer roleId)` - Given a role id, returns the configured role name e.g. "USER". Equivalent to {@link #getRoleKey(Integer)}.
- `List<Integer> getRoleIds()`
- `Integer[] getRoleIds(String[] roleNames)` - Convert from role names to role ids
- `Integer getRoleId(String roleKey)`
- `String getRoleKey(Integer roleId)`
- `// public Map<String, Set<String>> getRoleKeyToRoleChange()` - //	/** //	 * @return a map acting role --> roles that can be changed, as role keys. Configured by &lt;handles> //	 */
- `Map<Integer, Set<Integer>> getRoleIdToRoleChange()`
- `List<YadaClause> getSetupClauses()` - Ritorna la lista di YadaClause trovate nella configurazione
- `boolean isContentUrlLocal()` - Check if content urls must be local to the server or remote with a http address
- `String getContentUrl()` - Url where to download user-uploaded content, without final slash. Can be webapp-relative or absolute, for example "/contents" or "http://cdn.my.com/contents" or "//cdn.my.com/contents"
- `File getContentsFolder()` - Base folder for uploaded content
- `String getContentPath()` - Base folder for uploaded content
- `String getContentName()` - Name of the folder inside basePath where contents are stored
- `String getBasePathString()` - Absolute path on the filesystem where application files not belonging to the webapp war are stored. Example: /srv/myproject
- `Path getBasePath()` - Absolute path on the filesystem where application files not belonging to the webapp war are stored. Example: /srv/myproject
- `String getResourceDir()`
- `String getYadaResourceDir()`
- `String getVersionedResourceDir()`
- `String getVersionedYadaResourceDir()`
- `List<Map<String, Object>> getSetupUsers()` - Every tag in the config/setup/users/user configuration is added to the map as a key-value pair except roles, added as a list to the "roles" key.
- `String getMaxFileUploadSizeMega()`
- `long getMaxFileUploadSizeBytes()`
- `String getDbJndiName()`
- `String[] getEmailFrom()` - Get the email address and the personal name of the sender.
- `boolean isEmailEnabled()`
- `String getEmailHost()`
- `int getEmailPort()`
- `String getEmailProtocol()`
- `String getEmailUsername()`
- `String getEmailPassword()`
- `Properties getEmailProperties()`
- `List<String> getValidDestinationEmails()` - Ritorna le email per le quali è abilitato l'invio
- `boolean isProductionEnvironment()`
- `boolean isDevelopmentEnvironment()`
- `String getYadaVersion()` - Ritorna la stringa che rappresenta la versione di yada
- `String getApplicationVersion()` - Ritorna la stringa che rappresenta la versione, completa di build
- `String getApplicationEnvironment()` - Get the configured environment name: config/info/env
- `String getApplicationBuild()`
- `String getApplicationDate()`
- `// public File getResourcesBasepath()` - //	/** //	 * Ritorna il valore di "config.resources.basepath" come File //	 * @param config //	 * @return //	 */
- `// public String getResourcesBaseurl()` - //	/** //	 * Ritorna il valore di "config.resources.basepath" come URL, terminata con "/" //	 * @param config //	 * @return //	 */
- `static Properties containsInProperties(Properties options, String toSearch)` - Cerca all'interno delle options il testo toSearch
- `YadaConfiguration()`
- `// public YadaConfiguration(NodeCombiner comb)`
- `// public YadaConfiguration(Lock lock)`
- `// public YadaConfiguration(NodeCombiner comb, Lock lock)`
- `boolean getShowSql()`
- `boolean encodePassword()`
- `int getMaxPasswordFailedAttempts()`
- `int getPasswordFailedAttemptsLockoutMinutes()`
- `int seleniumWaitQuick()`
- `int seleniumWait()`
- `int seleniumWaitSlow()`
- `String getSeleniumHubAddress()` - The url of the Selenium HUB or the ChromeDriver server. Example: http://localhost:4444/wd/hub, http://127.0.0.1:9515
- `long getSeleniumTimeoutSlowPageLoadSeconds()`
- `long getSeleniumTimeoutProxyTestPageLoadSeconds()`
- `long getSeleniumTimeoutPageLoadSeconds()`
- `long getSeleniumTimeoutScriptSeconds()`
- `long getSeleniumTimeoutImplicitlyWaitSeconds()`
- `ImmutableHierarchicalConfiguration getConfiguration()`
- `void setConfiguration(ImmutableHierarchicalConfiguration configuration)`
- `String getString(String key, String defaultValue)`
- `String getString(String key)`
- `int getInt(String key, int defaultValue)`
- `long getLong(String key, long defaultValue)`
- `void setBuilder(CombinedConfigurationBuilder builder) throws ConfigurationException`
- `void reloadIfNeeded() throws ConfigurationException` - Call this method to trigger a configuration reload, but only if the file has changed and the timeout since last reload has passed
- `long getYadaJobSchedulerPeriod()` - The YadaJobScheduler period in milliseconds. <1 means do not schedule
- `int getYadaJobSchedulerThreadPoolSize()` - The YadaJobScheduler thread pool size. When the number of concurrent jobs is higher, they are queued.
- `long getYadaJobSchedulerStaleMillis()` - Milliseconds after a running job is considered to be stale and killed.
- `int getYadaJobSchedulerCacheSize()` - Number of YadaJob entities to keep in cache. It should be equal to the estimated maximum number of concurrent running jobs. If the number is too small, concurrent writes to the same job data could result in "concurrent modification" exceptions. Also the running job could be evicted from cache and terminated prematurely with a log saying "Evicting job {} while still running". This is a possible scenario: <pre> - thread A loads instance J1 for a job, puts it in the cache and starts a long running job - after some time, while the job is still running, the J1 instance is evicted because of cache size limits - thread B asks for a job instance of the same job, which is not found and loaded new from db as J2. It then changes some values and saves J2 to database, incrementing @version - thread A terminates the long running job by writing J1 to the database In this scenario, thread A will receive an exception because its @version differs from what is in the database </pre>
- `String[] getMessageSourceBasenames()` - There can be more than one "messages.properties" file for i18n, for example to split by concern e.g. "errors", "countries", etc.

=== YadaDummyDatasource (YadaWeb/src/main/java/net/yadaframework/core/YadaDummyDatasource.java)
- `java.util.logging.Logger getParentLogger() throws SQLFeatureNotSupportedException`
- `<T> T unwrap(Class<T> iface) throws SQLException`
- `boolean isWrapperFor(Class<?> iface) throws SQLException`
- `Connection getConnection() throws SQLException`
- `Connection getConnection(String username, String password) throws SQLException`
- `PrintWriter getLogWriter() throws SQLException`
- `void setLogWriter(PrintWriter out) throws SQLException`
- `void setLoginTimeout(int seconds) throws SQLException`
- `int getLoginTimeout() throws SQLException`

=== YadaDummyEntityManagerFactory (YadaWeb/src/main/java/net/yadaframework/core/YadaDummyEntityManagerFactory.java)
- `EntityManager createEntityManager()`
- `EntityManager createEntityManager(Map<?, ?> map)`
- `EntityManager createEntityManager(SynchronizationType synchronizationType)`
- `EntityManager createEntityManager(SynchronizationType synchronizationType, Map<?, ?> map)`
- `CriteriaBuilder getCriteriaBuilder()`
- `Metamodel getMetamodel()`
- `boolean isOpen()`
- `void close()`
- `String getName()`
- `Map<String, Object> getProperties()`
- `Cache getCache()`
- `PersistenceUnitUtil getPersistenceUnitUtil()`
- `PersistenceUnitTransactionType getTransactionType()`
- `SchemaManager getSchemaManager()`
- `void addNamedQuery(String name, Query query)`
- `<T> T unwrap(Class<T> cls)`
- `<T> void addNamedEntityGraph(String graphName, EntityGraph<T> entityGraph)`
- `<R> Map<String, TypedQueryReference<R>> getNamedQueries(Class<R> resultType)`
- `<E> Map<String, EntityGraph<? extends E>> getNamedEntityGraphs(Class<E> entityType)`
- `void runInTransaction(Consumer<EntityManager> work)`
- `<R> R callInTransaction(Function<EntityManager, R> work)`

=== YadaDummyJpaConfig (YadaWeb/src/main/java/net/yadaframework/core/YadaDummyJpaConfig.java)
- `NamedParameterJdbcTemplate namedParameterJdbcTemplate(DataSource dataSource)`
- `JdbcTemplate jdbcTemplate(DataSource dataSource)`
- `DataSource dataSource()`
- `EntityManagerFactory EntityManagerFactory()`

=== YadaFluentBase (YadaWeb/src/main/java/net/yadaframework/core/YadaFluentBase.java)
- `YadaFluentBase(T parent)`
- `T back()`

=== YadaJpaConfig (YadaWeb/src/main/java/net/yadaframework/core/YadaJpaConfig.java)
- `NamedParameterJdbcTemplate namedParameterJdbcTemplate(DataSource dataSource)`
- `JdbcTemplate jdbcTemplate(DataSource dataSource)`
- `DataSource dataSource() throws SQLException` - Returns the datasource. It can either be configured in the application configuration (uses vibur-dbcp pool) or on JNDI via META-INF/context.xml (uses Tomcat-jndi pool)
- `EntityManagerFactory entityManagerFactory() throws SQLException`
- `EntityManager entityManager(EntityManagerFactory entityManagerFactory)`
- `PlatformTransactionManager transactionManager() throws SQLException`
- `HibernateExceptionTranslator hibernateExceptionTranslator()`
- `synchronized DataSource getProgrammaticDatasource()` - Returns a DataSource that has NOT been configured on JNDI. Given that there is a configuration file for each environment, you could have a programmatic datasource in development and a JNDI datasource in production, if needed. This method should be overridden to set more parameters than currently implemented.

=== YadaLinkBuilder (YadaWeb/src/main/java/net/yadaframework/core/YadaLinkBuilder.java)
- `YadaLinkBuilder(YadaConfiguration config, String notLocalizedResourcePattern)`

=== YadaLocalEnum (YadaWeb/src/main/java/net/yadaframework/core/YadaLocalEnum.java)
- `default public long toId()`

=== YadaLocalePathChangeInterceptor (YadaWeb/src/main/java/net/yadaframework/core/YadaLocalePathChangeInterceptor.java)
- `static final String LOCALE_ATTRIBUTE_NAME = YadaLocalePathChangeInterceptor.class.getName() + ".LOCALE";`
- `static boolean localePathRequested(HttpServletRequest request)` - Check if the current locale has been set on the url, e.g. /en/something
- `boolean preHandle(HttpServletRequest request, HttpServletResponse response, Object handler)`

=== YadaLocalePathLinkBuilder (YadaWeb/src/main/java/net/yadaframework/core/YadaLocalePathLinkBuilder.java)
- `YadaLocalePathLinkBuilder(String notLocalizedResourcePattern)`

=== YadaRegistrationType (YadaWeb/src/main/java/net/yadaframework/core/YadaRegistrationType.java)
- `static YadaRegistrationType fromInt(int num)`
- `int toInt()`

=== YadaTomcatServer (YadaWeb/src/main/java/net/yadaframework/core/YadaTomcatServer.java)
- `static void main(String[] args) throws Exception` - Starts the standalone server on port 8080 by default. Different ports can be specified in the application configuration.        - acronym+environment used for the shutdown command        - relative path of the webapp folder in eclipse ("src/main/webapp"), or the full path elsewhere        - the third argument is optional in Eclipse, otherwise it must be the full path of the temp folder for Tomcat data (where the war is exploded)        When the third argument is not provided, "dev mode" is assumed.        When the third argument is specified, an additional fourth argument of "dev" starts in developer mode
- `YadaTomcatServer()` - Create an instance of the server for custom configuration (need to subclass the configure() method and call it explicitly).
- `YadaTomcatServer(String[] args) throws ServletException, MalformedURLException, IOException, ConfigurationException` - Create an instance of the server using the configuration specified in the args parameter
- `void start() throws LifecycleException`

=== YadaWebApplicationInitializer (YadaWeb/src/main/java/net/yadaframework/core/YadaWebApplicationInitializer.java)
- `boolean isSecurityProjectPresent()`

=== YadaWebConfig (YadaWeb/src/main/java/net/yadaframework/core/YadaWebConfig.java)
- `void init()`
- `void configureAsyncSupport(AsyncSupportConfigurer configurer)`
- `String getNotLocalizedResourcePattern()` - Return a string pattern to match urls that should not be localised when using a language path variable i.e. the language code will not be added when using @{} in thymeleaf
- `static String getYadaResourceFolder()` - Name of the folder where versioned static yada files are to be found, starting with /
- `static String getResourceFolder()` - Name of the folder where versioned static files are to be found, starting with /
- `static String getStaticFileFolder()` - Name of the folder where non-versioned static files are to be found, starting with /
- `// public void addArgumentResolvers(List<HandlerMethodArgumentResolver> argumentResolvers)`
- `void addInterceptors(InterceptorRegistry registry)`
- `LocaleChangeInterceptor localeChangeInterceptor()`
- `YadaLocalePathChangeInterceptor yadaLocalePathChangeInterceptor()`
- `LocaleResolver localeResolver()`
- `FormattingConversionService mvcConversionService()`
- `void addResourceHandlers(ResourceHandlerRegistry registry)` - Tutti i file dentro a /res vengono indicati come cacheabili lato browser per 1 anno (tramite l'header expires). Per evitare che nuove versioni non vengano mai prese, si usa il "trucco" di indicare il numero di build nell'url, così cambiando la build cambia l'url e la cache del browser va in miss la prima volta. I file dentro a /static, invece, non cambiano mai nemmeno alle nuove release (anche se in cache stanno solo 100 giorni). Però non è per questo che si usa static, ma per il fatto che dentro ai commenti condizionali non si possono usare i tag thymeleaf, per cui ad esempio html5shiv.js viene messo in /static
- `ClassLoaderTemplateResolver yadaTemplateResolver()`
- `ITemplateResolver mailPreviewTemplateResolver()`
- `ITemplateResolver webTemplateResolver()`
- `ITemplateResolver javascriptTemplateResolver()`
- `ITemplateResolver xmlTemplateResolver()`
- `SpringTemplateEngine templateEngine()`
- `SpringTemplateEngine javascriptTemplateEngine()`
- `SpringTemplateEngine xmlTemplateEngine()`
- `ViewResolver javascriptViewResolver(@Qualifier("javascriptTemplateEngine") SpringTemplateEngine javascriptTemplateEngine)` - View resolver for js files that can be anywhere in the views folder. Contrary to usual practice, the view name MUST include the .js extension: return "/some/path/myfile.js".
- `ViewResolver xmlViewResolver(@Qualifier("xmlTemplateEngine") SpringTemplateEngine xmlTemplateEngine)` - View resolver for xml files that are inside the /xml folder.
- `ViewResolver viewResolver(@Qualifier("templateEngine") SpringTemplateEngine templateEngine)` - View resolver for html pages. It handles web pages, mail preview pages from classpath, yada snippets from classpath


== net.yadaframework.exceptions
=== InternalException (YadaWeb/src/main/java/net/yadaframework/exceptions/InternalException.java)
- `InternalException()`
- `InternalException(String message)`
- `InternalException(String format, Object... params)`
- `InternalException(Throwable cause)`
- `InternalException(String message, Throwable cause)`

=== InvalidValueException (YadaWeb/src/main/java/net/yadaframework/exceptions/InvalidValueException.java)
- `InvalidValueException()`
- `InvalidValueException(String message)`
- `InvalidValueException(String format, Object... params)`
- `InvalidValueException(Throwable cause)`
- `InvalidValueException(String message, Throwable cause)`

=== SystemException (YadaWeb/src/main/java/net/yadaframework/exceptions/SystemException.java)
- `SystemException()`
- `SystemException(String message)`
- `SystemException(String format, Object... params)`
- `SystemException(Throwable cause)`
- `SystemException(String message, Throwable cause)`

=== YadaAlreadyRunningException (YadaWeb/src/main/java/net/yadaframework/exceptions/YadaAlreadyRunningException.java)
- `YadaAlreadyRunningException()`
- `YadaAlreadyRunningException(String runningUsername)` - Set the name of the user who is running the execution
- `String getRunningUsername()` - Get the name of the user who is running the execution

=== YadaConfigurationException (YadaWeb/src/main/java/net/yadaframework/exceptions/YadaConfigurationException.java)
- `YadaConfigurationException()`
- `YadaConfigurationException(String message)`
- `YadaConfigurationException(String format, Object... params)` - Build the message using slf4j log format syntax
- `YadaConfigurationException(Throwable cause, String format, Object... params)`
- `YadaConfigurationException(Throwable cause)`
- `YadaConfigurationException(String message, Throwable cause)`

=== YadaEmailException (YadaWeb/src/main/java/net/yadaframework/exceptions/YadaEmailException.java)
- `YadaEmailException()`
- `YadaEmailException(Exception e)`

=== YadaInternalException (YadaWeb/src/main/java/net/yadaframework/exceptions/YadaInternalException.java)
- `YadaInternalException()`
- `YadaInternalException(String message)`
- `YadaInternalException(String format, Object... params)` - Build the message using slf4j log format syntax
- `YadaInternalException(Throwable cause, String format, Object... params)` - Build the message using slf4j log format syntax
- `YadaInternalException(Throwable cause)`
- `YadaInternalException(String message, Throwable cause)`

=== YadaInvalidUsageException (YadaWeb/src/main/java/net/yadaframework/exceptions/YadaInvalidUsageException.java)
- `YadaInvalidUsageException()`
- `YadaInvalidUsageException(String message)`
- `YadaInvalidUsageException(String format, Object... params)` - Build the message using slf4j log format syntax
- `YadaInvalidUsageException(Throwable cause, String format, Object... params)`
- `YadaInvalidUsageException(Throwable cause)`
- `YadaInvalidUsageException(String message, Throwable cause)`

=== YadaInvalidValueException (YadaWeb/src/main/java/net/yadaframework/exceptions/YadaInvalidValueException.java)
- `YadaInvalidValueException()`
- `YadaInvalidValueException(String message)`
- `YadaInvalidValueException(String format, Object... params)` - Build the message using slf4j log format syntax
- `YadaInvalidValueException(Throwable cause, String format, Object... params)`
- `YadaInvalidValueException(Throwable cause)`
- `YadaInvalidValueException(String message, Throwable cause)`

=== YadaJobFailedException (YadaWeb/src/main/java/net/yadaframework/exceptions/YadaJobFailedException.java)
- `YadaJobFailedException()`
- `YadaJobFailedException(String message)`
- `YadaJobFailedException(String format, Object... params)` - Build the message using slf4j log format syntax
- `YadaJobFailedException(Throwable cause, String format, Object... params)`
- `YadaJobFailedException(Throwable cause)`
- `YadaJobFailedException(String message, Throwable cause)`

=== YadaSocialException (YadaWeb/src/main/java/net/yadaframework/exceptions/YadaSocialException.java)
- `YadaSocialException()`
- `YadaSocialException(String message)`
- `YadaSocialException(String format, Object... params)` - Build the message using slf4j log format syntax
- `YadaSocialException(Throwable cause, String format, Object... params)`
- `YadaSocialException(Throwable cause)`
- `YadaSocialException(String message, Throwable cause)`

=== YadaSystemException (YadaWeb/src/main/java/net/yadaframework/exceptions/YadaSystemException.java)
- `YadaSystemException()`
- `YadaSystemException(String message)`
- `YadaSystemException(String format, Object... params)` - Build the message using slf4j log format syntax
- `YadaSystemException(Throwable cause, String format, Object... params)`
- `YadaSystemException(Throwable cause)`
- `YadaSystemException(String message, Throwable cause)`


== net.yadaframework.persistence
=== YadaDao (YadaWeb/src/main/java/net/yadaframework/persistence/YadaDao.java)
- `Object save(Object entity)` - Save any Entity, also new or detached ones.
- `Object getEntityAttributeValue(Object entity, String attributeName)` - Get the value of some field on any entity, within a transaction. Warning: it does a merge, which may trigger an insert to the database.
- `<T> T find (Class<T> someClass, Long id)` - Find any object given its class and id
- `Object find(String className, Long id)`

=== YadaDataTableDao (YadaWeb/src/main/java/net/yadaframework/persistence/YadaDataTableDao.java)
- `<entityClass> Map<String, Object> getJsonPage(YadaDatatablesRequest yadaDatatablesRequest, Class<?> entityClass, Locale locale)` - Returns a map with the result in the format needed by DataTables. All values that are included in the annotated json view (on the caller) will end up in the resulting json
- `<entityClass> Map<String, Object> getConvertedJsonPage(YadaDatatablesRequest yadaDatatablesRequest, Class<?> entityClass, Locale locale)` - Returns a map with the result in the format needed by DataTables. Only requested values are returned in the map. There is no need to call a json converter because everything is a map already.

=== YadaMoney (YadaWeb/src/main/java/net/yadaframework/persistence/YadaMoney.java)
- `static final YadaMoney ZERO = new YadaMoney(0);`
- `YadaMoney(int amount)`
- `YadaMoney(String amount, Locale locale) throws ParseException` - Convert a string with a decimal value that uses the decimal separator of the specified locale
- `YadaMoney(long amount)`
- `YadaMoney(double doubleValue)`
- `static YadaMoney fromDatabaseColumn(Long dbValue)` - Create a YadaMoney from the value stored in the database
- `boolean isAtLeast(String amount, Locale locale) throws ParseException` - Returns true if the current value is equal to the amount specified or higher
- `boolean isAtLeast(int amount)` - Returns true if the current value is equal to the amount specified or higher
- `YadaMoney getAbsolute()` - Returns a new instance with the positive value of the current value
- `YadaMoney getPositive()` - Returns a new instance with the positive value of the current value. Same as getAbsolute()
- `YadaMoney getNegative()` - Returns a new instance with the negative value of the current value. If the current value was already negative, it stays negative.
- `YadaMoney getNegated()` - Returns a new instance with the negated (inverted) value of the current value. It will be positive if the current value is negative, it will be negative if the current value is positive.
- `boolean isZero()` - Returns true if the value is zero
- `boolean isNegative()` - Returns true if the value is lower than zero
- `boolean isPositive()` - Returns true if the value is greater than zero
- `YadaMoney getSum(long cents)`
- `YadaMoney getSum(YadaMoney toAdd)` - Return a new YadaMoney where the value is the sum of the current value and the argument. The original object is not changed.
- `YadaMoney getSubtract(YadaMoney toRemove)` - Return a new YadaMoney where the value is the subtraction of the current value and the argument. The original object is not changed.
- `YadaMoney getDivide(double factor)` - Return a new YadaMoney where the value is the division of the current value by the argument. The original object is not changed.
- `YadaMoney getMultiply(double factor)` - Return a new YadaMoney where the value is the multiplication of the current value by the argument. The original object is not changed.
- `double getRoundValue(int decimals)` - Returns the value with N decimal places
- `double getRoundValue()` - Returns the value with 2 decimal places
- `String toString()` - Convert to a string with 2 decimal places
- `String toIntString()` - Convert to a string with no decimal places (truncated)
- `String toString(Locale locale)` - Convert to a string with 2 decimal places
- `int hashCode()`
- `boolean equals(Object obj)`
- `int compareTo(YadaMoney other)`

=== YadaMoneyConverter (YadaWeb/src/main/java/net/yadaframework/persistence/YadaMoneyConverter.java)
- `Long convertToDatabaseColumn(YadaMoney attribute)`
- `YadaMoney convertToEntityAttribute(Long dbData)`

=== YadaSql (YadaWeb/src/main/java/net/yadaframework/persistence/YadaSql.java)
- `static String getOrderByNative(YadaPageRequest yadaPageRequest)` - Returns the "order by" statement in MySql syntax
- `YadaSql union(YadaSql unioned)` - Add a query as a union of the current one.
- `YadaSql deleteFrom(boolean enabled, String deleteFrom)` - Start a "delete from ..." query
- `YadaSql deleteFrom(String deleteFrom)` - Start a "delete from ..." query
- `YadaSql selectFrom(boolean enabled, String selectFrom)` - Start a "select ... from ..." query If this method has already been called, the new select text is inserted before the "from" after a comma
- `// public YadaSql insertInto(boolean enabled, String tableName)` - //	/** //	 * Start a "insert ... values ..." query //	 * @param enabled //	 * @param tableName the table where to insert //	 * @return //	 */
- `// public YadaSql insertInto(boolean enabled, String columnName, String value)`
- `// public YadaSql insertInto(boolean enabled, String columnName, Long value)`
- `// public YadaSql insertInto(boolean enabled, String columnName, Integer value)`
- `// public YadaSql insertInto(boolean enabled, String columnName, Object value)`
- `YadaSql selectFromReplace(String selectFrom)` - Replaces the current "select... from..." with another one
- `YadaSql selectFrom(String selectFrom)` - Start or extend a "select ... from ..." query. If this method has already been called, the new select text is inserted before the "from" after a comma: "select x, y from" when the parameter is "y" This method also accepts a full query (with join, where etc)
- `YadaSql set(String nameValue)` - Continues an update - set query with name=value, e.g. "name=:usename"
- `YadaSql set(boolean enabled, String nameValue)` - Continues an update - set query with name=value, e.g. "name=:usename"
- `YadaSql updateSet(String updateSet)` - Start a "update ... set ..." query
- `static YadaSql instance()` - Returns an empty YadaSql for later use
- `YadaSql join(boolean enabled, String joinOn)` - Adds a join condition if not already added (the join keyword is required).
- `YadaSql join(String joinOn)` - Adds a join condition if not already added (the join keyword is required).
- `YadaSql type(String attributeAlias, Class<?> targetType)` - Adds a TYPE condition - JPQL QUERIES ONLY
- `YadaSql dtype(Class<?> targetType)` - Adds a DTYPE condition - NATIVE QUERIES ONLY
- `YadaSql where()`
- `YadaSql where(String whereConditions)` - Adds a where condition
- `YadaSql where(boolean enabled, String whereConditions)` - Adds a where condition
- `YadaSql whereNotEmpty(Collection collection, String whereConditions)` - Adds a where condition only if the collection is not null and non empty
- `YadaSql whereIn(String attributeName, Collection values)` - Add a "where aaa in (x, y, z)" clause. Skipped if the collection is null or empty. The collection is converted to a comma-separated strings. <b>NOTE:</b> It is always better to use collections as a parameter, like "aaa in :someCollection"
- `YadaSql whereIn(String attributeName, YadaSql subselect)` - Add a "where aaa in (select ...)" clause.
- `YadaSql having(String havingConditions)` - Adds a having condition
- `YadaSql having(boolean enabled, String havingConditions)`
- `YadaSql and(boolean enabled)` - Adds an AND condition if the parameter is true, but only if the previous expression was not skipped
- `YadaSql and()` - Adds an AND condition, but only if the previous expression was not skipped
- `YadaSql or(boolean enabled)`
- `YadaSql or()`
- `YadaSql xor(boolean enabled)`
- `YadaSql xor()`
- `YadaSql startSubexpression()` - Starts a subexpression. Be careful that the returned YadaSql object is different from the original one, so don't use the original one for ending the subexpression.
- `YadaSql startSubexpression(boolean enabled)` - Starts a subexpression. Be careful that the returned YadaSql object is different from the original one, so don't use the original one for ending the subexpression.
- `YadaSql endSubexpression()` - Ends a where/having subexpression. To be called on the object returned by the {@link #startSubexpression()} method (or any method called on that object)
- `YadaSql endSubexpression(boolean enabled)` - Ending a subexpression is always safe even if the startSubexpression had an "enabled" condition
- `YadaSql endSubexpression(String alias)` - Ends a where/having subexpression. To be called on the object returned by the {@link #startSubexpression()} method (or any method called on that object)
- `YadaSql groupBy(boolean enabled, String groupBy)`
- `YadaSql groupBy(String groupBy)`
- `YadaSql orderBy(String orderBy)`
- `YadaSql orderBy(boolean enabled, String orderBy)`
- `YadaSql orderByNative(YadaPageRequest yadaPageRequest)` - Add sorting from a YadaPageRequest, using MySQL syntax
- `YadaSql orderBy(YadaPageRequest yadaPageRequest)` - Add sorting from a YadaPageRequest, using JPA syntax
- `String property = order.getProperty(); // "publicName%2Cdesc" results from "sort=publicName%252Cdesc"`
- `YadaSql limit(Integer limit)` - Set a limit
- `YadaSql clearWhere()`
- `YadaSql toCount()` - Transforms a "select ... from" to a "select count(*) from"
- `YadaSql toSelectFrom(boolean enabled, String newSelectFrom)` - Replaces the current "select ... from"
- `YadaSql toSelectFrom(String newSelectFrom)` - Replaces the current "select ... from"
- `YadaSql toCount(String sql)` - Transforms a "select ... from" to a "select count(*) from"
- `Query nativeQuery(EntityManager em)`
- `Query nativeQuery(EntityManager em, String resultSetMapping)`
- `Query nativeQuery(EntityManager em, Class targetClass)` - Create a native query that returns object instances
- `Query query(EntityManager em)` - Create an instance of Query for executing a Java Persistence query language statement.
- `<T> TypedQuery<T> query(EntityManager em, Class<T> targetClass)` - Create an instance of TypedQuery for executing a Java Persistence query language statement. The select list of the query must contain only a single item, which must be assignable to the type specified by the resultClass argument.
- `YadaSql setParameter(String name, Object value)` - To be used before calling query() or nativeQuery(). It is ok to set a parameter that is not used in the query.
- `YadaSql add(YadaSql yadaSql)` - Add all the joins, where, having, group, order statements and parameters of another YadaSql object. Joins are added only if not present already.
- `YadaSql setParameter(String name, String[] values)`
- `YadaSql setParameter(String name, Long[] values)`
- `YadaSql setParameterNotNull(String name, Object value)` - Set a parameter only when not null. Quite useless because setting a parameter that is not used has no effect.
- `String sql()` - Returns the resulting sql
- `void overwriteQuery(String query)` - Sets the whole query. Useful for debugging.
- `String sql(String oldAliasName, String newAliasName, String... oldToNew)` - Returns the resulting sql after replacing aliases. It can be used when reusing an existing query with a new alias, to get something like "select u from User u where u.type=1 and u.age = (select min(u2.age) from User u2 where u2.type=1)". In this case, create a query "where XX.type=1" and use it twice, first replacing "XX" with "u", then replacing "XX" with "u2".
- `Field[] getExcludedFields()`
- `String toString()`
- `// public void addAlias(String aliasPlaceholder, String aliasName)` - //	/** //	 * Sets an alias used in the query. Needed to reuse the same //	 * @param aliasPlaceholder the alias placeholder, like "ENTITY" //	 * @param aliasName the alias value, like "u" //	 */
- `String getWhere()` - Returns the "where" section of the query, not starting with "where"

=== YadaSqlBuilder (YadaWeb/src/main/java/net/yadaframework/persistence/YadaSqlBuilder.java)
- `void setParameter(String paramName, Object value, Query query)` - Set a query parameter if the parameter itself has actually been used in the query.
- `void setParameters(Query query)` - Set all parameters previously added, but ony if predent in the query
- `void addParameter(String name, Object value)` - Add a parameter to be later set on the query
- `boolean clearWhere()` - Clears all where conditions
- `void setUpdateAndSet(String updateAndTablename, String set)`
- `void addSelect(String selectSegment)` - Aggiunge un pezzo alla select.
- `void addSelect(Boolean enabled, String selectSegment)` - Aggiunge un pezzo alla select.
- `void setOrderAndLimit(YadaPageRequest pageable)` - Do not use for JPA queries because "limit" is not supported. Use query.setMaxResults() instead.
- `void setOrder(YadaPageRequest pageable)`
- `void setOrderAndLimit(String orderAndLimit)` - Shortcut for setting order and limit in one go. Do not use addOrder if limit is set here. For JPA queries use query.setMaxResults() for limit.
- `void addOrder(String columnName, String direction)` - Append an order. Do not use after setOrderAndLimit.
- `boolean addWhere(boolean enabled, String condition, String operator)`
- `void addWhere(String condition)`
- `void addWhere(String condition, String operator)`
- `void addHaving(String condition, String operator)`
- `String getSql()`
- `String getWhereConditionsString()` - Questo si può usare per costruire una stringa di condizioni per uso estemporaneo
- `void setFrom(String from)`
- `void addJoinOn(String join)` - Aggiunge una inner join ... on. Può essere usato anche per una left join e anche per un OQL senza on.
- `void addJoinOn(boolean enabled, String join)` - Aggiunge una inner join
- `void setSelectCount()` - Trasforma la query in un count(*), tenendo invariato il from ma cancellando group, order e limit
- `void setSelect(String select)`
- `void setSelectFrom(String select, String from)`
- `void setGroupby(String groupby)`
- `void addWhereIn(String attributeName, Collection values, String operator)` - Add a "where aaa in (x, y, z)" clause
- `void addWhereIn(boolean enabled, String clause, YadaSqlBuilder subquery, String operator)` - Gestisce una where tipo "where s.id not in (select distinct sl.storieLette_id from StorieLette sl where sl.UserProfile_id = 1)"
- `void addHaving(String condition)`
- `boolean hasParameter(String parameter)` - Checks if the query contains a ":named" parameter
- `void addJoinIfNotPresent(String joinPart)` - Add e join section if it hasn't already been added. The check is made on the literal string so be careful of spaces (case is insensitive though).
- `Field[] getExcludedFields()`
- `Query createNativeQuery(EntityManager em)`
- `Query createQuery(EntityManager em)`


== net.yadaframework.persistence.entity
=== YadaAttachedFile (YadaWeb/src/main/java/net/yadaframework/persistence/entity/YadaAttachedFile.java)
- `YadaAttachedFile()`
- `void ensureSortOrder()`
- `void setAllTitles(String uniqueTitle)` - Set the same title for all configured locales
- `void orderBefore(YadaAttachedFile toComeAfter)` - Ensures that the current entity is sorted before the parameter
- `File calcAndSetTargetFile(String namePrefix, String targetExtension, YadaAttachedFileType type, YadaIntDimension targetDimension, YadaConfiguration config)` - Computes the file to create, given the parameters, and sets it.
- `File calcAndSetTargetFile(String namePrefix, String targetExtension, YadaAttachedFileType type, YadaIntDimension targetDimension)` - Computes the file to create, given the parameters, and sets it.
- `File calcAndSetTargetFile(String namePrefix, String targetExtension, Integer targetWidth, YadaAttachedFileType type, YadaConfiguration config)` - Computes the file to create, given the parameters, and sets it.
- `File calcAndSetTargetFile(String namePrefix, String targetExtension, Integer targetWidth, YadaAttachedFileType type)` - Computes the file to create, given the parameters, and sets it.
- `File getAbsoluteFile(YadaAttachedFileType type, YadaConfiguration config)` - Returns the absolute file on the filesystem
- `void move(String newRelativeFolderPath) throws IOException` - Moves all files to a different relative path
- `boolean rename(String newName, YadaAttachedFileType type)` - Rename a file in the same folder. Do not use to move to a different folder.
- `File getAbsoluteFile(YadaAttachedFileType type)` - Returns the absolute file on the filesystem
- `// public YadaAttachedFile(Long attachedToId)` - //	/** //	 * Create an instance and attach to an Entity //	 * @param attachedToId the id of the owning entity //	 */
- `Long getId()`
- `void setId(Long id)`
- `Map<Locale, String> getTitle()`
- `String getLocalTitle()` - Returns the localized title in the current request locale
- `void setTitle(Map<Locale, String> title)`
- `Map<Locale, String> getDescription()`
- `void setDescription(Map<Locale, String> description)`
- `String getLocalDescription()` - Returns the localized title in the current request locale
- `Date getUploadTimestamp()`
- `void setUploadTimestamp(Date uploadTimestamp)`
- `long getVersion()`
- `Date getModified()`
- `void setModified(Date modified)`
- `boolean isPublished()`
- `void setPublished(boolean published)`
- `Locale getForLocale()`
- `void setForLocale(Locale forLocale)`
- `long getSortOrder()`
- `void setSortOrder(long sortOrder)`
- `String getRelativeFolderPath()`
- `void setRelativeFolderPath(String relativeFolderPath)`
- `String getFilenameMobile()`
- `void setFilenameMobile(String filenameMobile)`
- `String getFilenameDesktop()`
- `void setFilenameDesktop(String filenameDesktop)`
- `String getFilename()`
- `void setFilename(String filename)`
- `// public Long getAttachedToId()`
- `// public void setAttachedToId(Long attachedToId)`
- `String getClientFilename()`
- `void setClientFilename(String clientFilename)`
- `Field[] getExcludedFields()`
- `YadaIntDimension getImageDimension()`
- `void setImageDimension(YadaIntDimension dimension)`
- `boolean isImage()` - Returns true if the file is an image
- `boolean isDesktopImage()`
- `boolean isMobileImage()`
- `boolean isPdfImage()`
- `YadaIntDimension getDesktopImageDimension()`
- `void setDesktopImageDimension(YadaIntDimension desktopImageDimension)`
- `YadaIntDimension getMobileImageDimension()`
- `void setMobileImageDimension(YadaIntDimension mobileImageDimension)`
- `String getFilenamePdf()`
- `void setFilenamePdf(String filenamePdf)`
- `YadaIntDimension getPdfImageDimension()`
- `void setPdfImageDimension(YadaIntDimension pdfImageDimension)`
- `String getMetadata()`
- `void setMetadata(String metadata)`
- `int hashCode()`
- `boolean equals(Object obj)`
- `int compareTo(YadaAttachedFile other)` - This is useful to sort instances in a TreeSet based on sortOrder

=== YadaBrowserId (YadaWeb/src/main/java/net/yadaframework/persistence/entity/YadaBrowserId.java)
- `void setUUID(UUID uuid)` - Sets the UUID, e.g. from a cookie value
- `UUID getUUID()` - Returns the UUID, or null if not set
- `Long getId()`
- `void setId(Long id)`
- `Long getMostSigBits()`
- `void setMostSigBits(long mostSigBits)`
- `Long getLeastSigBits()`
- `void setLeastSigBits(long leastSigBits)`
- `long getVersion()`
- `void setMostSigBits(Long mostSigBits)`
- `void setLeastSigBits(Long leastSigBits)`

=== YadaClause (YadaWeb/src/main/java/net/yadaframework/persistence/entity/YadaClause.java)
- `Long getId()`
- `void setId(Long id)`
- `String getName()`
- `void setName(String name)`
- `String getContent()`
- `void setContent(String content)`
- `int getClauseVersion()`
- `void setClauseVersion(int version)`
- `long getVersion()`

=== YadaJob (YadaWeb/src/main/java/net/yadaframework/persistence/entity/YadaJob.java)
- `// public String getDT_RowId()`
- `YadaJobState getJobState()` - Returns the plain enum of the job state
- `void setJobState(YadaJobState jobState)` - Set the persistent state of this job
- `YadaJobState activate()` - Set the job state to active and return the previous state
- `YadaJobState pause()` - Set the job state to paused and return the previous state
- `YadaJobState disable()` - Set the job state to disabled and return the previous state
- `YadaJobState complete()` - Set the job state to completed and return the previous state
- `boolean isActive()`
- `boolean isCompleted()`
- `boolean isDisabled()`
- `boolean isPaused()`
- `boolean isRunning()`
- `int incrementErrorStreak()` - Increment the error streak count returning the new value
- `boolean equals(Object obj)` - Two YadaJob are equal only if the ids are equal and not null
- `int hashCode()` - The hashCode is derived from the id
- `Long getId()`
- `void setId(Long id)`
- `Date getJobScheduledTime()`
- `void setJobScheduledTime(Timestamp jobScheduledTime)`
- `Date getJobLastSuccessfulRun()` - Return the time that the job last ended successfully. Null if the job was never run successfully.
- `void setJobLastSuccessfulRun(Timestamp jobLastSuccessfulRun)`
- `String getJobName()`
- `void setJobName(String jobName)`
- `String getJobGroup()`
- `void setJobGroup(String jobGroup)`
- `String getJobDescription()`
- `void setJobDescription(String jobDescription)`
- `int getJobPriority()`
- `void setJobPriority(int jobPriority)` - 0 = lowest priority, MAX_INTEGER = highest priority
- `YadaPersistentEnum<YadaJobState> getJobStateObject()` - Do not use directly unless you know the implications. It could be stale and be different from the database value.
- `void setJobStateObject(YadaPersistentEnum<YadaJobState> jobStateObject)` - Do not use directly unless you know the implications. It could be stale and overwrite a more recent value in the database.
- `boolean isJobRecoverable()`
- `void setJobRecoverable(boolean recoverable)`
- `void setJobScheduledTime(Date jobScheduledTime)`
- `void setJobLastSuccessfulRun(Date jobLastSuccessfulRun)`
- `List<YadaJob> getJobsMustBeActive()`
- `void setJobsMustBeActive(List<YadaJob> jobsMustBeActive)`
- `List<YadaJob> getJobsMustBeInactive()`
- `void setJobsMustBeInactive(List<YadaJob> jobsMustBeInactive)`
- `List<YadaJob> getJobsMustComplete()`
- `void setJobsMustComplete(List<YadaJob> jobsMustComplete)`
- `boolean isJobGroupPaused()`
- `void setJobGroupPaused(boolean jobGroupPaused)`
- `Date getJobStartTime()`
- `void setJobStartTime(Date jobRunTime)`
- `boolean isRecovered()`
- `void setRecovered(boolean recovered)`
- `// public long getVersion()`
- `int getErrorStreakCount()`
- `void setErrorStreakCount(int errorStreakCount)`

=== YadaJobState (YadaWeb/src/main/java/net/yadaframework/persistence/entity/YadaJobState.java)
- `YadaPersistentEnum<YadaJobState> toYadaPersistentEnum()`
- `void setYadaPersistentEnum(YadaPersistentEnum yadaPersistentEnum)`
- `String toString(MessageSource messageSource, Locale locale)` - Return the localized text for this enum

=== YadaManagedFile (YadaWeb/src/main/java/net/yadaframework/persistence/entity/YadaManagedFile.java)
- `YadaManagedFile()`
- `String getFileExtension()` - Returns the file extension without dot, or null
- `YadaIntDimension getDimension()` - Returns width and height of a loaded image
- `YadaManagedFile move(File targetFile) throws IOException` - Moves this file to the target file, overwriting the target if it exists. The filename may change as a result.
- `* Returns the url to show this file on the web, but only if it is publicly visible (in a subfolder of the "contents" folder) * @return */ @Transient public String getUrl()`
- `File getAbsoluteFile()` - Returns the absolute file on the filesystem
- `Long getId()`
- `void setId(Long id)`
- `Date getUploadTimestamp()`
- `void setUploadTimestamp(Date uploadTimestamp)`
- `long getVersion()`
- `Date getModified()`
- `void setModified(Date modified)`
- `String getRelativeFolderPath()` - Returns the path of the folder where this file is stored, relative to the "base path"
- `void setRelativeFolderPath(String relativeFolderPath)` - Sets the path of the folder where this file is stored, relative to the "base path"
- `void setRelativeFolderPath(Path relativeFolderPath)` - Sets the path of the folder where this file is stored, relative to the "base path"
- `void setFilename(String filename)`
- `String getClientFilename()`
- `void setClientFilename(String clientFilename)`
- `Field[] getExcludedFields()`
- `String getDescription()`
- `void setDescription(String description)`
- `String getFilename()`
- `Long getSizeInBytes()`
- `void setSizeInBytes(Long bytes)`
- `boolean isExpired()`
- `boolean isTemporary()`
- `void setTemporary(boolean temporary)`
- `void setDimension(YadaIntDimension dimension)`
- `boolean isPrivateAccess()`
- `void setPrivateAccess(boolean privateAccess)`
- `Map<String, YadaManagedFile> getDerivedAssets()`
- `void setDerivedAssets(Map<String, YadaManagedFile> derivedAssets)`
- `Date getExpirationTimestamp()`
- `void setExpirationTimestamp(Date expirationTimestamp)`
- `void setVersion(long version)`

=== YadaPersistentEnum (YadaWeb/src/main/java/net/yadaframework/persistence/entity/YadaPersistentEnum.java)
- `void serialize(YadaPersistentEnum<?> value, JsonGenerator generator, SerializerProvider serializers) throws IOException, JsonProcessingException`
- `boolean equals(Enum<? extends YadaLocalEnum<?>> enumValue)` - Check this instance with a normal enum for equality. This is the preferred way for checking enums.
- `boolean equals(Object obj)`
- `int hashCode()`
- `void setEnum(E enumValue)` - Sets the attributes of a normal enum into this instance, but not the localized text
- `String getLocalText()` - Returns the localized value in the current locale
- `E toEnum() throws YadaInvalidValueException` - Convert the localised enum back to a normal Enum instance. Same as getEnum() WARNING: the enum returned fails to be compared with the original enum. Use {@link #equals(Enum)} instead.
- `E getEnum() throws YadaInvalidValueException` - Convert the localised enum back to a normal Enum instance. Same as toEnum() WARNING: the enum returned fails to be compared with the original enum. Use {@link #equals(Enum)} instead.
- `Long getId()`
- `void setId(Long id)`
- `int getEnumOrdinal()`
- `void setEnumOrdinal(int enumOrdinal)`
- `String getEnumName()`
- `void setEnumName(String enumName)`
- `Map<String, String> getLangToText()`
- `void setLangToText(Map<String, String> langToText)`
- `String getEnumClassName()`
- `void setEnumClassName(String enumClassName)`

=== YadaRateLog (YadaWeb/src/main/java/net/yadaframework/persistence/entity/YadaRateLog.java)
- `YadaRateLog()`
- `Long getId()`
- `void setId(Long id)`


== net.yadaframework.persistence.repository
=== YadaAttachedFileDao (YadaWeb/src/main/java/net/yadaframework/persistence/repository/YadaAttachedFileDao.java)
- `void purgeYadaAttachedFile(Long attachedFileId)` - Delete from db and disk
- `void delete(long yadaAttachedFileId)` - Delete a YadaAttachedFile and connected data, but not the file on disk. If there is a relationship between an Entity and the YadaAttachedFile, deletion fails because of the foreign key. The relationship should be removed before calling this method.
- `void delete(YadaAttachedFile yadaAttachedFile)` - Deletes a YadaAttachedFile when not null
- `void swapSortOrder(long oneId, long anotherId)` - Swaps the sortOrder of two YadaAttachedFile entities
- `YadaAttachedFile save(YadaAttachedFile yadaAttachedFile)`
- `void addTo(Object entity, List<YadaAttachedFile> list, YadaAttachedFile yadaAttachedFile)` - Adds a yadaAttachedFile to a list that could be lazy
- `YadaAttachedFile find(Long yadaAttachedFileId)` - Find entity from the id
- `Optional<YadaAttachedFile> findById(Long yadaAttachedFileId)`

=== YadaBrowserIdDao (YadaWeb/src/main/java/net/yadaframework/persistence/repository/YadaBrowserIdDao.java)
- `YadaBrowserId findByMostSigBitsAndLeastSigBits(long mostSigBits, long leastSigBits)` - Find the instance with the given value
- `YadaBrowserId ensureYadaBrowserId(String cookieName, int expirationSeconds, HttpServletRequest request, HttpServletResponse response)` - Get or create a YadaBrowserId using a cookie. The cookie will be set in case of creation.
- `YadaBrowserId findOrCreate(UUID uuid)` - Find or create the instance with the given UUID

=== YadaClauseDao (YadaWeb/src/main/java/net/yadaframework/persistence/repository/YadaClauseDao.java)
- `List<YadaClause> findByName(String name)`
- `YadaClause getTrattamentoDati()`
- `YadaClause getPubblicazioneSito()`
- `YadaClause getPubblicazioneRaccolta()`
- `long count()`
- `YadaClause save(YadaClause entity)`
- `void saveAll(List<YadaClause> yadaClauses)`

=== YadaFileManagerDao (YadaWeb/src/main/java/net/yadaframework/persistence/repository/YadaFileManagerDao.java)
- `int deleteStale()` - Delete all old YadaManagedFile that are temporary
- `boolean delete(YadaManagedFile yadaManagedFile)` - Deletes a file from disk and database
- `YadaManagedFile createManagedFile(MultipartFile multipartFile, File uploadedFile, String description)` - After a file has been uploaded and copied to the upload directory, create a YadaManagedFile that points to it.

=== YadaJobDao (YadaWeb/src/main/java/net/yadaframework/persistence/repository/YadaJobDao.java)
- `void setState(Long yadaJobId, YadaJobState yadaJobState)` - Set the state of a job in the database. If the job does not exist, nothing happens.
- `void delete(Long yadaJobId)` - Deletes a job
- `// public void setActiveWhenRunning(YadaJob yadaJob)`
- `List<YadaJob> getRecoverableJobs()` - Return the list of jobs that can be recovered after a crash.
- `void setUnrecoverableJobState()` - Disable all RUNNING jobs that are not recoverable and not group-paused. Called at server startup.
- `YadaPersistentEnum<YadaJobState> getJobState(Long yadaJobId)` - Get the current job state from the database
- `void stateChangeFromTo(Long yadaJobId, YadaPersistentEnum<YadaJobState> fromStateEnum, YadaPersistentEnum<YadaJobState> toStateEnum)` - Invoked by the scheduler classes when starting a job. Only an active job can become running
- `void stateChangeFromTo(YadaJob yadaJob, YadaJobState fromStateObject, YadaJobState toStateObject)` - Invoked by the scheduler classes when starting a job. Only an active job can become running
- `Date getJobStartTime(Long yadaJobId)` - Returns the start time of a job
- `Integer isJobGroupPaused(String jobGroup)` - Check if the job group has been paused. It is considered paused if at least one job in the group has the jobGroupPaused set
- `void setJobGroupPaused(String jobGroup, boolean paused)` - Set the pause flag on all jobs of a jobGroup
- `List<YadaJob> findByJobGroupAndState(String jobGroup, YadaPersistentEnum<YadaJobState> stateObject, YadaPageRequest pageable)` - Returns all jobs for the given group that are in the given state
- `YadaJob findRunning(String jobGroup)` - Returns the running job for the given group if any
- `long countByJobGroupAndStates(String jobGroup, Collection<YadaPersistentEnum<YadaJobState>> stateObjects)` - Returns the number of jobs for the given group, that are in one of the given states
- `List<YadaJob> findByJobGroupAndStates(String jobGroup, Collection<YadaPersistentEnum<YadaJobState>> stateObjects)` - Returns all jobs for the given group, that are in one of the given states
- `void setStartTime(long jobId, Date startTime)`
- `YadaJob save(YadaJob entity)`
- `Optional<YadaJob> findById(Long entityId)`

=== YadaJobSchedulerDao (YadaWeb/src/main/java/net/yadaframework/persistence/repository/YadaJobSchedulerDao.java)
- `// public void internalSetState(Long yadaJobId, YadaJobState state)`
- `void internalJobFailed(YadaJob yadaJob, Throwable thrown)`
- `void internalJobSuccessful(YadaJob yadaJob)`
- `List<? extends YadaJob> internalFindJobsToRun()`

=== YadaLocaleDao (YadaWeb/src/main/java/net/yadaframework/persistence/repository/YadaLocaleDao.java)
- `<entityClass> List<entityClass> findAllWithLocalValues(Class<?> entityClass)` - Finds all entities of the given type, then initializes all localized string attributes defined as Map&lt;Locale, String>
- `<entityClass> entityClass findOneWithLocalValues(Long entityId, Class<?> entityClass)` - Finds an object of the given id, then initializes all localized string attributes defined as Map&lt;Locale, String>
- `<entityClass> entityClass findOneWithLocalValuesRecursive(Long entityId, Class<?> entityClass)` - Finds an object of the given id, then initializes all localized string attributes defined as Map&lt;Locale, String> with recursion on the fields.
- `String getLocalValue(Object entity, String attributeName)` - Get the localized value of an entity's attribute or an empty string if no value is found.
- `String getLocalValue(Object entity, String attributeName, Locale locale, Boolean... returnNull)` - Get the localized value of an entity's attribute or an empty string if no value is found or null if no value is found and returnNull is true.
- `String getLocalValue(long entityId, Class<?> entityClass, String attributeName, Locale locale, Boolean... returnNull)` - Get the localized value of an entity's attribute or an empty string if no value is found or null if no value is found and returnNull is true.

=== YadaPersistentEnumDao (YadaWeb/src/main/java/net/yadaframework/persistence/repository/YadaPersistentEnumDao.java)
- `<T extends Enum<T>> YadaPersistentEnum<T> find(YadaLocalEnum<T> yadaLocalEnum) throws YadaInvalidUsageException` - Returns the persistent entity representing the given YadaLocalEnum implementation.
- `void initDatabase(List<Class<? extends YadaLocalEnum<?>>> enumClasses, Collection<Locale> locales)` - Fills the database with enum localized values, when missing. Can be used at app startup. Can add new enum values for a given enum class, but not remove them (must be done manually). Any added/deleted language will be taken care of properly. You can even change from a "en" to a "en_US" locale. A change in the localized text is handled properly.
- `<T extends Enum<T>> YadaPersistentEnum<T> findOne(Long id)`


== net.yadaframework.raw
=== YadaHttpUtil (YadaWeb/src/main/java/net/yadaframework/raw/YadaHttpUtil.java)
- `long validateProxy(String proxyAddress, int proxyPort, URL testUrl, int timeoutMillis)` - Validate a proxy over a given http page, with no proxy authentication
- `long validateProxy(String proxyAddress, int proxyPort, URL testUrl, int timeoutMillis, String username, String password, String userAgent)` - Validate a proxy over a given http page
- `void setCookie(String name, String value, HttpServletResponse response)` - Aggiunge un cookie alla response, senza scadenza e per tutte le pagine
- `boolean hasCookie(String name, HttpServletRequest request)` - Ritorna true se il cookie specificato è presente
- `Map<String, String> getCookies(HttpServletRequest request)` - Ritorna tutti i cookie mandati con la request
- `String getOneCookieValue(HttpServletRequest request, String name)` - Return the value of the first cookie found with the given name.
- `boolean isAjax(HttpServletRequest request)` - Tests if a request is an Ajax request
- `byte[] uncompress(byte[] compressedContent, String encodingName)` - Uncompress an InputStream
- `static boolean isContentTypeText(String contentType)` - Return true if the contentType is of text type: html, css, javascript...
- `int getRequestDocumentType(HttpServletRequest request)` - Return the document type by checking the request content type and the extension of the request path
- `int getDocumentType(String fullContentType)` - Returms the content type of a response given the content-type
- `String removePort(String hostAddress)` - Removes the port from a host address, which could also be ipv4 or ipv6
- `String extractAddress(String url)` - Extract the address from a url, without schema and path but with port. The schema or path can be missing. The result will be forced lowercase.
- `String extractPath(String url)` - Extract the path from a url (the servlet context and request parameters will be included)
- `String relativeToAbsolute(String currentFullAddress, String newRelativeAddress)` - Given the current full address and a relative address, computes the new full address. If the relative address starts with / then it is considered relative to the server, not to the servlet context. The relative address can be a url parameter like ?xx=yy or an anchor like #aaa
- `// public String extractDomain(String address)` - //	/** //	 * Extract the first level domain from a given address. //	 * @param address the address in any form: www.facebook.com or https://www.google.com:8080/doit //	 * @return the first level domain: facebook.com or google.com in the above examples //	 */
- `String getWebappFullAddress(HttpServletRequest request)` - Returns the current webapp address, e.g. http://www.myserver.net:8080/context/ with a trailing slash
- `void redirectPermanent(String newUrl, HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException`
- `void redirectTemporary(String newUrl, HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException`
- `void setAttribute(HttpServletRequest request, String name, Object value)`
- `void forward(String page, HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException`

=== YadaIntDimension (YadaWeb/src/main/java/net/yadaframework/raw/YadaIntDimension.java)
- `final static YadaIntDimension UNSET = new YadaIntDimension(-1, -1);` - UNSET is a special value to be used in @Entity instances instead of null that would generate a constraint violation
- `YadaIntDimension()` - Creates an instance of <code>YadaIntDimension</code> with a width of zero and a height of zero.
- `YadaIntDimension(YadaIntDimension d)` - Creates an instance of <code>YadaIntDimension</code> whose width and height are the same as for the specified dimension.               <code>width</code> and               <code>height</code> values
- `YadaIntDimension(Integer width, Integer height)` - Constructs a <code>YadaIntDimension</code> and initializes it to the specified width and specified height.
- `int getMax()` - Returns the biggest of width and height
- `static YadaIntDimension biggestCover(YadaIntDimension...dimensions)` - Returns the dimension with the width and height that cover all given dimensions.
- `static YadaIntDimension biggest(YadaIntDimension...dimensions)` - Returns the dimension with the width or height that has the biggest value, or the first one if there are many that qualify.
- `static YadaIntDimension smallest(YadaIntDimension...dimensions)` - Returns the smallest dimension, or the first one if there are many of the same size
- `boolean isUnset()`
- `boolean isBiggerThan(YadaIntDimension yadaIntDimension)` - Returns true if both dimensions are not smaller than the argument, nor both equal.
- `boolean isSmallerThan(YadaIntDimension yadaIntDimension)` - Returns true if both dimensions are not bigger than the argument, nor both equal.
- `boolean isAnyBiggerThan(YadaIntDimension yadaIntDimension)` - Returns true if at least one dimension is bigger.
- `boolean isAnySmallerThan(YadaIntDimension yadaIntDimension)` - Returns true if at least one dimension is smaller.
- `boolean isEqualTo(YadaIntDimension yadaIntDimension)` - Returns true if this instance is equal to the argument
- `int getWidth()`
- `int getHeight()`
- `void setSize(int width, int height)` - Sets the size of this <code>YadaIntDimension</code> object to the specified width and height.
- `void setSize(YadaIntDimension d)` - Sets the size of this <code>YadaIntDimension</code> object to the specified size.
- `boolean equals(Object obj)` - Checks whether two dimension objects have equal values.
- `int hashCode()` - Returns the hash code for this <code>YadaIntDimension</code>.
- `String toString()` - Returns a string representation in the form WxH

=== YadaLookupTable (YadaWeb/src/main/java/net/yadaframework/raw/YadaLookupTable.java)
- `final void put(V value, K... keys)` - Add a new row to the table using variable-length keys. Note that the value comes before the keys. No parameter can be null.
- `final V get(K... keys)` - Retrieve the value from the table given the keys.

=== YadaLookupTableFive (YadaWeb/src/main/java/net/yadaframework/raw/YadaLookupTableFive.java)
- `void put(K1 key1, K2 key2, K3 key3, K4 key4, V value)` - Add a new row to the table. No parameter can be null.
- `V get(K1 key1, K2 key2, K3 key3, K4 key4)` - Get the value of the last column given the first ones
- `YadaLookupTableFour<K2, K3, K4, V> getSubtable(K1 key1)` - Get a table of all the rows that match the first column value, excluding the first column.
- `void clear()` - Clear the table

=== YadaLookupTableFour (YadaWeb/src/main/java/net/yadaframework/raw/YadaLookupTableFour.java)
- `void put(K1 key1, K2 key2, K3 key3, V value)` - Add a new row to the table. No parameter can be null.
- `V get(K1 key1, K2 key2, K3 key3)` - Get the value of the last column given the first ones
- `YadaLookupTableThree<K2, K3, V> getSubtable(K1 key1)` - Get a table of all the rows that match the first column value, excluding the first column.
- `void clear()` - Clear the table

=== YadaLookupTableSix (YadaWeb/src/main/java/net/yadaframework/raw/YadaLookupTableSix.java)
- `void put(K1 key1, K2 key2, K3 key3, K4 key4, K5 key5, V value)` - Add a new row to the table. No parameter can be null.
- `V get(K1 key1, K2 key2, K3 key3, K4 key4, K5 key5)` - Get the value of the last column given the first ones
- `YadaLookupTableFive<K2, K3, K4, K5, V> getSubtable(K1 key1)` - Get a table of all the rows that match the first column value, excluding the first column.
- `void clear()` - Clear the table

=== YadaLookupTableThree (YadaWeb/src/main/java/net/yadaframework/raw/YadaLookupTableThree.java)
- `void put(K1 key1, K2 key2, V value)` - Add a new row to the table. No parameter can be null.
- `V get(K1 key1, K2 key2)` - Get the value of the last column given the first ones
- `Map<K2, V> getSubtable(K1 key1)` - Get a table of all the rows that match the first column value, excluding the first column.
- `void clear()` - Clear the table
- `boolean isEmpty()`
- `Collection<V> values()` - Returns a Collection of the values contained in this table (i.e. the last column). The collection is not backed by the table, so changes to the table are not reflected in the collection.

=== YadaNetworkUtil (YadaWeb/src/main/java/net/yadaframework/raw/YadaNetworkUtil.java)
- `List<InetAddress> findPublicAddresses() throws IOException` - Find all ipv4 and ipv6 addresses on the local host that can be used for sending packets to the internet

=== YadaRegexUtil (YadaWeb/src/main/java/net/yadaframework/raw/YadaRegexUtil.java)
- `String escapeDots(String source)` - Escapes dots in regular expressions, replacing them with \.
- `Pattern getOrCreatePattern(String pattern)` - Return a compiled Pattern, either from the cache or new.
- `Pattern getOrCreatePattern(String pattern, Map<String, Pattern> patternCache)` - Return a compiled Pattern, either from the cache or new, with case-insensitive and dotall flags set.
- `Matcher createMatcher(String source, String pattern)` - Return a matcher for the given source document and pattern. Compiled Pattern objects are cached.
- `Matcher createMatcher(String source, String pattern, Map<String, Pattern> patternCache)` - Return a matcher for the given source document and pattern. Compiled Pattern objects are cached.
- `String replaceInRegion(String source, String startPattern, String endPattern, boolean repeatRegion, YadaRegexReplacer replacer)` - Performs a find-and-replace in a delimited area of the source, returning the source with replacements applied.
- `String extractInRegion(String source, String startPattern, String endPattern, String extractPattern)` - Searches a region of text for a pattern that contains a capturing group, and returns the captured group.                     The matched text is excluded from search.                     The matched text is excluded from search.


== net.yadaframework.selenium
=== YadaSeleniumUtil (YadaWeb/src/main/java/net/yadaframework/selenium/YadaSeleniumUtil.java)
- `Object runJavascript(String script, WebDriver webDriver, Object...args)` - Run some javascript. <p> If the script has a return value (i.e. if the script contains a <code>return</code> statement), then the following steps will be taken: <ul> <li>For an HTML element, this method returns a WebElement</li> <li>For a decimal, a Double is returned</li> <li>For a non-decimal number, a Long is returned</li> <li>For a boolean, a Boolean is returned</li> <li>For all other cases, a String is returned.</li> <li>For an array, return a List&lt;Object&gt; with each object following the rules above. We support nested lists.</li> <li>For a map, return a Map&lt;String, Object&gt; with values following the rules above.</li> <li>Unless the value is null or there is no return value, in which null is returned</li> </ul> <p> Arguments must be a number, a boolean, a String, WebElement, or a List of any combination of the above. An exception will be thrown if the arguments do not meet these criteria. The arguments will be made available to the JavaScript via the "arguments" magic variable, as if the function were called via "Function.apply"
- `boolean urlMatches(Pattern urlPattern, WebDriver webDriver)` - Returns true if the current url matches the specified pattern
- `boolean urlContains(String urlSegment, WebDriver webDriver)` - Returns true if the current url contains the specified string at any position
- `String getSourceSnippet(String startPattern, String endPattern, String extractPattern, WebDriver webDriver)` - Returns a part of the page source. The matched text is not part of the search area. The matched text is not part of the search area.
- `String getByJavascript(String javascriptCode, WebDriver webDriver)` - Return a value calculated via javascript.
- `void typeAsHuman(WebElement inputField, String text)` - Insert some text slowly into a field
- `void setSlowPageLoadTimeout(WebDriver webDriver)` - From now on, page load timeout will be set to the "slow" value defined in the "slowPageLoadSeconds" confg property
- `void setNormalPageLoadTimeout(WebDriver webDriver)` - From now on, page load timeout will be set to the "normale" value defined in the "pageLoadSeconds" confg property
- `void setPageloadTimeoutSeconds(WebDriver webDriver, long timeoutSeconds)` - Set the pageload timeout for the following requests
- `WebElement findOrNull(SearchContext from, By by)` - Return the first element matched by the selector, or null if not found
- `String getTextIfExists(SearchContext from, By by)` - Search for text
- `boolean foundByText(List<WebElement> webElements, String text)` - Returns true if an element contains the given text (literally)
- `WebElement findById(String id, WebDriver webDriver)` - Get an element by id
- `boolean foundById(String id, WebDriver webDriver)` - Check if an element with a given id exists
- `boolean foundByClass(String className, WebDriver webDriver)` - Check if at least one element with the given class exists
- `WebDriver makeWebDriver(File customProfileDir, InetSocketAddress proxyToUse, String proxyUser, String proxyPassword, Set<Cookie> cookiesToSet, int driverType)` - Create a new browser instance positioning the window cookie with the same name has not been received. It's not possible to set cookies BEFORE the first get (by design of WebDriver).
- `WebDriver makeWebDriver(File customProfileDir, InetSocketAddress proxyToUse, String proxyUser, String proxyPassword, Set<Cookie> cookiesToSet, int driverType, String userAgent)` - Create a new browser instance positioning the window cookie with the same name has not been received. It's not possible to set cookies BEFORE the first get (by design of WebDriver).
- `void get(String url)`
- `void positionWindow(int posx, int posy, int width, int height, WebDriver webDriver)`
- `void clickByJavascript(WebElement webElement, WebDriver webDriver)` - Click on the given element using javascript. This is useful when any other form of clicking fails with the error "Element is not clickable". Can't be used on HTML elements that don't have the "click()" method
- `void clickByJavascript(WebElement webElement, WebDriver webDriver, int minPercentX, int maxPercentX, int minPercentY, int maxPercentY)` - Click on the given element using javascript. This is useful when any other form of clicking fails with the error "Element is not clickable". Can't be used on HTML elements that don't have the "click()" method
- `void randomClick(WebElement webElement, WebDriver webDriver)` - Click on the given element in a range between 20% and 80% of the dimensions
- `void randomClick(WebElement webElement, WebDriver webDriver, int minPercentX, int maxPercentX, int minPercentY, int maxPercentY)` - Click on the given element in a range between min and max % of the dimensions.
- `void waitUntilAttributeNotEmpty(WebElement element, String attribute, WebDriver webDriver, long timeOutInSeconds)` - Waits until the attribute contains some non-empty text
- `void waitWhileEmptyText(WebElement element, WebDriver webDriver, long timeOutInSeconds)` - Waits until the selected element contains some non-empty text (warning: this method may not work as expected)
- `void waitWhileEmptyText(String cssSelector, WebDriver webDriver, long timeOutInSeconds)` - Waits until the selected element contains some non-empty text
- `void waitUntilPresent(String cssSelector, WebDriver webDriver, long timeOutInSeconds)` - Wait until the selector matches an element
- `void waitUntilVisible(String cssSelector, WebDriver webDriver, long timeOutInSeconds)` - Wait until the selector matches a visible element
- `void waitWhilePresent(String cssSelector, WebDriver webDriver, long timeOutInSeconds)` - Wait until the selector matches zero elements
- `void waitWhileVisible(String cssSelector, WebDriver webDriver, long timeOutInSeconds)`
- `void waitWhileVisible(WebElement webElement, WebDriver webDriver, long timeOutInSeconds)`
- `void waitUntilLost(WebElement webElement, WebDriver webDriver, long timeOutInSeconds)` - Waits until the element is no more attached to the DOM (stale). It happens when a new page is loaded, for example.
- `void waitWhileVisible(List<WebElement> webElements, WebDriver webDriver, long timeOutInSeconds)`
- `// public boolean waitForClass(String className, WebDriver webDriver)`
- `// public Boolean apply(WebDriver d)`
- `String relativeToAbsolute(String relativeAddress, WebDriver webDriver)` - Given a relative address, computes the new full address. If the relative address starts with / then it is considered relative to the server, not to the servlet context Do not use it to convert href attributes because this is done by WebDriver automatically.
- `void takeScreenshot(WebDriver webDriver, Path toPath)` - Take a browser screenshot and move it to the specified path


== net.yadaframework.tools
=== AntIncrementBuild (YadaWeb/src/main/java/net/yadaframework/tools/AntIncrementBuild.java)
- `AntIncrementBuild(File configFile)`
- `static void main(String[] args)`

=== YadaSchemaGenerator (YadaWeb/src/main/java/net/yadaframework/tools/YadaSchemaGenerator.java)
- `static void main(String[] args)`


== net.yadaframework.web
=== YadaController (YadaWeb/src/main/java/net/yadaframework/web/YadaController.java)
- `@ResponseBody public Map<String, Object> yadaDataTableData(YadaDatatablesRequest yadaDatatablesRequest, HttpServletRequest request, Locale locale) throws AccessDeniedException`
- `String yadaTimezone(String timezone, HttpSession httpSession, Model model, Locale locale)` - Set the user timezone. Called by yada.js at each new browser session
- `String yadaError(HttpServletRequest request, HttpServletResponse response, RedirectAttributes redirectAttributes, Model model, Locale locale)`

=== YadaCropImage (YadaWeb/src/main/java/net/yadaframework/web/YadaCropImage.java)
- `double getTargetMobileProportions()` - Used on the browser
- `double getTargetDesktopProportions()` - Used on the browser
- `double getTargetPdfProportions()` - Used on the browser
- `String getSourceUrl()` - Returns the url of the image to be cropped
- `YadaAttachedFile linkAdd()` - Link to an Entity by means of a new YadaAttachedFile. Useful when adding a new image to a list.
- `YadaAttachedFile link(YadaAttachedFile someYadaAttachedFile)` - Link to an Entity by means of a YadaAttachedFile.
- `YadaCropImage cropDesktop()` - Enables image cropping for desktop size
- `YadaCropImage cropMobile()` - Enables image cropping for mobile size
- `YadaCropImage cropPdf()` - Enables image cropping for pdf size
- `YadaManagedFile getImageToCrop()`
- `YadaAttachedFile getYadaAttachedFile()`
- `void setYadaAttachedFile(YadaAttachedFile yadaAttachedFile)`
- `String getTargetRelativeFolder()`
- `String getTargetNamePrefix()`
- `YadaIntDimension getTargetDesktopDimension()`
- `YadaIntDimension getTargetMobileDimension()`
- `boolean isCropDesktop()`
- `boolean isCropMobile()`
- `String getTitleKey()`
- `YadaCropImage titleKey(String titleKey)` - Set the title to show when cropping this image
- `boolean isCropPdf()`
- `YadaIntDimension getTargetPdfDimension()`

=== YadaCropQueue (YadaWeb/src/main/java/net/yadaframework/web/YadaCropQueue.java)
- `YadaCropQueue(String cropRedirect, String destinationRedirect)` - Package-accessible constructor used by YadaSession
- `YadaCropImage getCurrentImage()` - Returns the next image that has to be cropped
- `void delete()` - Delete any images in the queue, aborting the upload of any remaining pictures.
- `YadaCropImage addCropImage(YadaManagedFile imageToCrop, YadaIntDimension[] targetDimensions, String targetRelativeFolder, String targetNamePrefix)` - Add an image to be cropped
- `boolean hasCropImages()` - Check if there are any images to be cropped
- `int getCount()` - Count the images left to be cropped
- `String getDestinationRedirect()` - Returns the url to open when the crop is terminated on all images
- `String getCropPerformAction()`
- `String getCropRedirect()`
- `int getTotInitialImages()`

=== YadaDatatablesColumn (YadaWeb/src/main/java/net/yadaframework/web/YadaDatatablesColumn.java)
- `YadaDatatablesColumn()`
- `YadaDatatablesColumnSearch getSearch()`
- `String getDataOrName()`
- `String getNameOrData()` - Returns the column name if any, the column data otherwise.
- `String getData()`
- `String getName()`
- `boolean isSearchable()`
- `boolean isOrderable()`

=== YadaDatatablesColumnSearch (YadaWeb/src/main/java/net/yadaframework/web/YadaDatatablesColumnSearch.java)
- `String getValue()`
- `boolean isRegex()`

=== YadaDatatablesOrder (YadaWeb/src/main/java/net/yadaframework/web/YadaDatatablesOrder.java)
- `int getColumnIndex()` - Column number to which ordering should be applied. This is an index reference to the columns array of information that is also submitted to the server.
- `String getDir()`

=== YadaDatatablesRequest (YadaWeb/src/main/java/net/yadaframework/web/YadaDatatablesRequest.java)
- `void addExtraJsonAttribute(String attributePath)` - Add any entity attribute you need to return in the json object. Can be a path.
- `int getDraw()`
- `void setDraw(int draw)`
- `int getStart()`
- `void setStart(int start)`
- `int getLength()`
- `void setLength(int length)`
- `List<YadaDatatablesColumn> getColumns()`
- `void setColumns(List<YadaDatatablesColumn> columns)`
- `YadaDatatablesColumnSearch getSearch()`
- `void setSearch(YadaDatatablesColumnSearch search)`
- `List<YadaDatatablesOrder> getOrder()`
- `void setOrder(List<YadaDatatablesOrder> order)`
- `long getRecordsTotal()`
- `void setRecordsTotal(long recordsTotal)`
- `long getRecordsFiltered()`
- `void setRecordsFiltered(long recordsFiltered)`
- `YadaSql getYadaSql()` - Can be used to set join and where conditions on the query. Use "e" as the alias for the root table. E.g.: "e.user.role=4"
- `Map<String, String> getExtraParam()`
- `void setExtraParam(Map<String, String> extraParam)`
- `List<String> getExtraJsonAttributes()`
- `void setExtraJsonAttributes(List<String> extraJsonAttributes)`
- `String getDataTableId()`
- `void setDataTableId(String dataTableId)`
- `boolean isLegacy()`
- `void setLegacy(boolean legacy)`

=== YadaGlobalExceptionHandler (YadaWeb/src/main/java/net/yadaframework/web/YadaGlobalExceptionHandler.java)
- `ModelAndView globalErrorHandler(HttpServletRequest request, HttpServletResponse response, Exception e) throws Exception`

=== YadaJsonDateSimpleSerializer (YadaWeb/src/main/java/net/yadaframework/web/YadaJsonDateSimpleSerializer.java)
- `void serialize(Date date, JsonGenerator jsonGenerator, SerializerProvider serializerProvider) throws IOException, JsonProcessingException`

=== YadaJsonDateTimeShortSerializer (YadaWeb/src/main/java/net/yadaframework/web/YadaJsonDateTimeShortSerializer.java)
- `void serialize(Date date, JsonGenerator jsonGenerator, SerializerProvider serializerProvider) throws IOException, JsonProcessingException`

=== YadaJsonRawStringSerializer (YadaWeb/src/main/java/net/yadaframework/web/YadaJsonRawStringSerializer.java)
- `void serialize(String value, JsonGenerator gen, SerializerProvider provider) throws IOException`

=== YadaPageRequest (YadaWeb/src/main/java/net/yadaframework/web/YadaPageRequest.java)
- `YadaPageSortApi appendSort(String paramNames)` - Add sort parameters after any existing ones
- `YadaPageSortApi prependSort(String paramNames)` - Add sort parameters before any existing ones
- `static YadaPageRequest of(int page, int size)` - Creates a YadaPageRequest with the given page and size. Drop-in replacement for the equivalent Spring Data PageRequest.of() method
- `YadaPageRequest()` - Creates a new "invalid" YadaPageRequest, with {@code page=-1, size=0}. Has the same meaning of a "null" value. Used by Spring when injecting method parameters in a @Controller with no request values to set. See {@link #isValid()}
- `boolean isValid()` - Check if this object has been created with actual values.
- `YadaPageRequest(int page, int size)` - Creates a new {@link YadaPageRequest}. Pages are zero indexed, thus providing 0 for {@code page} will return the first page.
- `YadaPageRequest(int page, int size, boolean loadPrevious)` - Creates a new {@link YadaPageRequest}. Pages are zero indexed, thus providing 0 for {@code page} will return the first page.
- `YadaPageRequest(int page, int size, boolean loadPrevious, String paramPrefix)` - Creates a new {@link YadaPageRequest}. Pages are zero indexed, thus providing 0 for {@code page} will return the first page.
- `YadaPageRequest addSort(String paramName, Boolean desc, Boolean ignoreCase)` - Add a sort order to this request
- `YadaPageSort getPageSort()`
- `void setPageSort(YadaPageSort pageSort)` - Set the page sort options
- `void setLoadPrevious(boolean loadPrevious)` - Tell if data from previous pages should also be returned
- `YadaPageRequest getNextPageRequest()`
- `YadaPageRequest getPreviousPageRequest()`
- `YadaPageRequest getFirstPageRequest()`
- `String toString()`
- `int getSize()`
- `int getPage()`
- `int getOffset()` - Returns the position of the first element of this page in the database: {@code size*page}
- `int getFirstResult()` - Returns the position of the first element to be loaded from the database: {@code size*page} when loadPrevious is false, 0 otherwise
- `int getMaxResults()` - Returns the amount of rows to fetch from the database + 1. It is equal to {@link #getSize()+1} when loadPrevious is false, otherwise it adds the count of all the previous pages to the value then adds 1 to find out if there are more rows to fetch after this page. Note: this method must only be used when the results are stored into a YadaPageRows object otherwise the page size will be one element bigger than expected. <p>If you are not going to store the result in YadaPageRows, use {@link #getSize()} instead</p>
- `boolean isFirst()`
- `YadaPageRequest getPreviousOrFirstRequest()`
- `boolean isLoadPrevious()`
- `int hashCode()`
- `boolean equals(Object obj)`
- `void setPage(int page)`
- `void setSize(int size)`
- `int getPageSize()` - Spring Data - compatible method to get the page size (number of rows)
- `List<String> getSort()` - sort strings passed as request parameters - not to be used by the application
- `void setSort(List<String> sort)` - sort strings passed as request parameters - not to be used by the application
- `String getYadaContainer()`
- `void setYadaContainer(String yadaContainer)`
- `int getYadaScroll()`
- `void setYadaScroll(int yadaScroll)`
- `String getParamPrefix()`
- `void setParamPrefix(String paramPrefix)`

=== YadaPageRows (YadaWeb/src/main/java/net/yadaframework/web/YadaPageRows.java)
- `YadaPageRows()` - This constructor is useful when dynamically adding rows to an existing list on user interaction
- `YadaPageRows(List<T> rows, YadaPageRequest currentPageRequest, long outOfRows)`
- `YadaPageRows(List<T> rows, YadaPageRequest currentPageRequest)`
- `int getNumberOfElements()` - Returns the number of elements in this page, which can be less, equal or higher than the page size
- `List<T> getContent()` - Returns the content of this page
- `String getPageParam()`
- `String getSizeParam()`
- `String getLoadPreviousParam()`
- `List<T> getRows()` - Returns the page data fetched from database. It also contains the rows of all previous pages if {@link YadaPageRequest#isLoadPrevious()} is true
- `int getRowNumber()`
- `int getPageSize()`
- `int getPage()`
- `int getNextPage()`
- `YadaPageRequest getYadaPageRequest()`
- `long getOutOfRows()`
- `boolean hasMoreRows()`
- `boolean isEmpty()`
- `boolean isFirst()`
- `boolean isLast()`
- `Iterator<T> iterator()`
- `int hashCode()`
- `boolean equals(Object obj)`
- `YadaPageRequest getNextPageRequest()` - Returns the next page request, for use in the "load more" buttons, or null if this is the last page.
- `void add(T row)` - This method is useful when dynamically adding rows to an existing list on user interaction

=== YadaPageSort (YadaWeb/src/main/java/net/yadaframework/web/YadaPageSort.java)
- `String getProperty()`
- `void setProperty(String property)`
- `String getDirection()`
- `void setDirection(String direction)`
- `boolean isIgnorecase()`
- `void setIgnorecase(boolean ignorecase)`
- `YadaPageSortApi asc()` - Set 'asc' sort direction on the previously specified sort parameters
- `YadaPageSortApi desc()` - Set 'desc' sort direction on the previously specified sort parameters
- `YadaPageSortApi ignorecase()` - Set ignorecase sort on the previously specified sort parameters
- `Iterator<Order> iterator()` - Spring Data compatible API to retrieve sort parameters.
- `List<Order> getOrders()`
- `void add(Order order)` - Adds the given sort order to the list.

=== YadaPersistentEnumEditor (YadaWeb/src/main/java/net/yadaframework/web/YadaPersistentEnumEditor.java)
- `* public void initBinder(WebDataBinder binder)`
- `YadaPersistentEnumEditor(Class[] allEnums)` - Create an editor that can convert the specified enum classes.
- `void setAsText(String enumName) throws IllegalArgumentException`
- `String getAsText()`

=== YadaPublicSuffix (YadaWeb/src/main/java/net/yadaframework/web/YadaPublicSuffix.java)
- `String extractServerAddress(String hostAddress)` - Given a value like "www.something.co.uk" returns "something.co.uk"
- `void parse(File list) throws IOException`
- `static void main(String[] args) throws IOException`
- `yadaPublicSuffix.parse(new File("C:\\srv\\fcmp\\public_suffix_list.dat"));`


== net.yadaframework.web.datatables
=== YadaDataTable (YadaWeb/src/main/java/net/yadaframework/web/datatables/YadaDataTable.java)
- `// Must not be public because there's no need to store the locale when the instance lives with the current request (i.e. is not a singleton) protected YadaDataTable(String id, Locale locale)` - Constructor used internally by {@link YadaDataTableFactory}
- `YadaDataTableLanguage dtLanguageObj(String languageBaseUrl)` - Set the base url where language files are located. It can be the official DataTables URL or a local endpoint. 			Can be a CDN or a local folder e.g. "/static/datatables-2.1.8/i18n" depending on where the sources have been saved. 			It can contain Thymeleaf expressions. 			When null or empty, the default value is used: https://cdn.datatables.net/plug-ins/2.1.8/i18n/
- `YadaDataTable dtAjaxUrl(String ajaxUrl)` - Set the URL where data is fetched from (without language in the path). Can contain Thymeleaf expressions. Only to be used when the default handling is not enough.
- `YadaDataTable dtAjaxUrl(YadaDtAjaxHandler handlerRef)` - Type-safe overload that accepts a method reference to a controller method and extracts its @RequestMapping path (combined with class-level mapping if present). Usage example inside a controller: <pre>.dtAjaxUrl(this::userProfileTablePage)</pre> The referenced method is expected to have the signature: Map<String,Object> method(YadaDatatablesRequest request, Locale locale)
- `YadaDataTable dtEntityClass(Class entityClass)` - Set the entity class that will be used to fetch data from the database. Needs to be set when using yadaDataTableDao.getConvertedJsonPage() or an exception will be thrown by the ajax call.
- `// public YadaDataTable dtSecurityAs(String path)` - //	/** //	 * Set the path to check for user authorisation when downloading the table data. //	 * This should be a path that has been configured in SecurityConfig. //	 * //	 * @param path the path to check for user authorisation //	 * @return this instance for method chaining //	 */
- `YadaDataTableHTML dtStructureObj()` - Set the HTML table structure
- `YadaDTOptions dtOptionsObj()`


== net.yadaframework.web.datatables.config
=== YadaDataTableButton (YadaWeb/src/main/java/net/yadaframework/web/datatables/config/YadaDataTableButton.java)
- `YadaDataTableConfirmDialog dtConfirmDialogObj()` - Enable javascript-side confirmation dialog for button action. Note: a backend call will always be able to trigger a confirmation dialog regardless of this setting.
- `YadaDataTableButton dtMultiRow()` - Enable the toolbar button when one or many rows are selected. The default is for the toolbar button to be enabled only when one row is selected.
- `YadaDataTableButton dtToolbarCssClass(String cssClass)` - CSS class to be applied to the button in the toolbar, for example 'btn-primary'. The 'btn' class is added automatically.
- `YadaDataTableButton dtIcon(String iconHTML)` - HTML content for the button's icon. The icon is used in the command column when enabled, and on the left of the toolbar button text.
- `YadaDataTableButton dtUrlProvider(String function)` - javascript function to be called when the button is clicked in order to compute the target URL for the button action. When the row icon is clicked, it will receive the row data as a parameter. When the toolbar button is clicked, it will receive the datatable api object and the array of selected ids as parameters. The function should return the target URL or null to prevent any further action.
- `YadaDataTableButton dtUrl(String url)` - URL to be called when the button is clicked, it can be a thymeleaf expression and will be inserted in a @{} when missing
- `YadaDataTableButton dtGlobal()` - Set for a button that is always enabled regardless of row selection, for example an Add button. Global buttons are not shown in the command column.
- `YadaDataTableButton dtIdName(String idName)` - Name of the ID request parameter, default is "id". Useless for global buttons.
- `YadaDataTableButton dtNoAjax()` - Indicate that the button should use a normal request
- `YadaDataTableButton dtHidePageLoader()` - Do not show the page loader when the button is clicked
- `YadaDataTableButton dtElementLoader(String cssSelector)` - Show the loader on the selected element
- `YadaDataTableButton dtWindowTarget(String windowTarget)` - Name of the window for opening the URL in a new window. Special values are "_blank", "_self", "_parent", "_top"
- `YadaDataTableButton dtWindowFeatures(String windowFeatures)` - Features of the window when opened, such as its size, scrollbars, and whether it is resizable.
- `YadaDataTableButton dtShowCommandIcon(String function)` - Javascript function that determines whether to show the button icon for each row. It receives the "data", "row" and "meta" parameters as for the render functions, and also dataTableJson and currentUserRoles. It must return true/false to show/hide the icon or "disabled" to show it disabled. These buttons are not shown in the toolbar.
- `YadaDataTableButton dtRole(String roleKey)` - Role that can access the button. Will be removed if the user does not have this role. Can be called multiple times.
- `YadaDataTableHTML back()`

=== YadaDataTableColumn (YadaWeb/src/main/java/net/yadaframework/web/datatables/config/YadaDataTableColumn.java)
- `// public YadaDTColumnsWrapper dt()`
- `YadaDataTableColumn dtCssClasses(String cssClasses)` - css classes to set on the cell.
- `YadaDataTableColumn dtOrderableOff()` - Makes the column not orderable
- `YadaDataTableColumn dtSearchableOff()` - Makes the column not searchable
- `YadaDataTableColumn dtName(String name)` - Set a name on this column for database operations and cross reference. Be careful that it matches the Entity attribute name when "data" is not a function. Examples: "id", "useCredentials.username" When unset, the "data" option value is used.
- `YadaDataTableColumn dtOrderAsc()` - Choose this column for the initial sorting, ascending
- `YadaDataTableColumn dtOrderAsc(int precedence)` - Choose this column for the initial sorting, ascending
- `YadaDataTableColumn dtOrderDesc()` - Choose this column for the initial sorting, descending
- `YadaDataTableColumn dtOrderDesc(int precedence)` - Choose this column for the initial sorting, descending
- `YadaDataTableColumn dtResponsivePriority(int responsivePriority)` - In a responsive table control the order in which columns are hidden. Responsive will automatically remove columns from the right-hand-side of the table when a table is too wide for a given display, unless this value is set. 			are given a higher priority (i.e. a column with priority 2 will be 			hidden before a column with priority 1). The default is 10000.
- `YadaDataTableHTML back()`

=== YadaDataTableConfirmDialog (YadaWeb/src/main/java/net/yadaframework/web/datatables/config/YadaDataTableConfirmDialog.java)
- `YadaDataTableConfirmDialog dtPlaceholderColumnName(String ... columnName)` - Set the columns that hold the value for the placeholders
- `YadaDataTableConfirmDialog dtTitle(String confirmTitle)` - Set the title for the confirmation dialog
- `YadaDataTableConfirmDialog dtMessageSingular(String confirmOneMessage)` - Set the message for the confirmation dialog when one element is selected
- `YadaDataTableConfirmDialog dtMessagePlural(String confirmManyMessage)` - Set the message for the confirmation dialog when multiple elements are selected. 		The {0} placeholder will be replaced with the number of selected rows while the others are shifted one place
- `YadaDataTableConfirmDialog dtConfirmButton(String confirmButtonText)` - Set the text for the confirmation button
- `YadaDataTableConfirmDialog dtAbortButton(String abortButtonText)` - Set the text for the cancel button

=== YadaDataTableHTML (YadaWeb/src/main/java/net/yadaframework/web/datatables/config/YadaDataTableHTML.java)
- `YadaDataTableColumn dtColumnObj(String headerText, String data)` - Add a new column to the table. Order is preserved. 		It can eventually be localized to access localized strings using e.g. <pre>"title."+locale.getLanguage()</pre> 		for a title in the current user locale.<br> 		Can also be a javascript function provided on page if the value is like "someFunction()". 		In that case the function signature should be <pre>function(data, type, row, meta)</pre> 		Remember in such case to set a name on the column with {@link YadaDataTableColumn#dtName(String)} for database operations. 		@see <a href="https://datatables.net/reference/option/columns.data">DataTables Reference: columns.data</a>
- `YadaDataTableButton dtButtonObj(String text)` - Add a new button to the table. It can appear both in the commands column and in the toolbar. Order is preserved.
- `YadaDataTableHTML dtFooter()` - Shows a footer with the same content as the header
- `YadaDataTableHTML dtColumnCheckbox(String title)` - Adds the leftmost column with select checkboxes
- `YadaDataTableHTML dtColumnCommands(String title)` - Adds the rightmost column with command icons
- `YadaDataTableHTML dtColumnCommands(String title, int responsivePriority)` - Adds the rightmost column with command icons
- `YadaDataTableHTML dtCssClasses(String cssClasses)` - Set the css classes to set on the table tag. Defaults to 'table-striped no-wrap'
- `YadaDataTable back()`

=== YadaDataTableLanguage (YadaWeb/src/main/java/net/yadaframework/web/datatables/config/YadaDataTableLanguage.java)
- `YadaDataTableLanguage dsAddLanguage(String language, String jsonFile)` - Add a language definition. Default values for it, de, es and fr are already set. See <a href="https://datatables.net/plug-ins/i18n/">Internationalisation plug-ins</a> for a list of available translations. The language must also be enabled in the application configuration under the &lt;i18n> tag.


== net.yadaframework.web.datatables.options
=== YadaDTBreakpoint (YadaWeb/src/main/java/net/yadaframework/web/datatables/options/YadaDTBreakpoint.java)
- `YadaDTBreakpoint(YadaDTResponsive parent)`
- `YadaDTBreakpoint dtName(String name)`
- `YadaDTBreakpoint dtWidth(String width)`

=== YadaDTColumnDef (YadaWeb/src/main/java/net/yadaframework/web/datatables/options/YadaDTColumnDef.java)
- `YadaDTColumnDef dtTargets(int ... columnIndex)` - Set the target for the column definition. <ul> <li>0 or a positive integer - column index counting from the left</li> <li>a negative integer - column index counting from the right</li> </ul>
- `YadaDTColumnDef dtTargetsCss(String cssSelector)` - Set the target for the column definition.
- `YadaDTColumnDef dtTargetsName(String columnName)` - Set the target for the column definition.
- `YadaDTColumnDef dtTargetsAll()` - Target all columns.

=== YadaDTColumns (YadaWeb/src/main/java/net/yadaframework/web/datatables/options/YadaDTColumns.java)
- `YadaDTColumns(YadaDTOptions parent)`
- `// public boolean dtIsOrderable()` - //    /** //     * @return Whether the column is orderable. //     * @see <a href="https://datatables.net/reference/option/columns.orderable">DataTables Reference: columns.orderable</a> //     */
- `// public boolean dtIsSearchable()` - //    /** //     * @return Whether the column is searchable. //     * @see <a href="https://datatables.net/reference/option/columns.searchable">DataTables Reference: columns.searchable</a> //     */
- `// public boolean dtIsVisible()` - //    /** //     * @return Whether the column is visible. //     * @see <a href="https://datatables.net/reference/option/columns.visible">DataTables Reference: columns.visible</a> //     */
- `YadaDTColumns dtAriaTitle(String ariaTitle)`
- `YadaDTColumns dtCellType(String cellType)`
- `YadaDTColumns dtClassName(String className)`
- `YadaDTColumns dtContentPadding(String contentPadding)`
- `YadaDTColumns dtCreatedCell(String createdCell)`
- `YadaDTColumns dtDataNull()` - Use the original data source for the row rather than plucking data directly from it.
- `YadaDTColumns dtData(Integer index)` - This property can be used to read and write data to and from any data source property. 			This is the default that DataTables uses (incrementally increased for each column).
- `YadaDTColumns dtData(String path)` - This property can be used to read and write data to and from any data source property. Set to null to use the original data source for the row
- `YadaDTColumns dtDataFunction(String function)` - This property can be used to read and write data to and from any data source property. 	or get the data for a cell in the column.
- `YadaDTColumns dtDefaultContent(String htmlContent)` - Static content in a column or default content when the data source is null.
- `YadaDTColumns dtFooter(String footer)`
- `YadaDTColumns dtName(String name)` - Set a descriptive name for a column.
- `YadaDTColumns dtOrderData(Integer orderData)`
- `YadaDTColumns dtOrderDataType(String orderDataType)`
- `YadaDTColumns dtOrderSequence(String orderSequence)`
- `YadaDTColumns dtOrderableOff()` - Disable ordering for the column.
- `YadaDTColumns dtRender(String renderFunction)`
- `YadaDTColumns dtResponsivePriority(int responsivePriority)` - In a responsive table control the order in which columns are hidden. Responsive will automatically remove columns from the right-hand-side of the table when a table is too wide for a given display, unless this value is set. 			are given a higher priority (i.e. a column with priority 2 will be 			hidden before a column with priority 1). The default is 10000.
- `YadaDTColumns dtSearchableOff()` - Disables searching for this column.
- `YadaDTColumns dtTitle(String title)`
- `YadaDTColumns dtVisibleOff()` - Sets a column as not visible.
- `YadaDTColumns dtWidth(String width)`

=== YadaDTOptions (YadaWeb/src/main/java/net/yadaframework/web/datatables/options/YadaDTOptions.java)
- `YadaDTOptions(YadaDataTable parent)`
- `// public YadaDTOptions dtAjax(String ajaxUrl)` - //    /** //     * The URL where data is fetched from (without language in the path). Can contain thymeleaf expressions. //     * //     * There is no other ajax options available because this is the only one that makes sense in Yada Framework. //     * This can still be custonmized in a yada:preprocessor //     * //     * @param ajaxUrl the url that returns table data //     * @return this instance for method chaining //     * @see <a href="https://datatables.net/reference/option/ajax">ajax</a> //     */
- `YadaDTOptions dtData(List<Object> data)` - Sets the `data` option. This is normally not used as data is fetched from the server in Yada Framework.
- `YadaDTOptions dtDataTableExtErrMode(String dataTableExtErrMode)` - Sets the `dataTableExtErrMode` option.
- `YadaDTOptions dtAutoWidthOff()` - Disable automatic column width calculation as an optimisation.
- `YadaDTOptions dtCaption(String caption)` - Sets the `caption` option.
- `YadaDTOptions dtCreatedRow(String createdRow)` - Sets the `createdRow` option.
- `YadaDTOptions dtDeferLoading(int totItems)` - Enables deferred loading, and instructs DataTables as to how many items are in the full data set
- `YadaDTOptions dtDeferLoading(int filteredItems, int totItems)` - Enables deferred loading, where the first data index tells DataTables how many rows are in the filtered result set, and the second how many in the full data set without filtering applied.
- `YadaDTOptions dtDeferRenderOff()` - Do not wait to create HTML elements when they are needed	for display. The only reason to use this option is if you must have all DOM elements available, even those currently not in the document.
- `YadaDTOptions dtDestroy()` - Initialise a new DataTable as usual, but if there is an existing DataTable which matches the selector, it will be destroyed and replaced with the new table.
- `YadaDTOptions dtTypeDetectOff()` - Disable the auto type detection that DataTables performs. This might be useful if you are using server-side processing where data can change between pages and the client-side is unable to automatically reliably determine a column's data type.
- `YadaDTOptions dtDisplayStart(Integer displayStart)` - Sets the `displayStart` option.
- `YadaDTOptions dtDrawCallback(String drawCallback)` - Sets the `drawCallback` option.
- `YadaDTOptions dtFooterCallback(String footerCallback)` - Sets the `footerCallback` option.
- `YadaDTOptions dtFormatNumber(String formatNumber)` - Sets the `formatNumber` option.
- `YadaDTOptions dtHeaderCallback(String headerCallback)` - Sets the `headerCallback` option.
- `YadaDTOptions dtInfoOff()` - Disables showing details about the table including information about filtered data if that action is being performed.
- `YadaDTOptions dtInfoCallback(String infoCallback)` - Sets the `infoCallback` option.
- `YadaDTOptions dtInitComplete(String initComplete)` - Sets the `initComplete` option.
- `YadaDTOptions dtLayout(String layout)` - Sets the `layout` option. Fluent api not implemented yet.
- `YadaDTOptions dtLengthChangeOff()` - Disables user's ability to change the paging display length of the table.
- `YadaDTOptions dtLengthMenu(int ... lengthChoice)` - Sets the `lengthMenu` option.
- `YadaDTOptions dtOrderClassesOff()` - Disable the addition of ordering classes to the columns when performance is an issue.
- `YadaDTOptions dtOrderDescReverseOff()` - Disable the default reverse.
- `YadaDTOptions dtOrderFixed(Object orderFixed)` - Sets the `orderFixed` option. Fluent api not implemented yet.
- `YadaDTOptions dtOrderMultiOff()` - Disables multiple column ordering ability
- `YadaDTOptions dtOrderingOff()` - Disables ordering (sorting) abilities
- `YadaDTOptions dtOrder(int idx, String dir)` - Sets the `order` option. Can be called multiple times to set multiple orders. There should be no need to use this directly because this functionality is implemented by the dtColumnObj() fluent interface.
- `YadaDTOptions dtPageLength(Integer pageLength)` - Sets the `pageLength` option that tells how many rows should be visible.
- `YadaDTOptions dtPagingOff()` - Disable table pagination.
- `YadaDTOptions dtPreDrawCallback(String preDrawCallback)` - Sets the `preDrawCallback` option.
- `YadaDTOptions dtProcessingOn()` - Enable the display of a 'processing' indicator when the table is being processed (e.g. a sort) for server-side processing. This is particularly useful for tables with large amounts of data where it can take a noticeable amount of time to sort the entries.
- `YadaDTOptions dtRenderer(String renderer)` - Sets the `renderer` option.
- `YadaDTOptions dtRowCallback(String rowCallback)` - This callback allows you to 'post process' each row after it have been generated for each table draw, but before it is rendered into the document.
- `YadaDTOptions dtRowId(String rowId)` - DataTables will attempt to automatically read an id value from the data source for each row using the property defined by this option. By default it is DT_RowId but can be set to any other name.
- `YadaDTOptions dtScrollCollapseOn()` - Allow the table to reduce in height when a limited number of rows are shown.
- `YadaDTOptions dtScrollXOn()` - Enable horizontal scrolling.
- `YadaDTOptions dtScrollY(String cssHeight)` - Enable vertical scrolling. Vertical scrolling will constrain the DataTable to the given height.
- `YadaDTOptions dtSearchDelay(Integer searchDelayMillis)` - Set a delay for search operations. This can be of particular use when using server-side processing and you don't want every keystroke to trigger an Ajax request for data.
- `YadaDTOptions dtSearchingOff()` - Disable searching abilities in DataTables.
- `YadaDTOptions dtServerSideOff()` - Disable server-side processing. This will prevent DataTables from making an Ajax request for data.
- `YadaDTOptions dtStateDuration(Integer stateDuration)` - Duration for which the saved state information is considered valid. After this period has elapsed the state will be returned to the default.
- `YadaDTOptions dtStateLoadCallback(String stateLoadCallback)` - Callback that defines where and how a saved state should be loaded.
- `YadaDTOptions dtStateLoadParams(String stateLoadParams)` - Callback which allows modification of the saved state prior to loading that state.
- `YadaDTOptions dtStateLoaded(String stateLoaded)` - Callback that is fired once the state has been loaded and the saved data manipulated.
- `YadaDTOptions dtStateSaveOn()` - Enable state saving such as pagination position, display length, filtering and sorting. When this initialisation option is active and the end user reloads the page the table's state will be altered to match what they had previously set up.
- `YadaDTOptions dtStateSaveCallback(String stateSaveCallback)` - Callback that defines how the table state is stored and where.
- `YadaDTOptions dtStateSaveParams(String stateSaveParams)` - Callback which allows modification of the parameters to be saved for the DataTables state saving prior to the data actually being saved.
- `YadaDTOptions dtTabIndex(Integer tabIndex)` - Sets the `tabIndex` overrule option.
- `YadaDTColumnDef dtColumnDefsObj()`
- `YadaDTColumns dtColumnsObj()`
- `YadaDTOptions dtResponsiveOn()` - Sets the responsive option so that the table will adapt for different screen sizes.
- `YadaDTResponsive dtResponsiveObj()` - Turns on responsive behaviour and provides access to the responsive configuration options for DataTables.

=== YadaDTOrder (YadaWeb/src/main/java/net/yadaframework/web/datatables/options/YadaDTOrder.java)
- `int getIdx()`
- `String getDir()`

=== YadaDTResponsive (YadaWeb/src/main/java/net/yadaframework/web/datatables/options/YadaDTResponsive.java)
- `YadaDTResponsive(YadaDTOptions parent)`
- `YadaDTBreakpoint dtBreakpointsObj()` - Add one breakpoint at which the table will change its size for responsive behavior. Should normally be called multiple times.
- `YadaDTResponsive dtDetailsFalse()` - Disable the child rows completely (columns will simply be removed and their content not be accessible other than through the DataTables API).
- `YadaDTResponsive dtOrthogonal(String orthogonal)` - Set the orthogonal data request type for the hidden information display.
- `YadaDTResponsiveDetails dtDetailsObj()` - Enable and configure the child rows shown by Responsive for collapsed tables.

=== YadaDTResponsiveDetails (YadaWeb/src/main/java/net/yadaframework/web/datatables/options/YadaDTResponsiveDetails.java)
- `YadaDTResponsiveDetails(YadaDTResponsive parent)`
- `YadaDTResponsiveDetails dtDisplay(String display)` - The function given is responsible for showing and hiding the child data. <ul> <li>"DataTable.Responsive.display.childRowImmediate" will show the child rows immediately without need of clicking on the control</li> <li>"DataTable.Responsive.display.modal()" will show the child rows in a modal dialog</li> <ul>
- `YadaDTResponsiveDetails dtRenderer(String renderer)` - Sets the function that will be used to render the child row when the table is responsive.
- `YadaDTResponsiveDetails dtTarget(Integer target)` - Column for child row display control when using column details type. This column is the one where the user needs to click in order to show the hidden details. 				This can be >=0 to count columns from the left, or <0 to count from the right.
- `YadaDTResponsiveDetails dtTarget(String target)` - Selector for child row display control when using column details type. 			control for the details child rows. This provides the ability to use any element 			in a table - for example you can use the whole row, or a single img element in the row.
- `YadaDTResponsiveDetails dtTypeColumn()` - Sets the "column" type of control for displaying the child rows i.e. use a whole column to display the control element. This means that you need to click on a cell to show the hidden columns and the cell is in the column set with dtTarget().
- `YadaDTResponsiveDetails dtTypeNone()` - Don't show the show / hide icons.


== net.yadaframework.web.datatables.proxy
=== YadaDTColumnDefProxy (YadaWeb/src/main/java/net/yadaframework/web/datatables/proxy/YadaDTColumnDefProxy.java)
- `YadaDTColumnDefProxy(YadaDTOptions parent)`
- `Object getTargets()`
- `String getAriaTitle()`
- `String getCellType()`
- `String getClassName()`
- `String getContentPadding()`
- `String getCreatedCell()`
- `String getDefaultContent()`
- `String getFooter()`
- `String getName()`
- `List<Integer> getOrderData()`
- `String getOrderDataType()`
- `List<String> getOrderSequence()`
- `String getWidth()`
- `String getTitle()`
- `String getRender()`
- `Integer getResponsivePriority()`
- `Boolean isOrderable()`
- `Boolean isSearchable()`
- `Boolean isVisible()`

=== YadaDTColumnsProxy (YadaWeb/src/main/java/net/yadaframework/web/datatables/proxy/YadaDTColumnsProxy.java)
- `YadaDTColumnsProxy(YadaDTOptions parent)`
- `YadaDTOptions getParent()`
- `Object getData()`
- `String getAriaTitle()`
- `String getCellType()`
- `String getClassName()`
- `String getContentPadding()`
- `String getCreatedCell()`
- `String getDefaultContent()`
- `String getFooter()`
- `String getName()`
- `List<Integer> getOrderData()`
- `String getOrderDataType()`
- `List<String> getOrderSequence()`
- `String getWidth()`
- `String getTitle()`
- `String getRender()`
- `Integer getResponsivePriority()`
- `Boolean isOrderable()`
- `Boolean isSearchable()`
- `Boolean isVisible()`

=== YadaDTOptionsProxy (YadaWeb/src/main/java/net/yadaframework/web/datatables/proxy/YadaDTOptionsProxy.java)
- `YadaDTOptionsProxy(YadaDataTable parent)`
- `YadaDTColumns addNewColumn(int pos)` - Add a new column at the specified position.
- `Object getResponsive()`
- `String getDataTableExtErrMode()`
- `Boolean getAutoWidth()`
- `String getCaption()`
- `List<YadaDTColumnDef> getColumnDefs()`
- `List<YadaDTColumns> getColumns()`
- `String getCreatedRow()`
- `Object getData()`
- `Object getDeferLoading()`
- `Boolean getDeferRender()`
- `Boolean getDestroy()`
- `Boolean getTypeDetect()`
- `Integer getDisplayStart()`
- `String getDrawCallback()`
- `String getFooterCallback()`
- `String getFormatNumber()`
- `String getHeaderCallback()`
- `Boolean getInfo()`
- `String getInfoCallback()`
- `String getInitComplete()`
- `Object getLayout()`
- `Boolean getLengthChange()`
- `int[] getLengthMenu()`
- `List<YadaDTOrder> getOrder()`
- `Boolean getOrderClasses()`
- `Boolean getOrderDescReverse()`
- `Object getOrderFixed()`
- `Boolean getOrderMulti()`
- `Boolean getOrdering()`
- `Integer getPageLength()`
- `Boolean getPaging()`
- `String getPreDrawCallback()`
- `Boolean getProcessing()`
- `String getRenderer()`
- `// public YadaDTResponsive getYadaDTResponsive()`
- `String getRowCallback()`
- `String getRowId()`
- `Boolean getScrollCollapse()`
- `Boolean getScrollX()`
- `Integer getSearchDelay()`
- `Boolean getSearching()`
- `Boolean getServerSide()`
- `Integer getStateDuration()`
- `String getStateLoadCallback()`
- `String getStateLoadParams()`
- `String getStateLoaded()`
- `Boolean getStateSave()`
- `String getStateSaveCallback()`
- `String getStateSaveParams()`
- `Integer getTabIndex()`
- `void setOrderMulti(boolean multi)` - Enable/disable multiple column ordering ability This has been added for internal use only.

=== YadaDataTableButtonProxy (YadaWeb/src/main/java/net/yadaframework/web/datatables/proxy/YadaDataTableButtonProxy.java)
- `YadaDataTableButtonProxy(String text, YadaDataTableHTML parent)`
- `boolean isMultiRow()`
- `String getElementLoader()`
- `String getType()`
- `boolean isGlobal()`
- `String getUrl()`
- `String getUrlProvider()`
- `String getText()`
- `String getIcon()`
- `String getToolbarCssClass()`
- `String getIdName()`
- `boolean isAjax()`
- `Boolean isHidePageLoader()`
- `String getWindowTarget()`
- `String getWindowFeatures()`
- `String getShowCommandIcon()`
- `Set<Integer> getRoles()`
- `YadaDataTableConfirmDialog getConfirmDialog()`

=== YadaDataTableColumnProxy (YadaWeb/src/main/java/net/yadaframework/web/datatables/proxy/YadaDataTableColumnProxy.java)
- `YadaDataTableColumnProxy(String headerText, String data, YadaDataTableHTML parent)`
- `String getName()`
- `String getHeaderText()`
- `void setPositionInTable(int position)`

=== YadaDataTableConfirmDialogProxy (YadaWeb/src/main/java/net/yadaframework/web/datatables/proxy/YadaDataTableConfirmDialogProxy.java)
- `YadaDataTableConfirmDialogProxy(YadaDataTableButton parent, YadaDataTableHTML yadaDataTableHTML)`
- `String getConfirmTitle()`
- `String getConfirmOneMessage()`
- `String getConfirmManyMessage()`
- `String getConfirmButtonText()`
- `String getAbortButtonText()`

=== YadaDataTableHTMLProxy (YadaWeb/src/main/java/net/yadaframework/web/datatables/proxy/YadaDataTableHTMLProxy.java)
- `YadaDataTableHTMLProxy(YadaDataTable parent, YadaDTOptionsProxy options)`
- `void prepareConfiguration()`
- `boolean isCommandsTitle()`
- `boolean isSelectCheckbox()`
- `String getSelectCheckboxTitle()`
- `List<YadaDataTableColumnProxy> getColumns()`
- `String getCssClasses()`
- `String getCommandsTitle()`
- `Boolean isShowFooter()`
- `List<YadaDataTableButtonProxy> getButtons()`
- `// public Map<Integer, YadaDataTableColumn> getOrderingMap()`
- `// public YadaDTOptionsProxy getOptions()`

=== YadaDataTableLanguageProxy (YadaWeb/src/main/java/net/yadaframework/web/datatables/proxy/YadaDataTableLanguageProxy.java)
- `YadaDataTableLanguageProxy(String languageBaseUrl, YadaDataTable parent)`
- `String getLanguageUrl(String language)`

=== YadaDataTableProxy (YadaWeb/src/main/java/net/yadaframework/web/datatables/proxy/YadaDataTableProxy.java)
- `YadaDataTableProxy(String id, Locale locale)`
- `String getSecurityAsPath()`
- `void setSecurityAsPath(String securityAsPath)`
- `void setConfigurer(YadaDataTableConfigurer configurer)` - Set the configurer used to create this instance, for internal use.
- `Object getConfigurer()` - Gets the configurer used to create this instance, for internal use.
- `YadaDTOptions getOptions()`
- `String getId()`
- `YadaDataTableHTML getStructure()`
- `String getAjaxUrl()`
- `String getLanguageUrl()`
- `Class getEntityClass()`


== net.yadaframework.web.dialect
=== YadaAjaxAttrProcessor (YadaWeb/src/main/java/net/yadaframework/web/dialect/YadaAjaxAttrProcessor.java)
- `YadaAjaxAttrProcessor(final String dialectPrefix)`
- `// public int getPrecedence()`

=== YadaBrOnFirstSpaceAttrProcessor (YadaWeb/src/main/java/net/yadaframework/web/dialect/YadaBrOnFirstSpaceAttrProcessor.java)
- `YadaBrOnFirstSpaceAttrProcessor(final String dialectPrefix, boolean escapeText)`
- `// public int getPrecedence()`

=== YadaDataTableTagProcessor (YadaWeb/src/main/java/net/yadaframework/web/dialect/YadaDataTableTagProcessor.java)
- `YadaDataTableTagProcessor(String dialectPrefix, YadaConfiguration config)`

=== YadaDialect (YadaWeb/src/main/java/net/yadaframework/web/dialect/YadaDialect.java)
- `YadaDialect(YadaConfiguration config)`
- `Set<IProcessor> getProcessors(final String dialectPrefix)`

=== YadaDialectUtil (YadaWeb/src/main/java/net/yadaframework/web/dialect/YadaDialectUtil.java)
- `YadaDialectUtil(YadaConfiguration config)`
- `String getInnerHtml(IModel model, int openNodeIndex)` - Returns the HTML contained in a tag, as a string
- `<T> T parseExpression(String value, ITemplateContext context, Class<T> resultClass)` - Parse some value as a EL expression
- `String joinStrings(String joiner, String start, String ... append)` - Concatenate some strings using the given joiner, checking that the joiner is not added when already present and it is trimmed from the result. Example: <pre>joinStrings(" - ", "a, b", "c, d")    = "a, b - c, d"</pre> <pre>joinStrings(" - ", "a, b - ", "c, d") = "a, b - c, d"</pre> <pre>joinStrings(" - ", "a, b", "c, d - ") = "a, b - c, d"</pre>
- `String makeYadaTagId(ITemplateEvent someTag)` - Returns a unique identifier on the page, for the given tag
- `String getConvertedHTMLCustomTagAttributeString(IOpenElementTag customTag, ITemplateContext context, String...ignoreAttributes)` - Retrieves a map of HTML attributes from the custom tag, where all HTML attributes are kept as they are (NO: and thymeleaf th: attributes are converted to HTML attributes when possible). The map is then converted to a comma-separated string of name=value attributes
- `String removePrefix(String value, String dialectPrefixNoColumn)` - Remove the dialect prefix from the start of the value
- `String getVersionedAttributeValue(String value)` - Browser cache bypass trick for resources. Converts "/res/xxx" into "/res-123/xxx", "/yadares/xxx" into "/yadares-7/xxx", where the number is the application build number.

=== YadaHrefAttrProcessor (YadaWeb/src/main/java/net/yadaframework/web/dialect/YadaHrefAttrProcessor.java)
- `YadaHrefAttrProcessor(final String dialectPrefix)`

=== YadaInputCounterTagProcessor (YadaWeb/src/main/java/net/yadaframework/web/dialect/YadaInputCounterTagProcessor.java)
- `YadaInputCounterTagProcessor(String dialectPrefix, YadaConfiguration config)`

=== YadaInputTagProcessor (YadaWeb/src/main/java/net/yadaframework/web/dialect/YadaInputTagProcessor.java)
- `static final int PRECEDENCE = 12000; // Must be higher than any yada: attribute (why?)`
- `YadaInputTagProcessor(String dialectPrefix, YadaConfiguration config)`

=== YadaNewlineTextAttrProcessor (YadaWeb/src/main/java/net/yadaframework/web/dialect/YadaNewlineTextAttrProcessor.java)
- `YadaNewlineTextAttrProcessor(final String dialectPrefix, boolean escapeText)`
- `// public int getPrecedence()`

=== YadaSimpleAttrProcessor (YadaWeb/src/main/java/net/yadaframework/web/dialect/YadaSimpleAttrProcessor.java)
- `YadaSimpleAttrProcessor(final String dialectPrefix, String attributeFrom, String attributeTo, YadaConfiguration config)` - Creates a new attribute processor that converts from the thymeleaf attribute to a html attribute.

=== YadaSrcAttrProcessor (YadaWeb/src/main/java/net/yadaframework/web/dialect/YadaSrcAttrProcessor.java)
- `YadaSrcAttrProcessor(final String dialectPrefix, YadaConfiguration config)`
- `// public int getPrecedence()`

=== YadaSrcsetAttrProcessor (YadaWeb/src/main/java/net/yadaframework/web/dialect/YadaSrcsetAttrProcessor.java)
- `YadaSrcsetAttrProcessor(final String dialectPrefix, YadaConfiguration config)`
- `// public int getPrecedence()`

=== YadaTextareaTagProcessor (YadaWeb/src/main/java/net/yadaframework/web/dialect/YadaTextareaTagProcessor.java)
- `static final int PRECEDENCE = 12000; // Must be higher than any yada: attribute (why?)`
- `YadaTextareaTagProcessor(String dialectPrefix, YadaConfiguration config)`


== net.yadaframework.web.flot
=== YadaFlotAxis (YadaWeb/src/main/java/net/yadaframework/web/flot/YadaFlotAxis.java)
- `* public static int GetJavascriptTimestamp(System.DateTime input) *`
- `void setTimeformat(final String timeformat)` - Sets the preferred time format for the axis. This setter will automatically change the mode to "time". Use the following guide to specify the format as a {@link String}: <pre>   %h: hours   %H: hours (left-padded with a zero)   %M: minutes (left-padded with a zero)   %S: seconds (left-padded with a zero)   %d: day of month (1-31), use %0d for zero-padding   %m: month (1-12), use %0m for zero-padding   %y: year (four digits)   %b: month name (customizable)   %p: am/pm, additionally switches %h/%H to 12 hour instead of 24   %P: AM/PM (uppercase version of %p) </pre>   For example, specifying timeformat as "%y/%m/%d" will   result in tick labels like "2000/12/24". <p>   @param timeformat What time format given axis should be displayed in.   @see <A href="http://flot.googlecode.com/svn/trunk/API.txt" target="_blank">Flot API.txt</A>
- `void setRange(final Double min, final Double max, final Double tickSize)` - The options "min"/"max" are the precise minimum/maximum value on the scale. If you don't specify either of them, a value will automatically be chosen based on the minimum/maximum data values. Note that Flot always examines all the data values you feed to it, even if a restriction on another axis may make some of them invisible (this makes interactive use more stable). <p>
- `void setRange(final Double min, final Double max)`
- `void setShow(final Boolean show)` - If you don't set the "show" option (i.e. it is null), visibility is auto-detected, i.e. the axis will show up if there's data associated with it. You can override this by setting the "show" option to true or false. <p>
- `void setPosition(final String position)` - The "position" option specifies where the axis is placed, bottom or top for x axes, left or right for y axes. The "mode" option determines how the data is interpreted, the default of null means as decimal numbers. Use "time" for time series data, see the time series data section. <p>
- `void setMode(final String mode)`
- `void setColor(final String color)` - The "color" option determines the color of the labels and ticks for the axis (default is the grid color). For more fine-grained control you can also set the color of the ticks separately with "tickColor" (otherwise it's autogenerated as the base color with some transparency). <p>
- `void setTickColor(final String tickColor)`
- `void setFont(final String font)`
- `void setMin(final Double min)`
- `void setMax(final Double max)`
- `void setAutoscaleMargin(final Double autoscaleMargin)` - The "autoscaleMargin" is a bit esoteric: it's the fraction of margin that the scaling algorithm will add to avoid that the outermost points ends up on the grid border. Note that this margin is only applied when a min or max value is not explicitly set. If a margin is specified, the plot will furthermore extend the axis end-point to the nearest whole tick. The default value is "null" for the x axes and 0.02 for y axes which seems appropriate for most cases. <p>
- `void setTransform(final String transform)` - "transform" and "inverseTransform" are callbacks you can put in to change the way the data is drawn. You can design a function to compress or expand certain parts of the axis non-linearly, e.g. suppress weekends or compress far away points with a logarithm or some other means. When Flot draws the plot, each value is first put through the transform function. Here's an example, the x axis can be turned into a natural logarithm axis with the following code: <pre>   xaxis: {     transform: function (v) { return Math.log(v); },     inverseTransform: function (v) { return Math.exp(v); }   } </pre> Similarly, for reversing the y axis so the values appear in inverse order: <pre>   yaxis: {     transform: function (v) { return -v; },     inverseTransform: function (v) { return -v; }   } </pre> Note that for finding extrema, Flot assumes that the transform function does not reorder values (it should be monotone). <p> The inverseTransform is simply the inverse of the transform function (so v == inverseTransform(transform(v)) for all relevant v). It is required for converting from canvas coordinates to data coordinates, e.g. for a mouse interaction where a certain pixel is clicked. If you don't use any interactive features of Flot, you may not need it.
- `void setInverseTransform(final String inverseTransform)`
- `void setTicks(final String ticks)` - If you don't specify any ticks, a tick generator algorithm will make some for you. The algorithm has two passes. It first estimates how many ticks would be reasonable and uses this number to compute a nice round tick interval size. Then it generates the ticks. <p> You can specify how many ticks the algorithm aims for by setting "ticks" to a number. The algorithm always tries to generate reasonably round tick values so even if you ask for three ticks, you might get five if that fits better with the rounding. If you don't want any ticks at all, set "ticks" to 0 or an empty array. <p> Another option is to skip the rounding part and directly set the tick interval size with "{@link #setTickSize(String) tickSize}". If you set it to 2, you'll get ticks at 2, 4, 6, etc. <p> Alternatively, you can specify that you just don't want ticks at a size less than a specific tick size with "{@link #setMinTickSize(String) minTickSize}". <p> Note that for time series, the format is an array like [2, "month"], see the next section. <p> If you want to completely override the tick algorithm, you can specify an array for "ticks", either like this: <pre>  ticks: [0, 1.2, 2.4]</pre> Or like this where the labels are also customized: <pre>  ticks: [[0, "zero"], [1.2, "one mark"], [2.4, "two marks"]]</pre> You can mix the two if you like. <p> For extra flexibility you can specify a function as the "ticks" parameter. The function will be called with an object with the axis min and max and should return a ticks array. Here's a simplistic tick generator that spits out intervals of pi, suitable for use on the x axis for trigonometric functions: <pre>   function piTickGenerator(axis) {     var res = [], i = Math.floor(axis.min / Math.PI);     do {       var v = i * Math.PI;       res.push([v, i + "\u03c0"]);       ++i;     } while (v < axis.max);     return res;   } </pre>
- `void setTickSize(final String tickSize)`
- `void setMinTickSize(final String minTickSize)`
- `void setTickFormatter(final String tickFormatter)` - Alternatively, for ultimate control over how ticks are formatted you can provide a function to "tickFormatter". The function is passed two parameters, the tick value and an axis object with information, and should return a string. The default formatter looks like this: <pre>   function formatter(val, axis) {     return val.toFixed(axis.tickDecimals);   } </pre> The axis object has "min" and "max" with the range of the axis, "tickDecimals" with the number of decimals to round the value to and "tickSize" with the size of the interval between ticks as calculated by the automatic axis scaling algorithm (or specified by you). Here's an example of a custom formatter: <pre>   function suffixFormatter(val, axis) {     if (val > 1000000)       return (val / 1000000).toFixed(axis.tickDecimals) + " MB";     else if (val > 1000)       return (val / 1000).toFixed(axis.tickDecimals) + " kB";     else       return val.toFixed(axis.tickDecimals) + " B";   } </pre>
- `void setTickDecimals(final Integer tickDecimals)` - You can control how the ticks look like with "tickDecimals", the number of decimals to display (default is auto-detected). <p>
- `void setLabelSize(final Integer labelWidth, final Integer labelHeight)` - "labelWidth" and "labelHeight" specifies a fixed size of the tick labels in pixels. They're useful in case you need to align several plots. "reserveSpace" means that even if an axis isn't shown, Flot should reserve space for it - it is useful in combination with labelWidth and labelHeight for aligning multi-axis charts. <p>
- `void setLabelWidth(final Integer labelWidth)`
- `void setLabelHeight(final Integer labelHeight)`
- `void setReserveSpace(final Integer reserveSpace)`
- `void setTickLength(final Integer tickLength)` - "tickLength" is the length of the tick lines in pixels. By default, the innermost axes will have ticks that extend all across the plot, while any extra axes use small ticks. A value of null means use the default, while a number means small ticks of that length - set it to 0 to hide the lines completely. <p>
- `void setAlignTicksWithAxis(final Integer alignTicksWithAxis)` - If you set "alignTicksWithAxis" to the number of another axis, e.g. alignTicksWithAxis: 1, Flot will ensure that the autogenerated ticks of this axis are aligned with the ticks of the other axis. This may improve the looks, e.g. if you have one y axis to the left and one to the right, because the grid lines will then match the ticks in both ends. The trade-off is that the forced ticks won't necessarily be at natural places. <p>
- `Boolean getShow()`
- `String getPosition()`
- `String getMode()`
- `String getTimeformat()`
- `String getColor()`
- `String getTickColor()`
- `String getFont()`
- `Double getMin()`
- `Double getMax()`
- `Double getAutoscaleMargin()`
- `String getTransform()`
- `String getInverseTransform()`
- `String getTicks()`
- `String getTickSize()`
- `String getMinTickSize()`
- `String getTickFormatter()`
- `Integer getTickDecimals()`
- `Integer getLabelWidth()`
- `Integer getLabelHeight()`
- `Integer getReserveSpace()`
- `Integer getTickLength()`
- `Integer getAlignTicksWithAxis()`

=== YadaFlotGrid (YadaWeb/src/main/java/net/yadaframework/web/flot/YadaFlotGrid.java)
- `void setShow(final Boolean show)` - You can turn off the whole grid including tick labels by setting "show" to false. <p>
- `void setAboveData(final Boolean aboveData)` - "aboveData" determines whether the grid is drawn above the data or below (below is default). <p>
- `void setColor(final String color)` - "color" is the colour of the grid itself. <p>
- `void setBackgroundColor(final String backgroundColor)` - "backgroundColor" specifies the background colour inside the grid area, here null means that the background is transparent. <p>
- `void setLabelMargin(final Integer labelMargin)` - "labelMargin" is the space in pixels between tick labels and axis line. <p>
- `void setAxisMargin(final Integer axisMargin)` - "axisMargin" is the space in pixels between axes when there are two next to each other. <p>
- `void setMarkings(final String markings)` - "markings" is used to draw simple lines and rectangular areas in the background of the plot. You can either specify an array of ranges on the form { xaxis: { from, to }, yaxis: { from, to } } (with multiple axes, you can specify coordinates for other axes instead, e.g. as x2axis/x3axis/...) or with a function that returns such an array given the axes for the plot in an object as the first parameter. <p> You can set the color of markings by specifying "color" in the ranges object. Here's an example array: <pre>   markings: [ { xaxis: { from: 0, to: 2 }, yaxis: { from: 10, to: 10 }, color: "#bb0000" }, ... ] </pre> If you leave out one of the values, that value is assumed to go to the border of the plot. So for example if you only specify { xaxis: { from: 0, to: 2 } } it means an area that extends from the top to the bottom of the plot in the x range 0-2. <p> A line is drawn if from and to are the same, e.g. <pre>   markings: [ { yaxis: { from: 1, to: 1 } }, ... ] </pre> would draw a line parallel to the x axis at y = 1. You can control the line width with "lineWidth" in the range object.
- `void setBorderWidth(final Integer borderWidth)` - "borderWidth" is the width of the border around the plot. Set it to 0 to disable the border. <p>
- `void setBorderColor(final String borderColor)` - You can also set "borderColor" if you want the border to have a different color than the grid lines. <p>
- `void setMinBorderMargin(final Integer minBorderMargin)` - "minBorderMargin" controls the default minimum margin around the border - it's used to make sure that points aren't accidentally clipped by the canvas edge so by default the value is computed from the point radius. <p>
- `void setClickable(final Boolean clickable)` - If you set "clickable" to true, the plot will listen for click events on the plot area and fire a "plotclick" event on the placeholder with a position and a nearby data item object as parameters. The coordinates are available both in the unit of the axes (not in pixels) and in global screen coordinates. <p>
- `void setHoverable(final Boolean hoverable)` - Likewise, if you set "hoverable" to true, the plot will listen for mouse move events on the plot area and fire a "plothover" event with the same parameters as the "plotclick" event. If "autoHighlight" is true (the default), nearby data items are highlighted automatically. If needed, you can disable highlighting and control it yourself with the highlight/unhighlight plot methods described elsewhere. <p>
- `void setAutoHighlight(final Boolean autoHighlight)` - If "autoHighlight" is true (the default), nearby data items are highlighted automatically. If needed, you can disable highlighting and control it yourself with the highlight/unhighlight plot methods described elsewhere. <p>
- `void setMouseActiveRadius(final Integer mouseActiveRadius)` - "mouseActiveRadius" specifies how far the mouse can be from an item and still activate it. If there are two or more points within this radius, Flot chooses the closest item. For bars, the top-most bar (from the latest specified data series) is chosen. <p>

=== YadaFlotPlotOptions (YadaWeb/src/main/java/net/yadaframework/web/flot/YadaFlotPlotOptions.java)
- `// public YadaFlotPlotOptions()` - //	/** //	 * //	 */
- `// public YadaFlotPlotOptions(Object o)`
- `// public void addXAxis(final Axis xAxis)` - //	/** //	 * //	 * @param xAxis TODO //	 */
- `// public void addYAxis(final Axis yAxis)` - //	/** //	 * //	 * @param yAxis TODO //	 */
- `// public void setGrid(final Grid grid)` - //	/** //	 * //	 * @param grid TODO //	 */
- `// public void setCrossHair(final CrossHair crosshair)` - //	/** //	 * //	 * @param crosshair TODO //	 */
- `// public void setSelection(final Selection selection)` - //	/** //	 * //	 * @param selection TODO //	 */
- `// public Grid getGrid()` - //	/** //	 * //	 * @return TODO //	 */
- `// public CrossHair getCrossHair()` - //	/** //	 * //	 * @return TODO //	 */
- `// public Selection getSelection()` - //	/** //	 * @return the selection //	 */

=== YadaFlotSeriesObject (YadaWeb/src/main/java/net/yadaframework/web/flot/YadaFlotSeriesObject.java)
- `YadaFlotSeriesObject()`
- `YadaFlotSeriesObject(final String label)`
- `YadaFlotSeriesObject(final String label, final String color)`
- `void addPoint(final Object x, final Object y)`
- `void setRightYAxis()`
- `void setColor(final String color)`
- `void setData(final ArrayList<Object[]> data)`
- `void setLabel(final String label)`
- `void setXaxis(final Integer xaxis)` - The "xaxis" and "yaxis" options specify which axis to use. The axes are numbered from 1 (default), so { yaxis: 2} means that the series should be plotted against the second y axis.
- `void setYaxis(final Integer yaxis)` - The "xaxis" and "yaxis" options specify which axis to use. The axes are numbered from 1 (default), so { yaxis: 2} means that the series should be plotted against the second y axis.
- `void setClickable(final Boolean clickable)` - "clickable" and "hoverable" can be set to false to disable interactivity for specific series if interactivity is turned on in the plot, see TODO.
- `void setHoverable(final Boolean hoverable)` - "clickable" and "hoverable" can be set to false to disable interactivity for specific series if interactivity is turned on in the plot, see TODO.
- `String getColor()`
- `ArrayList<Object[]> getData()`
- `String getLabel()`
- `Integer getXaxis()`
- `Integer getYaxis()`
- `Boolean getClickable()`
- `Boolean getHoverable()`

=== YadaFlotSeriesOptions (YadaWeb/src/main/java/net/yadaframework/web/flot/YadaFlotSeriesOptions.java)
- `// public void setLines(final LinesOptions lines)` - //	/** //	 * @param lines the lines to set //	 * @see LinesOptions //	 */
- `// public void setPoints(final PointsOptions points)` - //	/** //	 * @param points the points to set //	 * @see PointsOptions //	 */
- `// public PointsOptions getPoints()` - //	/** //	 * @return the points //	 */
- `// public void setBars(final BarsOptions bars)` - //	/** //	 * @param bars the bars to set //	 * @see BarsOptions //	 */
- `// public void setShadowSize(final Integer shadowSize)` - //	/** //	 * "shadowSize" is the default size of shadows in pixels. Set it to 0 to //	 * remove shadows. //	 * //	 * @param shadowSize the shadowSize to set //	 */
- `// public Integer getShadowSize()` - //	/** //	 * @return the shadowSize //	 */
- `// public void setPointOptions(final Integer lineWidth, final Integer radius)` - //	/** //	 * Helper method to set Point options. //	 * //	 * @param lineWidth TODO //	 * @param radius TODO //	 */
- `// public void setPointOptions()` - //	/** //	 * Helper method to set Point options. //	 * //	 */
- `// public void setLineOptions(final Integer lineWidth, final String fillColor)` - //	/** //	 * Helper method to set Line options. //	 * //	 * @param lineWidth TODO //	 * @param fillColor TODO //	 */
- `// public void setLineOptions(final Integer lineWidth)` - //	/** //	 * Helper method to set Line options. //	 * //	 * @param lineWidth TODO //	 */
- `// public LinesOptions getLines()` - //	/** //	 * @return the lines //	 */
- `// public void setLineOptions()` - //	/** //	 * Helper method to set Line options. //	 */
- `// public void setBarOptions(final Integer lineWidth, final String fillColor)` - //	/** //	 * Helper method to set Line options. //	 * //	 * @param lineWidth TODO //	 * @param fillColor TODO //	 */
- `// public BarsOptions getBars()` - //	/** //	 * @return the bars //	 */
- `// public void setBarOptions()` - //	/** //	 * Helper method to set Bar options. //	 */


== net.yadaframework.web.form
=== YadaFormFieldMap (YadaWeb/src/main/java/net/yadaframework/web/form/YadaFormFieldMap.java)
- `Map<String, String> fieldMap = new HashMap<>();`
- `Map<String, String> getMap()`

=== YadaFormPasswordChange (YadaWeb/src/main/java/net/yadaframework/web/form/YadaFormPasswordChange.java)
- `String getPassword()`
- `void setPassword(String password)`
- `String getToken()`
- `void setToken(String token)`
- `String getUsername()`
- `void setUsername(String username)`
- `String getConfirmPassword()`
- `void setConfirmPassword(String confirmPassword)`


== net.yadaframework.web.social
=== YadaFacebookRequestV9 (YadaWeb/src/main/java/net/yadaframework/web/social/YadaFacebookRequestV9.java)
- `static YadaFacebookRequestV9 postLinkToPage(String pageId, String link, String pageAccessToken)` - Factory method to create a "post link to page" request. See the <a href="https://developers.facebook.com/docs/pages/publishing/">Facebook docs</a>.

=== YadaSocial (YadaWeb/src/main/java/net/yadaframework/web/social/YadaSocial.java)
- `String execute(YadaFacebookRequest yadaFacebookRequest) throws YadaSocialException` - Execute a Facebook request

