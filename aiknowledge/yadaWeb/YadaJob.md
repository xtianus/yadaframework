# `net.yadaframework.persistence.entity.YadaJob`

| Method | Description |
|---|---|
| `activate` | Set the job state to active and return the previous state |
| `pause` | Set the job state to paused and return the previous state |
| `disable` | Set the job state to disabled and return the previous state |
| `complete` | Set the job state to completed and return the previous state |
| `incrementErrorStreak` | Increment the error streak count returning the new value |
